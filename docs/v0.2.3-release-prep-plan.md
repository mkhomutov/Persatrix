# v0.2.3 Release Preparation Plan

**Status**: 🚧 In progress
**Target version**: v0.2.3 (Observability Foundation — RFCs 0018 + 0019)
**Created**: 2026-04-24
**Branch prefix**: `feature/v023-release-prep-`
**Target**: `main`
**Merge strategy**: Squash merge per [BRANCHING.md](BRANCHING.md)

**Scope**: manual test execution report for the RFC 0018 + 0019 surface, documentation refresh for the observability stack (README, ROADMAP, guide callouts, an observability architecture diagram), release checklist, version bump, changelog curation (the `[Unreleased] — v0.2.3` section is already seeded with operator-visible changes and needs the usual Highlights / Features / Bug Fixes / Upgrade Notes structure layered on), final pre-tag verification.
**Out of scope**: new feature work; RFCs 0018 (7/7) and 0019 (5/5) are both `✅ Implemented` — one optional polish PR also merged (RFC 0018 PR 8 — #177; RFC 0019 PR 6 — #176). No new RPCs, no new REST surface, no new CLI subcommand beyond `persatrix logs` (which shipped in RFC 0018 PR 6 and is the primary new user-facing surface).

**Documentation timing policy**:
- **Before release (required)**: update public docs for all merged behavior that will ship in `v0.2.3` so release-day documentation is accurate.
- **After release (follow-up)**: a small docs pass for release artifacts that only exist post-tag (version/tag links, release URL, published-date references).

---

## Progress Overview

Update this table as each PR merges. Flip **Status** and add GitHub PR number + merge date.

| PR | Track | Title | Branch | Status | GitHub PR | Merged |
|----|-------|-------|--------|--------|-----------|--------|
| 0 | — | v0.2.3 release prep plan (this document) | `feature/v023-release-prep-plan` | 🔀 PR open | TBD | — |
| 1 | A | Manual test execution report (v0.2.3 surface) | `feature/v023-release-prep-mt-exec` | ⬜ Not started | — | — |
| 2 | A | README + ROADMAP + guide refresh + observability diagram + release checklist | `feature/v023-release-prep-docs` | ⬜ Not started | — | — |
| 3 | B | Version bump (`agents/pyproject.toml`, `cli/Cargo.toml`, `cli/Cargo.lock`) + changelog curation | `feature/v023-release-prep-version-bump` | ⬜ Not started | — | — |
| 4 | B | Final pre-tag verification & release notes | `feature/v023-release-prep-final` | ⬜ Not started | — | — |

**Status legend**: ⬜ Not started · 🔄 In progress · 🔀 PR open · ✅ Merged

---

## Dependency Graph

```
PR 0 (this plan) ──► PR 1 (MT execution report) ──► PR 2 (docs + checklist)
                                                             │
                                                             ▼
                                                   PR 3 (version bump + changelog) ──► PR 4 (final)
```

Hard dependencies:
- **PR 2** waits on PR 1 (release checklist references the execution-report path; README "Known limitations in v0.2.3" pulls from the accepted-with-known-gap rows).
- **PR 3** waits on PR 2 (version bump runs after checklist confirms readiness).
- **PR 4** waits on PR 3; it is the pre-tag gate.

Soft coordination:
- PR 0 does not hard-block PR 1 — the MT execution report can begin in parallel once this plan is public.

---

## Current state (baseline, 2026-04-24)

### RFC 0018 — Complete (7/7 merged)

| PR | GitHub | Title |
|----|--------|-------|
| 1 | #164 | Schema doc + Python structlog chain + redactor surface |
| 2 | #165 | Go zap rename + pretty + redactor wired + source |
| 3 | #168 | Cross-process correlation IDs + OTEL trace IDs on logs |
| 4 | #172 | Phase 4a `log_service.proto` + ring buffer + disk store + rate limiter |
| 5 | #173 | `LogService` server + agent shipper + REST + SSE |
| 6 | #174 | `persatrix logs` CLI rewrite + filters + SSE follow + E2E |
| 7 | #180 | Closeout — review follow-ups + status flip |
| 8 (polish, optional) | #177 | Log buffer + shipper polish |

Also merged during this phase and tied to the logging surface:
- #183 — issue #178 zap encoder correctness cluster (Must-style ctor + reserved-key shadowing)
- #182 — issue #179 Should-Fix correctness cluster (sentinel collision + timestamp policy + SSE write deadline)
- #184 — MT-LOGS-001 follow-up: tee orchestrator zap entries into log buffer
- #185 — MT-OTEL-001 walkthrough alignment + propagation-gap surfacing

### RFC 0019 — Complete (5/5 merged, plus 1 optional-polish PR)

| PR | GitHub | Title |
|----|--------|-------|
| 1 | #163 | `internal/telemetry` → `internal/observability` rename + Python OTEL init + gRPC + Baggage |
| 2 | #167 | Semantic spans + Span Links + Gen-AI conventions |
| 3 | #170 | OTEL metrics (Python + Go) with exemplars |
| 4 | #171 | Collector pipeline + docker-compose + E2E + schema-parity test |
| 5 | #181 | Closeout — review follow-ups + status flip |
| 6 (polish, optional) | #176 | Tracing/spans review follow-ups |

### Version state

Both shipped components are still at `0.2.2` — needs bump to `0.2.3`:

| Component | File | Current |
|-----------|------|---------|
| Python agents | `agents/pyproject.toml` | `0.2.2` |
| Rust CLI | `cli/Cargo.toml` | `0.2.2` |
| Go orchestrator | git tag | `v0.2.2` |

### Documentation state

- **README.md** — mentions the Jaeger / OTLP / Collector host-port changes and the new observability stack in the quickstart ports block and in the top-level feature matrix, but has no dedicated "What's added in v0.2.3" section or Upgrade-notes block (added in PR 2).
- **ROADMAP.md** — already has a detailed v0.2.3 phase entry with "What ships in v0.2.3 / What does not ship in v0.2.3 / Planned Components" sub-sections; flip the milestone row to `✅ Released` in PR 4.
- **docs/observability.md** — comprehensive operational reference for the structured-log schema plus §11 Observability stack (Jaeger / Prometheus / Loki / Collector); no v0.2.3-specific edits expected in this release-prep pass unless the MT execution report surfaces a doc gap.
- **docs/guides/persona-agents.md** — no observability callout yet; PR 2 adds a short pointer to `docs/observability.md` and the trace-id surfacing inside persona logs.
- **docs/diagrams/** — system, component, workflow, persona-runtime, memory-architecture diagrams. No observability-stack diagram (Collector pipeline, trace/log/metric flow, `persatrix logs` flow). Added in PR 2.
- **docs/v0.2.3-release-checklist.md** — does not exist yet. Created in PR 2 using the v0.2.2 checklist as a structural template.

### Manual test state

- Two new tests authored on 2026-04-24 cover the v0.2.3 surface:
  - `MT-LOGS-001` — `persatrix logs` end-to-end (REST + SSE follow + restart durability + pretty mode).
  - `MT-OTEL-001` — OpenTelemetry traces + metrics + Collector tail-sampling (trace-to-metric-exemplar click-through, tail-sampling categories).
- 19 tests carried forward from the v0.2.2 suite (same IDs as listed in [`docs/v0.2.2-release-checklist.md`](v0.2.2-release-checklist.md) §4).
- Total for v0.2.3 execution: **21 tests**.
- No v0.2.3 execution report exists yet. Authored in PR 1.

### Changelog state

- `CHANGELOG.md` already has an `[Unreleased] — v0.2.3` section seeded with the operator-visible changes (Jaeger OTLP host-port unpublish, Python OTLP transport switch grpc→http, Go package rename `internal/telemetry` → `internal/observability`, zap reserved-key renames).
- Needs reshaping in PR 3 into a curated `[0.2.3] - YYYY-MM-DD` section with Highlights / Features / Bug Fixes / Documentation / Testing / Upgrade Notes structure, preserving the seeded operator-visible content verbatim under the Upgrade Notes subsection.
- Existing curated `[0.2.2]`, `[0.2.1]`, `[0.2.0]`, and `[0.1.0]` sections must be preserved verbatim.

### Dependency notices state

- `THIRD_PARTY_NOTICES.md` was last regenerated in #169 for PRs #163–#168. New dependencies pulled in by later v0.2.3 PRs (zap ecosystem polish, OTLP exporter switch to HTTP on the Python side, etc.) require one more `make notices` sweep during PR 4 and a commit of any diff.

---

## Track A — Documentation + Manual Tests

### PR 1 — Manual Test Execution Report (v0.2.3 surface)

**Branch**: `feature/v023-release-prep-mt-exec`

Scope:

- Create `docs/manual-tests/v0.2.3-execution-report.md` using the v0.2.2 execution report
  ([`docs/manual-tests/v0.2.2-execution-report.md`](manual-tests/v0.2.2-execution-report.md))
  as a structural template.
- Execute all 21 tests on a clean checkout against the `main` tip used as the release candidate;
  record date, tester, environment (Windows + Docker Desktop WSL2), commands run, and evidence.
- The two new observability tests (`MT-LOGS-001`, `MT-OTEL-001`) are the primary gates for the
  v0.2.3 surface; the 19 carried-forward tests regression-check the v0.2.1 / v0.2.2 surface under
  the new structured-logging + OTEL code paths.
- Any `Accepted-with-known-gap` outcome must cite a GitHub issue or an RFC-closeout follow-up
  bullet so release-note §6 (Known Gaps) can reference it.

Acceptance:

- Execution report exists, is marked `✅ Complete`, and every test row has an outcome of
  `Pass` or `Accepted-with-known-gap` (zero `Fail`).
- Outcomes link back to commands run and evidence (log snippets, Jaeger screenshots described
  in text, `curl` output) so a reviewer can reproduce without re-running the entire suite.

---

### PR 2 — README + ROADMAP + Guide Refresh + Observability Diagram + Release Checklist

**Branch**: `feature/v023-release-prep-docs`

Scope:

- **README.md**
  - Add a "What's added in v0.2.3" section summarising: structured JSON logs on a versioned
    schema (RFC 0018); distributed traces across Go/Python with Gen-AI semantic conventions
    (RFC 0019); OTLP metrics with histogram exemplars; W3C Baggage propagation across the gRPC
    boundary; tail-sampling Collector pipeline; `persatrix logs` CLI.
  - Update the roadmap table: v0.2.3 row → `🚧 Release prep`; flip to `✅ Released` in PR 4.
  - Add a "Known Limitations in v0.2.3" subsection summarising carry-forward gaps plus any new
    v0.2.3-scope `Accepted-with-known-gap` rows surfaced by PR 1 (e.g. the MT-LOGS-001
    seal-on-completion warm-load gap flagged in the test document itself).
  - Surface the Upgrade-notes items already seeded in `CHANGELOG.md` under a brief README
    Upgrade-notes callout pointing to the curated changelog section PR 3 will produce.
- **ROADMAP.md** — trim the running "Current milestone" one-liner now that RFCs 0018 + 0019 are
  both ✅; update the "Last updated" date; mark the v0.2.3 header as `🚧 Release prep`.
- **docs/guides/persona-agents.md** — short callout that v0.2.3 emits structured JSON logs on a
  versioned schema and that tick-loop / event-dispatch / LLM-call spans are visible in Jaeger;
  link to [`docs/observability.md`](observability.md) and RFCs 0018 + 0019.
- **docs/diagrams/** — add a new `observability-stack.md` diagram showing: orchestrator (zap) +
  agents (structlog) → `LogService` shipper → orchestrator ring buffer + disk store → REST/SSE
  → `persatrix logs` CLI; orchestrator + agents → OTLP → Collector → {Jaeger, Prometheus, Loki};
  baggage + trace-id propagation across the gRPC boundary. Link from `docs/diagrams/README.md`
  and from `docs/observability.md` §11.
- **docs/v0.2.3-release-checklist.md** — create using the v0.2.2 checklist as a structural
  template; wire §3.1 Upgrade Notes directly from the `[Unreleased] — v0.2.3` seeded content
  so there is one canonical source of Upgrade-notes text before PR 3 finalises the changelog.

Acceptance:

- `make validate` passes on the updated docs.
- README roadmap table lists v0.2.3 as `🚧 Release prep`.
- Persona-agents guide links `docs/observability.md`.
- Observability diagram renders (Mermaid or ASCII; consistent with the style of existing
  `docs/diagrams/*.md`).
- Release checklist references the execution-report path and lists every gate needed before
  tagging, including `make notices` and a Docker-compose smoke test covering the `persatrix
  logs` end-to-end path and a Jaeger trace-lookup step.

---

## Track B — Release Engineering

### PR 3 — Version Bump + Changelog Curation

**Branch**: `feature/v023-release-prep-version-bump`

Scope:

- Bump `agents/pyproject.toml` `version` to `0.2.3`.
- Bump `cli/Cargo.toml` `version` to `0.2.3`.
- Regenerate `cli/Cargo.lock` via `cargo generate-lockfile --offline` (or a clean build) and
  commit.
- Run `make all` to confirm builds pass at the new version.
- Curate the `[0.2.3]` changelog section:
  - Convert the current `[Unreleased] — v0.2.3` content into `## [0.2.3] - YYYY-MM-DD`.
  - Layer on Highlights / Features / Bug Fixes / Documentation / Testing subsections with the
    GitHub PR numbers already collected above (#163–#185 cluster).
  - Preserve the existing curated `[0.2.2]`, `[0.2.1]`, `[0.2.0]`, and `[0.1.0]` sections
    verbatim.
  - Preserve the seeded ⚠️ Operator-Visible Changes text verbatim (moved under an Upgrade Notes
    subsection).
- If `git-cliff --tag v0.2.3 --unreleased --prepend CHANGELOG.md` would overwrite curated
  content, generate into a temporary file and merge manually (same pattern as v0.2.2 PR #157).

Acceptance:

- `make all` builds at `0.2.3`.
- `CHANGELOG.md` contains a curated `[0.2.3]` section (Highlights, Upgrade Notes, Features,
  Bug Fixes, Documentation, Testing).
- All prior version sections untouched (`git diff` against `main` for lines above the new
  section shows zero diff).

---

### PR 4 — Final Pre-Tag Verification & Release Notes

**Branch**: `feature/v023-release-prep-final`

Scope:

- Run the full release-checklist sweep on a clean checkout:
  - `make test`, `make lint`, `make validate`, `make proto && git diff --exit-code`,
    `make check-licenses`, `make notices` (commit any updated `THIRD_PARTY_NOTICES.md`).
- Docker smoke test:
  - `make docker-build && make docker-up`, submit a workflow via `persatrix run`.
  - `persatrix logs <execution_id>` (REST round-trip).
  - `persatrix logs --follow <long_running_execution_id>` (SSE).
  - Hit `GET /api/v1/cost/summary`.
  - Look up the workflow by `persatrix.workflow_id` in Jaeger and confirm a connected trace tree.
  - Click through from a Prometheus histogram exemplar to the matching Jaeger trace.
- Flip the README roadmap row from `🚧 Release prep` to `✅ Released`.
- Update this prep-plan status to `✅ Complete` and check off every PR row.
- Update the release-checklist `Status` to `✅ Released` and check off every gate.

Tag procedure (executed after PR 4 merges):

```bash
git checkout main
git pull --ff-only origin main
git tag -a v0.2.3 -m "v0.2.3 — Observability Foundation"
git push origin v0.2.3
```

GitHub Release creation follows the v0.2.2 pattern: paste the curated `[0.2.3]` changelog as the
release body, append Upgrade Notes (§3.1 of the release checklist), append Known Gaps (§6 of the
release checklist).

---

## Status hygiene

This document is the single source of truth for v0.2.3 release-prep PR sequencing. Update the
**Progress Overview** table on every merge. When the final PR (PR 4) merges, mark **Status** at
the top to `✅ Complete` and set the release date.
