# v0.3.4 Release Checklist

> **Status**: ✅ Released — all four release-prep PRs merged (plan [#438](https://github.com/mkhomutov/Persatrix/pull/438), MT report [#439](https://github.com/mkhomutov/Persatrix/pull/439), docs + provider-neutral onboarding + knob-free refactor [#440](https://github.com/mkhomutov/Persatrix/pull/440), version bump + changelog [#442](https://github.com/mkhomutov/Persatrix/pull/442); plus the [#441](https://github.com/mkhomutov/Persatrix/pull/441) MT-recipe refresh and the [#443](https://github.com/mkhomutov/Persatrix/pull/443) ISSUE-0076 CI fix). Release-prep PR 4 (final pre-tag verification) re-certified the gate sweep on the post-version-bump tip — all green, with CI green on the RC tip covering the full suites and the live Docker smoke carried forward from the pre-bump config-driven re-run — and flipped the status docs to released. The `v0.3.4` git tag and GitHub Release are pushed after PR 4 merges per [release-prep plan Phase 5](v0.3.4-plan.md#phase-5--tag-and-post-release-follow-up).

> v0.3.4 — "Any Model, Any Provider" — ships [RFC 0033](rfcs/0033-model-alias-layer.md) Phases 1–2 (the provider-agnostic model-alias layer) plus the offline ([#422](https://github.com/mkhomutov/Persatrix/pull/422)) and Ollama ([#423](https://github.com/mkhomutov/Persatrix/pull/423)) providers. The user-facing promise: *the same agents run on any provider — Anthropic, OpenAI, a free local model (Ollama), or a $0 offline mock — selected the one standard way, by pointing a logical model alias (`quality` / `fast` / `summarizer`) at it. A vendor retirement or a provider swap is a one-line config edit, not a code change or a config sweep.* Provider selection is purely config/alias-driven — there is no env force-knob ([amendment 2026-05-27](v0.3.4-plan-amendment-2026-05-27.md)).

**Based on test report**: [`docs/manual-tests/v0.3.4-execution-report.md`](manual-tests/v0.3.4-execution-report.md)
**Release prep plan**: [`docs/v0.3.4-release-prep-plan.md`](v0.3.4-release-prep-plan.md)
**Master plan**: [`docs/v0.3.4-plan.md`](v0.3.4-plan.md)
**Amendments**: [`v0.3.4-plan-amendment-2026-05-24.md`](v0.3.4-plan-amendment-2026-05-24.md) (provider parity), [`v0.3.4-plan-amendment-2026-05-27.md`](v0.3.4-plan-amendment-2026-05-27.md) (knob-free provider selection)
**RFC**: [`docs/rfcs/0033-model-alias-layer.md`](rfcs/0033-model-alias-layer.md) (PR plan: [`0033-pr-plan.md`](rfcs/0033-pr-plan.md))

---

## 1. Pre-release Verification

All build gates run on a clean checkout of the release-candidate commit, executed by PR 4 of the release-prep plan. PR 1 already ran the full automated suite green on the `7e74873` RC tip (2553 unit / 212 integration incl. the alias cost gate + `cost-regression-gate` / 387 agents / 102 Rust / Go race-cover); PR 4 re-certified on the post-version-bump tip (`main` `3ad32b2`, which also includes the knob-free provider-selection refactor + its TDD test updates, and the [#443](https://github.com/mkhomutov/Persatrix/pull/443) CI-gating + close-path fix). **PR 4 verification (2026-05-27, tester Claude Opus 4.7).** CI is green on the exact RC tip `3ad32b2` — and per [#443](https://github.com/mkhomutov/Persatrix/pull/443) the CI `make test` job now runs the **full** integration suite, so the full unit + integration + agents-runtime suites are certified there. Locally on the post-bump tip PR 4 re-ran, all green: `make validate`, the sanitizer-parity gate, `make lint`, proto staleness (`make proto` + `git diff --exit-code`), `make check-licenses`, `make notices --check`, `go test ./internal/... -race -cover`, `cargo test`, the alias cost-attribution gate (both tests by name, fresh), the alias/provider Python unit slices (84 tests), and the bored-persona `cost-regression-gate` + close-path integration tests (7 tests). The live Docker smoke is **carried forward** from the pre-bump config-driven re-run ([#440](https://github.com/mkhomutov/Persatrix/pull/440)/[#441](https://github.com/mkhomutov/Persatrix/pull/441), HEAD `6ce23cd`) — the only delta to the RC tip is the version-string + changelog bump ([#442](https://github.com/mkhomutov/Persatrix/pull/442)) and the test-infra/CI fix ([#443](https://github.com/mkhomutov/Persatrix/pull/443)), neither touching provider routing or demo runtime (the v0.3.2 PR 4 carry-forward precedent; full evidence in the [execution report § Re-Execution](manual-tests/v0.3.4-execution-report.md#re-execution--release-prep-pr-4-post-version-bump-rc-tip)).

- [x] `make test` green on the post-bump tip — **CI green on the exact RC tip `3ad32b2`** runs Go `./internal/... -race -cover`, the full Python unit suite, the full Python integration suite (now gated in CI per [#443](https://github.com/mkhomutov/Persatrix/pull/443)), agents-runtime, and Rust. PR 4 locally re-ran the load-bearing slices green: `go test ./internal/... -race -cover` (all packages `ok`, no races), `cargo test` (**102 passed**), the refactored provider unit slices (`test_llm_offline.py` / `test_llm_ollama.py` / `test_model_aliases.py` / `test_llm_factory.py` / `test_optimization_cost_pricing.py` → **84 passed**), and the alias cost gate + `cost-regression-gate` (below).
- [x] `make lint` clean — golangci-lint clean; ruff (agents + repo-root `tests/`) clean; mypy whole-package (`cd agents && mypy .`) + `mypy tests/` → **no issues** (235 source files, tests tree); clippy clean on `persatrix v0.3.4`.
- [x] `make validate` passes — **5 configs valid**; 3 `instructions_file` refs resolve. (The per-provider demo configs under `config/demo/` are not in the validated set — `validate.py` keys on the exact `optimization.yaml` filename — but are schema-checked and exercised in the carried-forward Docker smoke below.)
- [x] Proto staleness clean — `make proto` (Go + Python stubs, mypy plugin on PATH) then `git diff --exit-code` shows **no generated drift** (RFC 0033 + the provider-selection refactor add no proto change; `LeaseRequest` is unchanged).
- [x] `make check-licenses` clean — Go / Python / Rust all OK (cargo-deny `licenses ok`).
- [x] `make notices` up to date — **no delta** (`generate_third_party_notices.py --check` → "THIRD_PARTY_NOTICES up to date"). The alias resolver, the in-process mock, and the Ollama provider (a thin `OpenAIProvider` subclass reusing the OpenAI SDK already in the tree) add no new third-party production dependency.
- [x] `make generate-sanitizer-patterns-check` passes — Go↔Python parity in sync (11 patterns, 5 sources, 2 actions).
- [x] **MT-ALIAS-001** (alias-routed agent reports correctly-keyed cost) — **primary release gate** `Pass` on the RC tip ([#439](https://github.com/mkhomutov/Persatrix/pull/439)): a `quality`-aliased agent resolved to physical `claude-sonnet-4-6`, completed a turn, and reported correctly-keyed **non-zero** cost via `GET /api/v1/cost/summary`, with `persatrix.llm.model_alias=quality` on the `agent.llm.call` span while `gen_ai.request.model` stayed the physical ID. PR 4 re-confirmed the automated Step-6 gate fresh post-bump; the live Steps 1–5 carry forward from the config-driven HEAD `6ce23cd` re-run ([#440](https://github.com/mkhomutov/Persatrix/pull/440)/[#441](https://github.com/mkhomutov/Persatrix/pull/441)).
- [x] **Alias cost-attribution gate** — release-blocker, **re-ran fresh by name post-bump**: `internal/server/cost_alias_gate_test.go` (`TestCostSummary_AliasRoutedAgent_ReportsNonZeroCost`) + `internal/cost/cost_alias_pricing_test.go` (`TestLoadCostConfig_ShippedConfig_PricesAliasPhysicalModels`) both **PASS** — the automated counterpart to MT-ALIAS-001. Since the base config ships **no default provider** ([amendment 2026-05-27](v0.3.4-plan-amendment-2026-05-27.md)), these pin the configured `config/demo/anthropic` artifact (and `config/demo/openai` for the priced peer), not the unconfigured base.
- [x] **Bored-persona `cost-regression-gate`** (carry-forward from v0.3.3) — `tests/integration/test_bored_persona_cost.py` **green on the post-bump tip** (re-ran with the close-path tests → 7 passed); stays a PR trigger, not a nightly.
- [x] **MT-ALIAS-002** (one-line provider swap) — `Pass` ([#439](https://github.com/mkhomutov/Persatrix/pull/439)): a single `quality`-alias edit re-routed the same agent to a different provider with cost re-keyed to the new physical model, reverted clean. Carried forward from the config-driven HEAD `6ce23cd` re-run (gpt-4o cost `$0.0048875`, exactly the swapped rate).
- [x] **MT-OFFLINE-001** / **MT-OLLAMA-001** — `Pass` ([#439](https://github.com/mkhomutov/Persatrix/pull/439)); the offline/Ollama factory-interplay regression (`tests/unit/python/test_llm_offline.py` / `test_llm_ollama.py`) **re-ran green post-bump** (part of the 84-test slice). The demo paths' config-driven live form is carried forward from the HEAD `6ce23cd` re-run.
- [x] **Personal-tier latency regression gate** — [`tests/perf/personal_tier_latency.py`](../tests/perf/personal_tier_latency.py) re-run on the RC tip: recall p99 = **1.99 ms**, p50 = **0.42 ms** (exit 0; informational baseline, no committed baseline file yet).
- [x] Docker smoke test — provider behaviours verified live on a freshly-rebuilt stack, each selected the config-driven way (an alias config the overlay mounts — no env force-knob, no default provider). **Carried forward** from the pre-bump config-driven re-run; the RC-tip delta is version-strings + changelog ([#442](https://github.com/mkhomutov/Persatrix/pull/442)) and test-infra/CI ([#443](https://github.com/mkhomutov/Persatrix/pull/443)), neither touching provider routing or demo runtime (the v0.3.2 PR 4 carry-forward precedent):
  - _Live run in [#440](https://github.com/mkhomutov/Persatrix/pull/440)/[#441](https://github.com/mkhomutov/Persatrix/pull/441) (HEAD `6ce23cd`): all four provider legs verified live ✅ Pass (F-5 closed) — evidence in the [execution report § Config-driven re-run](manual-tests/v0.3.4-execution-report.md#config-driven-re-run-head-6ce23cd). The bare-`up` fail-loud leg is code-evident (the `unconfigured` sentinel `SystemExit`, unit-asserted in `test_model_aliases.py` / `test_llm_factory.py`), not exercised live._
  - [x] A bare `docker compose up` (no demo) **fails loud** — agents exit at startup with the actionable "alias not configured — pick a provider" `SystemExit` (the no-default-provider behaviour; code-evident + unit-asserted, not run live).
  - [x] `make demo-anthropic` brings the society up on Claude; an alias-routed `quality` agent completes a turn with correct **non-zero** `/cost/summary` (`2708/20/$0.008424`, exactly the sonnet rate) + a `persatrix.llm.model_alias` span attribute.
  - [x] A one-line `quality.provider` swap (Anthropic → OpenAI, via `make demo-openai`) re-routes the same agent (the headline claim, live) — cost followed the swap to `$0.0048875` (exactly the gpt-4o rate).
  - [x] `make demo-offline` round-trips at `$0` with zero provider calls (`gen_ai.system=mock`, `gen_ai.request.model=offline`).
  - [x] `make demo-ollama` routes to a local model with real tokens and `$0` cloud (`gen_ai.system=ollama`, `model=llama3.2`).
  - [x] `make demo-openai` ran end-to-end with `OPENAI_API_KEY` (cloud-peer parity; transitive proof the base-compose key plumbing landed — closes MT [F-5](manual-tests/v0.3.4-execution-report.md#follow-ups)).

> **Environment note ([F-3], carry-forward from v0.3.3/v0.3.2).** An empty `ANTHROPIC_API_KEY` exported in the operator's shell can shadow a populated `.env` under Docker Compose interpolation. With v0.3.4's provider-neutral plumbing the keys use a `:-` empty default (no hard `:?` guard), so an empty shell value no longer aborts `docker compose up` — the affected provider simply logs an unset-key warning. Unset the empty shell variable (or give it the real key) before running a stack that uses that provider.

> **Local-model UX note ([F-6] / [ISSUE-0073](issues/ISSUE-0073-cpu-local-model-chat-exceeds-default-timeout.md)).** CPU-only `make demo-ollama` inference of a full reply can exceed the chat's 30 s *default* timeout; the chat endpoint's per-request `timeout_seconds` override (≤ 300 s) resolves it. Not a routing or cost defect — documented in the demo path.

---

## 2. Version Alignment

Before tagging, all shipped components must declare `0.3.4` consistently. All components are at `0.3.3`.

| Component | File | Required state for release |
|-----------|------|----------------------------|
| Python agents | `agents/pyproject.toml` | `version = "0.3.4"` |
| Python observability | `agents/observability/{metrics,tracing}.py` | `_DEFAULT_SERVICE_VERSION = "0.3.4"` (both) |
| Rust CLI | `cli/Cargo.toml` | `version = "0.3.4"` |
| Rust lockfile | `cli/Cargo.lock` | Regenerated after the CLI version bump |
| Go orchestrator | git tag | Release identified by `v0.3.4` tag |

`scripts/bump_version.py` bumps the `pyproject.toml`, `Cargo.toml`, and observability `service.version` strings; `cli/Cargo.lock` is then regenerated via `cargo update --workspace`. The observability defaults are not build inputs, so a stale value never fails `make all` — bump it explicitly.

Checklist (filled in by release-prep plan PR 3; PR 4 confirms on the post-bump tip):

- [x] `agents/pyproject.toml` → `0.3.4`
- [x] `agents/observability/{metrics,tracing}.py` → `_DEFAULT_SERVICE_VERSION = "0.3.4"` (both)
- [x] `cli/Cargo.toml` → `0.3.4`; `cli/Cargo.lock` regenerated (CLI builds as `persatrix v0.3.4`)
- [x] `make all` builds at the new version — orchestrator (`go build`) + CLI (`cargo build --release`) both at `0.3.4`
- [x] No docs describe v0.3.4 as unreleased WIP — PR 4 flipped the ROADMAP Version Map row + Current phase/milestone, this checklist's status, and the release-prep-plan status to released; only post-tag artifacts (tag link, Release URL) remain for the Phase 5 follow-up.

---

## 3. Changelog

The release changelog must preserve every existing curated section in [`CHANGELOG.md`](../CHANGELOG.md) (`[0.3.3]` and prior) and add a new `[0.3.4]` section. Unlike v0.3.3, `CHANGELOG.md` **has an `[Unreleased]` section** carrying curated 🚀 Features / 🔧 Changed / 📝 Config-Schema entries for the offline + Ollama providers and the knob-free provider-selection refactor. Release-prep PR 3 **folds that curated content into `[0.3.4]` rather than re-authoring it**, then generates the RFC 0033 + plan rows over `v0.3.3..HEAD` and reshapes into Highlights / Features / Documentation / Testing / Upgrade Notes (no Bug Fixes).

Checklist:

- [x] `CHANGELOG.md` contains a `[0.3.4]` section (PR 3); `[Unreleased]` content folded in, not duplicated
- [x] The existing curated `[0.3.3]` … `[0.1.0]` sections are preserved verbatim — not overwritten
- [x] The `[0.3.4]` section includes:
  - **Highlights**: the same agents run on any provider — Anthropic / OpenAI / Ollama / offline mock — selected the one standard way (a logical model alias's `provider` field); a vendor swap is a one-line config edit. Provider selection is purely config/alias-driven (no env force-knob).
  - **Features**: RFC 0033 PRs 1–7 (alias resolver + `models.aliases` block + OpenAI peer alias → factory alias resolution + raw-ID deprecation signal → config migration → missing-price guard → `model_alias` span + derived pricing + cost gate → doc sweep → closeout); the offline mock + Ollama providers; the knob-free, config-driven provider selection + per-provider demo configs + `make demo-openai`; PR ranges per [release-prep plan §RFC PR roll-up](v0.3.4-release-prep-plan.md#rfc-pr-roll-up)
  - **(No Bug Fixes section)** — no tracked-issue bug-fix PRs shipped in `v0.3.3..HEAD`
  - **Documentation**: MT execution report (release-prep PR 1, [#439](https://github.com/mkhomutov/Persatrix/pull/439)), the model-providers guide + provider-neutral onboarding (release-prep PR 2), release-prep + master plans + the two amendments, the RFC 0045–0047 drafts on an RFC-tracking line
  - **Testing**: 4 new MTs (`MT-ALIAS-001` / `MT-ALIAS-002` / `MT-OFFLINE-001` / `MT-OLLAMA-001`), the alias cost-attribution gate, the carried-forward `cost-regression-gate`, and the offline/Ollama factory-interplay suites
  - **Upgrade Notes** subsection (see §3.1 below)
- [x] RFC 0045–0047 drafts ([#427](https://github.com/mkhomutov/Persatrix/pull/427)–[#429](https://github.com/mkhomutov/Persatrix/pull/429), all `🔨 Draft`) stay under an RFC-tracking line in **Documentation**, not Features

Suggested generation command:

```bash
git-cliff --tag v0.3.4 --unreleased --prepend CHANGELOG.md
```

If `git-cliff` would overwrite curated content, generate into a temporary file and merge manually — same pattern as v0.3.3 / v0.3.2 PR 3.

### 3.1 Required Upgrade Notes in Changelog

This is the canonical source for Upgrade Notes text.

| Notable change | Detail |
|----------------|--------|
| **[Feature]** Provider-agnostic model aliases | Agents, routing defaults, and the summarisation path reference a logical alias (`quality` / `fast` / `summarizer`) resolved to a `(provider, model, pricing)` record in [`config/optimization.yaml`](../config/optimization.yaml) `models.aliases` (RFC 0033). A vendor retirement or provider swap is a one-line edit to one alias entry. `schema_version` bumped `0.1` → `0.2`. A priced OpenAI peer alias ships. |
| **[Behaviour change — no default provider]** Explicit, config-driven provider choice | The shipped `config/optimization.yaml` ships its `quality` / `fast` / `summarizer` role aliases **unconfigured** (`provider: unconfigured`) — there is **no default provider**. A plain `docker compose up` fails loud at agent startup (an actionable `SystemExit`) until you pick one: run a `make demo-*`, or set `provider`/`model`/pricing on the alias. Nothing privileges a vendor or can spend money by default. See [amendment 2026-05-27](v0.3.4-plan-amendment-2026-05-27.md). |
| **[Behaviour change — provider selection]** Config-driven, no env force-knob | All four providers (`anthropic` / `openai` / `ollama` / `mock`) are selected the same standard way — by the resolved alias `provider` field. The `PERSATRIX_OFFLINE` / `PERSATRIX_OLLAMA` global force-knobs are **removed** (they were never in a tagged release — added and removed within the v0.3.4 window). `make demo-anthropic` / `demo-openai` / `demo-ollama` / `demo-offline` each select their provider by mounting a per-provider alias config (`config/demo/<provider>/optimization.yaml`) via a Compose overlay. `PERSATRIX_OLLAMA_MODEL` / `PERSATRIX_OLLAMA_BASE_URL` / `PERSATRIX_OFFLINE_RESPONSES` survive as provider *configuration* (not selection). |
| **[Behaviour change — onboarding]** Provider-neutral compose + `.env` | [`docker-compose.yaml`](../docker-compose.yaml) plumbs every provider key (`ANTHROPIC_API_KEY` / `OPENAI_API_KEY`) into each agent optionally and drops the single-vendor `:?` startup guard, so a config-only swap to a cloud-OpenAI alias authenticates in stock Docker and first-run privileges no vendor; an agent routed to a provider whose key is unset logs a clear startup warning. [`.env.example`](../.env.example) is reframed "pick one provider." |
| **[Safety]** Missing-price guard | A resolved alias with no pricing fails closed (loud `SystemExit`) for non-local providers, rather than a silent `$0` estimate that would disable the RFC 0023 budget/lease gate. Local providers (`ollama` / `mock`, or a loopback `base_url`) are exempt — they carry an explicit `0` ($0-real). |
| **[Cost]** Alias-derived pricing + `model_alias` span | `cost.pricing.models` (the Go cost table) is now a checked-in *projection* of the alias map (lock-step test `TestShippedCostPricingDerivedFromAliases`). The `agent.llm.call` span carries `persatrix.llm.model_alias` alongside the physical `gen_ai.request.model` (telemetry-only). |
| Raw-ID back-compat | A raw vendor model ID still resolves (the §E fall-through) but fires a one-shot deprecation warning + increments `persatrix.llm.alias.raw_id_usage`. Phase 3 (raw-ID rejection, `_infer_provider` retirement, schema `"0.3"`) is **observed-traffic gated** — it opens when that counter reads zero across dogfood, → v0.3.5+. |
| `$0`-local vs. the wallet cap | A genuinely-$0 local alias (offline / Ollama) never trips the simulated wallet, so the README's "agent pauses itself at the cap" behaviour shows only on a priced (cloud) alias. The offline / Ollama demos are $0 by design; use `make demo-openai` (or an Anthropic alias) to watch the cap trip. |

---

## 4. Manual Test Sign-off

Release sign-off requires the full **v0.3.4 surface — 38 rows** executed and recorded in [`docs/manual-tests/v0.3.4-execution-report.md`](manual-tests/v0.3.4-execution-report.md) — 4 new rows plus the 34-row v0.3.3 surface carried forward and regression-checked against the alias-routed config. The authoritative test index is [`docs/manual-tests/README.md`](manual-tests/README.md).

### Primary v0.3.4 Gate

| Area | Test ID |
|------|---------|
| Alias-routed agent reports correctly-keyed **non-zero** cost; `persatrix.llm.model_alias` span alongside the physical `gen_ai.request.model` (RFC 0033 §G) — **new, primary gate** | `MT-ALIAS-001` |

### New this release

| Area | Test ID |
|------|---------|
| One-line provider swap re-routes the same agent (headline claim) | `MT-ALIAS-002` |
| Offline mode — $0, zero network (mock provider) | `MT-OFFLINE-001` |
| Ollama local model — real tokens, $0 cloud | `MT-OLLAMA-001` |

### Results Summary (from execution report, `7e74873` RC tip)

| Outcome | Count |
|---------|------:|
| ✅ Pass | 33 |
| ⚠️ Accepted-with-known-gap | 4 |
| ⊘ Deprecated (superseded) | 1 |
| ❌ Fail | 0 |
| ⬜ Not run | 0 |

Checklist:

- [x] [`docs/manual-tests/v0.3.4-execution-report.md`](manual-tests/v0.3.4-execution-report.md) exists and is marked ✅ Complete (release-prep PR 1 — [#439](https://github.com/mkhomutov/Persatrix/pull/439))
- [x] Each test records date, tester, environment, and evidence
- [x] Every test outcome is `Pass`, `Accepted-with-known-gap`, or `Deprecated` — zero `Fail`, zero `Not run`
- [x] `MT-ALIAS-001` recorded `Pass` with per-step evidence (non-zero correctly-keyed `/cost/summary` priced at the physical model + `model_alias` span); the alias cost-attribution gate green on the RC tip
- [x] `MT-ALIAS-002` / `MT-OFFLINE-001` / `MT-OLLAMA-001` recorded `Pass`
- [x] `MT-MEMORY-005` is **not re-run** — no recall-tier change in v0.3.4 (RFC 0031 Phase 2 deferred to v0.3.5); the prior result carries forward. `MT-MEMORY-003` keeps its raw `claude-haiku-4-5` references (memory-compression is not aliased — [ISSUE-0072](issues/ISSUE-0072-memory-compression-hardcoded-model-literals.md))
- [x] Every `Accepted-with-known-gap` row links a tracked issue or an RFC-closeout / sequencing follow-up bullet
- [x] The MT recipes are refreshed from env-knob to **config-driven** alias-config activation (per [amendment 2026-05-27](v0.3.4-plan-amendment-2026-05-27.md)) and re-run live pre-bump in [#441](https://github.com/mkhomutov/Persatrix/pull/441): the four MT specs rewritten to alias-config form on HEAD `6ce23cd` (all ✅ Pass — see the report's [config-driven re-run](manual-tests/v0.3.4-execution-report.md#config-driven-re-run-head-6ce23cd)). The **post-bump** re-confirmation remains PR 4's [§ 1](#1-pre-release-verification) smoke gate.

---

## 5. Tag and GitHub Release Procedure

Use a clean `main` checkout after the final release-prep PR (PR 4 of the prep plan) merges.

```bash
git checkout main
git pull --ff-only origin main
git tag -a v0.3.4 -m "v0.3.4 — Any Model, Any Provider"
git push origin v0.3.4
```

GitHub Release checklist (post-merge manual steps after PR 4 merges):

- [ ] Create the GitHub Release from tag `v0.3.4`
- [ ] Use the curated `[0.3.4]` changelog section as the release notes baseline
- [ ] Include upgrade notes (§3.1) in the release body
- [ ] Include known gaps (§6) in the release body
- [ ] Attach binaries or container references if produced for this release — none expected (matches the v0.3.3 / v0.3.2 pattern)

---

## 6. Known Gaps to Document in Release Notes

These items remain intentionally incomplete and should be described as deferred scope, not regressions. Seeded from [v0.3.4-plan §Out of scope](v0.3.4-plan.md) and the [release-prep plan §Known follow-up issues](v0.3.4-release-prep-plan.md#known-follow-up-issues).

### v0.3.4-scope gaps

| Area | Description | Deferred to |
|------|-------------|-------------|
| RFC 0033 Phase 3 — raw-ID rejection | The §E raw-ID fall-through still works but fires a deprecation warning + the `persatrix.llm.alias.raw_id_usage` counter. Phase 3 (raw-ID rejection, `_infer_provider` retirement, schema `"0.3"`) opens only when that counter reads zero across dogfood — **observed traffic, not a date**. | RFC 0033 Phase 3 (v0.3.5+) |
| `$0`-local vs. wallet-cap claim | A genuinely-$0 local alias never trips the simulated wallet; the README is reconciled (the cap demo pins to a priced/cloud alias). Documented, not a regression. | Documented in README / [model-providers guide](guides/model-providers.md) |
| Memory-compression model literals ([ISSUE-0072](issues/ISSUE-0072-memory-compression-hardcoded-model-literals.md)) | The memory-compression surfaces still hardcode raw `claude-haiku-4-5*` defaults; correctly **not** swept (`MT-MEMORY-003` keeps its references). A fold-in candidate for the RFC 0023 un-leased-origin migration ([ISSUE-0063](issues/ISSUE-0063-workflow-step-unleased-llm-spend-uncounted.md)). | RFC 0023 §E follow-up |
| Two RFC 0033 PR-3 review findings | [ISSUE-0069](issues/ISSUE-0069-task-agent-evaluator-routing-defaults-unconsumed.md) (`task_agents`/`evaluators` routing defaults surfaced but unconsumed) and [ISSUE-0070](issues/ISSUE-0070-sub-agent-request-post-init-systemexit-construction.md) (`SubAgentRequest.__post_init__` `SystemExit`, latent while `SPAWN_SUB_AGENT` is a stub). Pre-existing/latent; neither affects the release surface. | Tracked issues |
| Alias-pricing guard has no eager startup validation ([ISSUE-0071](issues/ISSUE-0071-alias-pricing-guard-no-eager-startup-validation.md)) | The missing-price guard fires per-resolve, not as an eager whole-config startup sweep (`validate_alias_pricing` exists but is not wired to startup). | Tracked issue |
| CPU local-model chat timeout ([ISSUE-0073](issues/ISSUE-0073-cpu-local-model-chat-exceeds-default-timeout.md)) | CPU-only `make demo-ollama` inference can exceed the chat's 30 s default; the per-request `timeout_seconds` override resolves it. Default-UX on the advertised demo, not correctness. | Tracked issue |
| Per-session recall isolation / `persatrix session …` CLI | Carried forward. RFC 0031 Phases 2–4 (recall filtering — the F-3 fix) re-home to v0.3.5. | RFC 0031 Phases 2–4 (v0.3.5) |

### Carried forward from earlier releases

The v0.3.3-scope and earlier gaps documented in [v0.3.3 release checklist §6](v0.3.3-release-checklist.md#6-known-gaps-to-document-in-release-notes) remain open in v0.3.4 unchanged (the `tick_interval_seconds` deprecation warning → RFC 0024 Phase 5; salience-threshold calibration; RFC 0029 Phases 2–6 Postgres society backend; RFC 0009 Phases 3–4 sandbox + token auth; per-cause budget policies; channels REST surface unauthenticated; external bridges; `MT-COST-002` workflow-abort sentinel; and the rest). The v0.3.4 release notes link the v0.3.3 checklist for full detail rather than re-listing them here.

---

## 7. Summary Checklist

Single-page go/no-go gate. All items must be checked before tagging.

```text
[x] make test green on the post-bump RC tip (CI green on 3ad32b2 incl. full integration suite per #443; locally re-ran Go race-cover / Rust 102 / refactored provider slices 84 / alias cost gate / cost-regression-gate — 0 fail)
[x] make lint clean (golangci-lint / ruff / mypy whole-package no issues / clippy v0.3.4)
[x] make validate passes (5 configs, 3 instructions_file refs)
[x] make proto then git diff --exit-code shows no generated drift (no proto change)
[x] make check-licenses passes (Go / Python / Rust)
[x] THIRD_PARTY_NOTICES.md up to date, no delta (no new prod dep)
[x] make generate-sanitizer-patterns-check passes (Go↔Python parity)
[x] agents/pyproject.toml updated to 0.3.4
[x] agents/observability/{metrics,tracing}.py service.version defaults updated to 0.3.4
[x] cli/Cargo.toml updated to 0.3.4
[x] cli/Cargo.lock regenerated and committed
[x] make all builds successfully (orchestrator + CLI both at 0.3.4)
[x] CHANGELOG.md contains a curated [0.3.4] section with upgrade notes (no Bug Fixes section); [Unreleased] content folded in, not duplicated
[x] Existing [0.3.3] … [0.1.0] sections preserved verbatim
[x] All 38 manual-test rows recorded in docs/manual-tests/v0.3.4-execution-report.md
[x] No unresolved Fail in manual tests; zero Not run
[x] MT-ALIAS-001 recorded Pass (primary release gate); alias cost-attribution gate green on the RC tip
[x] MT-ALIAS-002 / MT-OFFLINE-001 / MT-OLLAMA-001 recorded Pass
[x] Bored-persona cost-regression-gate green on the post-bump tip (re-ran, 7 passed; release-blocker; PR trigger, not nightly)
[x] Docker smoke — alias-routed non-zero cost + one-line swap + demo-offline ($0) + demo-ollama (real tokens, $0 cloud) + demo-openai (cloud peer), all config-driven (carried forward from the pre-bump HEAD 6ce23cd live re-run; non-runtime RC-tip delta)
[x] Known gaps documented for release notes (§6 above)
[ ] git tag v0.3.4 created and pushed (Phase 5, post-merge)
[ ] GitHub Release published with changelog, upgrade notes, and known gaps (Phase 5, post-merge)
```
