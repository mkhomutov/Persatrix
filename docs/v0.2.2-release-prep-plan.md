# v0.2.2 Release Preparation Plan

**Status**: ✅ Complete
**Target version**: v0.2.2 (Persona Memory Injection Token Budget)
**Created**: 2026-04-22
**Branch prefix**: `feature/v022-release-prep-`
**Target**: `main`
**Merge strategy**: Squash merge per [BRANCHING.md](BRANCHING.md)

**Scope**: documentation refresh for the RFC 0017 surface, manual test execution report, release checklist, version bump, changelog generation
**Out of scope**: new feature work; RFC 0017 is `✅ Implemented` (7/7 PRs merged); no new diagrams (no user-facing API/CLI surface added in v0.2.2)

**Documentation timing policy**:
- **Before release (required)**: update public docs for all merged behavior that will ship in `v0.2.2` so release-day documentation is accurate.
- **After release (follow-up)**: a small docs pass for release artifacts that only exist post-tag (version/tag links, release URL, published date references).

---

## Progress Overview

Update this table as each PR merges. Flip **Status** and add GitHub PR number + merge date.

| PR | Track | Title | Branch | Status | GitHub PR | Merged |
|----|-------|-------|--------|--------|-----------|--------|
| 1 | A | Manual test execution report (RFC 0017 surface) | `feature/v022-manual-test-execution-report` | ✅ Merged | #155 | 2026-04-22 |
| 2 | A | README + ROADMAP + guide refresh + release checklist + prep plan | `feature/v022-release-prep-checklist` | ✅ Merged | #156 | 2026-04-22 |
| 3 | B | Version bump (`agents/pyproject.toml`, `cli/Cargo.toml`, `cli/Cargo.lock`) + changelog generation | `feature/v022-release-prep-version-bump` | ✅ Merged | #157 | 2026-04-22 |
| 4 | B | Final pre-tag verification & release notes | `feature/v022-release-prep-final` | ✅ Merged | #158 | 2026-04-22 |

**Status legend**: ⬜ Not started · 🔄 In progress · 🔀 PR open · ✅ Merged

---

## Dependency Graph

```
PR 1 (manual test execution report) ──► PR 2 (docs + checklist)
                                              │
                                              ▼
                                    PR 3 (version bump + changelog) ──► PR 4 (final)
```

Hard dependencies:
- **PR 2** waits on PR 1 (release checklist references the execution-report path).
- **PR 3** waits on PR 2 (version bump runs after checklist confirms readiness).
- **PR 4** waits on PR 3; it is the pre-tag gate.

No track-A diagram refresh PR is needed for v0.2.2: RFC 0017 introduces no new
component, no new gRPC RPC, and no new REST endpoint. The shipped change is an
internal allocator (`MemoryBudget`) and an internal short-circuit guard inside
the persona action loop.

---

## Current State (baseline, 2026-04-22)

### RFC 0017 — Complete

All 7 PRs merged. RFC closed:

| PR | GitHub | Title |
|----|--------|-------|
| 1 | #145 | `MemoryBudget` allocator + token-aware truncation |
| 2 | #146 | `_inject_memory_context` allocate-loop rewrite |
| 3 | #147 | `min_score` relevance threshold on `recall` / `recall_notes` |
| 4 | #148 | Wire `min_score` and remove legacy gates |
| 5 | #149 | Empty-context TICK short-circuit |
| 6 | #152 | PR 1–5 review follow-ups |
| 7 | #153 / #154 | RFC close + manual-test runbooks (`MT-MEMORY-004`, `MT-PERSONA-003`) |

### Version state

Both shipped components are still at `0.2.1` — needs bump to `0.2.2`:

| Component | File | Current |
|-----------|------|---------|
| Python agents | `agents/pyproject.toml` | `0.2.1` |
| Rust CLI | `cli/Cargo.toml` | `0.2.1` |
| Go orchestrator | git tag | `v0.2.1` |

### Documentation state

- README mentions v0.2.2 only in the cost-warning section (the RFC 0017 fix
  story for the empty-context tick bug). The roadmap table and the per-version
  "What's added in vX.Y" sections still stop at v0.2.1.
- `ROADMAP.md` already shows v0.2.2 as `✅ Implemented` and the current milestone
  as "v0.2.2 release prep". No further edit required there for this track.
- `docs/guides/persona-agents.md` describes the three-tier memory system and
  the tick loop, but does not yet reference the RFC 0017 token-budget allocator
  or the empty-context TICK short-circuit. A short note pointing readers at
  RFC 0017 is added in PR 2.
- No v0.2.2 release checklist exists. Created in PR 2.

### Manual test state

- 19 tests recorded in [`docs/manual-tests/v0.2.2-execution-report.md`](manual-tests/v0.2.2-execution-report.md):
  17 carried forward from v0.2.1 plus 2 new tests (`MT-MEMORY-004`, `MT-PERSONA-003`)
  authored under PR #154.
- Outcomes: ✅ 18 pass, ⚠️ 1 accepted-with-known-gap (`MT-COST-002`), 0 fail.

### Changelog state

- `CHANGELOG.md` has curated `[0.2.1]` and `[0.2.0]` sections. No `[0.2.2]`
  section yet. Generation deferred to PR 3.

---

## Track A — Documentation Refresh

### PR 2 — README + ROADMAP + Guide Refresh + Release Checklist

**Branch**: `feature/v022-release-prep-checklist`

Scope:

- Add a "What's added in v0.2.2" section to [README.md](../README.md) with the
  RFC 0017 highlights and a short pointer to the cost-warning rationale that
  is already present.
- Update the README roadmap table: v0.2.2 → `🚧 Release prep`; flip to
  `✅ Released` in PR 4.
- Add a "Known Limitations in v0.2.2" subsection summarising the carry-forward
  gaps (no behavioural regressions; the only new gap is the deferred
  `MT-COST-002` budget-exceed scenario, already known).
- Update [docs/guides/persona-agents.md](guides/persona-agents.md) §2 with a
  short callout that v0.2.2 introduces a per-event memory token budget and a
  TICK short-circuit when nothing is admitted; link to RFC 0017 §B and §F.
- Create [docs/v0.2.2-release-checklist.md](v0.2.2-release-checklist.md) using
  the v0.2.1 checklist as a structural template.
- Create this prep plan ([docs/v0.2.2-release-prep-plan.md](v0.2.2-release-prep-plan.md)).

Acceptance:

- `make validate` passes.
- README roadmap table mentions v0.2.2.
- Persona-agents guide references RFC 0017.
- Release checklist references the execution-report path and lists every gate
  needed before tagging.

---

## Track B — Release Engineering

### PR 3 — Version Bump + Changelog Generation

**Branch**: `feature/v022-release-prep-version-bump`

Scope:

- Bump `agents/pyproject.toml` `version` to `0.2.2`.
- Bump `cli/Cargo.toml` `version` to `0.2.2`.
- Regenerate `cli/Cargo.lock` and commit.
- Run `make all` to confirm builds pass at the new version.
- Generate the `[0.2.2]` changelog section via
  `git-cliff --tag v0.2.2 --unreleased --prepend CHANGELOG.md` (or merge
  manually to preserve curated content from earlier sections).
- Verify the existing `[0.2.1]` and `[0.2.0]` sections are preserved verbatim.

Acceptance:

- `make all` builds at `0.2.2`.
- `CHANGELOG.md` contains a curated `[0.2.2]` section (Highlights, Upgrade
  Notes, Features, Bug Fixes, Documentation, Testing).
- Both prior version sections untouched.

### PR 4 — Final Pre-Tag Verification & Release Notes

**Branch**: `feature/v022-release-prep-final`

Scope:

- Run the full release-checklist sweep on a clean checkout: `make test`,
  `make lint`, `make validate`, `make proto && git diff --exit-code`,
  `make check-licenses`, `make notices` (commit any updated
  `THIRD_PARTY_NOTICES.md`).
- Docker smoke test: `make docker-build && make docker-up`, submit a
  workflow, send a chat message, hit `GET /api/v1/cost/summary`.
- Flip the README roadmap row from `🚧 Release prep` to `✅ Released`.
- Update this prep-plan status to `✅ Complete` and check off every PR row.
- Update the release-checklist `Status` to `✅ Released` and check off every
  gate.

Tag procedure (executed after PR 4 merges):

```bash
git checkout main
git pull --ff-only origin main
git tag -a v0.2.2 -m "v0.2.2 — Bounded Persona Memory Injection"
git push origin v0.2.2
```

GitHub Release creation follows the v0.2.1 pattern: paste the curated
`[0.2.2]` changelog as the release body, append upgrade notes, append known
gaps.

---

## Status hygiene

This document is the single source of truth for v0.2.2 release-prep PR
sequencing. Update the **Progress Overview** table on every merge. When
the final PR (PR 4) merges, mark Status at the top to `✅ Complete` and set
the release date.
