# v0.3.10 Release Checklist

> **Status**: 🚧 Release prep — **in progress.** This checklist is created by [release-prep plan PR 2](v0.3.10-release-prep-plan.md#pr-2--readme--roadmap--persona-agents-guide--memory-architecture-diagram-verification--release-checklist) (docs surface). §1 (Pre-release Verification) and §2 (Version Alignment) are executed by [PR 3](v0.3.10-release-prep-plan.md#pr-3--version-bump--changelog-curation) (version bump + changelog) and re-certified live by [PR 4](v0.3.10-release-prep-plan.md#pr-4--final-pre-tag-verification--release-notes) on the post-version-bump tip; §3.1 (Upgrade Notes) is the canonical source PR 3 reconciles into `CHANGELOG.md`. §4 (Manual Test Sign-off) references the [MT execution report](manual-tests/v0.3.10-execution-report.md) landed by [PR 1 (#703)](https://github.com/mkhomutov/Persatrix/pull/703) — a **clean pass**: the headline `MT-REASON-001` ran **live on Anthropic** (the `main` RC tip + this PR-1's F-1 config fix), with the reasoning go-live structural release-blocker gates green live on host, so no live hard-block is outstanding (two findings — **F-1**, the demo config pinned the headline `off`, was **fixed in that same PR**; **F-2** / [ISSUE-0108](issues/ISSUE-0108-reasoning-reason-note-no-operator-egress.md), the verbatim `reason_note` has no operator egress, is **documented + deferred**, non-blocking — the §E wall is *stronger* than documented).

> v0.3.10 — "Conversations worth posting into" — ships the **worth-posting-into rung**: personas that **think before they speak**. **[RFC 0051 reasoning before posting](rfcs/0051-reasoning-before-posting.md) — the user-facing win** — a private, per-turn deliberation a persona runs *before* it publishes into a channel. It generalizes the shipped [RFC 0030](rfcs/0030-multi-agent-conversation-governance.md) Tier-B salience bid from a scalar `speak/score` into a structured `{ should_post, plan }` verdict on the same leased `fast`-model seam (metered against the same `interaction_id`; idle stays free). A `should_post=false` ends the turn in `DO_NOTHING` *before* the expensive compose — **semantic silence**, a net saving on pile-on, **on by default once a channel is governed** (`mode: bid`); a `should_post=true` threads a private `CompositionPlan` (intent / key points / addressed-to / avoid-restating) into the existing Tier-C compose — the **considered, plan-threaded post**, a one-rung-up per-channel operator opt-in (`mode: plan`). On top of `plan`, an opt-in critic→revise **reflexion** loop (`reasoning.revise: 1|2`, default `0`) sharpens a draft before it ships, fail-soft. The whole trace — the plan, the silence `reason_note`, and any discarded reflexion draft + critic note — is **walled** ([RFC 0051 §E](rfcs/0051-reasoning-before-posting.md#e-privacy-boundary--the-trace-is-walled)): never a channel message, never the channel store, never a peer's RFC 0034 working memory; the audit trail is count-only. Tuned per channel by the `reasoning.{mode,model,depth,revise}` knob on the [RFC 0050](rfcs/0050-extensible-channel-configuration.md) config surface. Two invisible fold-ins ride along with no story of their own: **[RFC 0034 Phase 3](rfcs/0034-persona-conversational-working-memory.md)** bounds the per-turn transcript fetch-cache (now an LRU) and instruments the conversation window (`conversation_window.*`), and **[RFC 0045](rfcs/0045-open-core-extraction-policy.md)** lands the `MIT ← BUSL ← Private` dependency-direction CI gate (`make imports-check` + the Go `internal/archpolicy` check) — pure license-boundary infrastructure that moves no code. The user-facing promise: *a group conversation reads as considered rather than reflexive — fewer pile-on turns, no eager "I agree" noise, and posts that say something.*

**Based on test report**: [`docs/manual-tests/v0.3.10-execution-report.md`](manual-tests/v0.3.10-execution-report.md)
**Release prep plan**: [`docs/v0.3.10-release-prep-plan.md`](v0.3.10-release-prep-plan.md)
**Master plan**: [`docs/v0.3.10-plan.md`](v0.3.10-plan.md)
**RFCs**: [`docs/rfcs/0051-reasoning-before-posting.md`](rfcs/0051-reasoning-before-posting.md) (the [RFC 0051 PR plan](rfcs/0051-pr-plan.md) — §C verdict/plan types / §D mechanism / §E privacy wall / §F idle/cost / §G the `reasoning` knob) · [`docs/rfcs/0034-persona-conversational-working-memory.md`](rfcs/0034-persona-conversational-working-memory.md) (the [Phase 3 PR plan](rfcs/0034-phase3-pr-plan.md) — the window instrumentation + the fetch-cache LRU bound) · [`docs/rfcs/0045-open-core-extraction-policy.md`](rfcs/0045-open-core-extraction-policy.md) (the [reserved-seams note](open-core-reserved-seams.md) — the dependency-direction CI gate) · [`docs/rfcs/0030-multi-agent-conversation-governance.md`](rfcs/0030-multi-agent-conversation-governance.md) / [`docs/rfcs/0050-extensible-channel-configuration.md`](rfcs/0050-extensible-channel-configuration.md) (the shipped seams RFC 0051 generalizes / rides)

---

## 1. Pre-release Verification

All build gates run on a clean checkout, executed by PR 3 at the new version and re-certified live by PR 4 on the post-version-bump tip. PR 1 already ran the **reasoning go-live structural suite green live on this host** on the RC tip (Go 1.26.3 on host — so the Go gates ran live, not carried to CI): the `off → bid` go-live + kill-switch + capability-gate + reconcile-survives-boot + proto-stamp Go suites, the REST validate suite, the Python deliberation/plan/reflexion suites, the **privacy-wall no-leak integration test** (the load-bearing correctness gate), and the RFC 0034 P3 cache/instrument suite — see the [execution report § Structural / Automated Gates](manual-tests/v0.3.10-execution-report.md#structural--automated-gates-live-on-host).

- [ ] `make test` green on the post-bump tip — the full **Go `./internal/... -race -cover` suite** (all packages `ok`, no data races) incl. the RFC 0051 reasoning config suite ([`config_reasoning_test.go`](../internal/channels/config_reasoning_test.go) / [`channel_config_reasoning_test.go`](../internal/server/channel_config_reasoning_test.go) — `off → bid` governed default, the explicit-`off` kill switch, the non-`off`-on-ungoverned rejection, the `depth: deep` / `revise ≥ 1`-outside-`plan` / `revise > 2` validate rejections), the reconcile-survives-boot suite (`TestReconcileRoundTrip_ReasoningSurvivesBoot` / `_GovernedKillSwitchOffSurvivesBoot`), the resolved-rung fan-out + proto-stamp suites ([`fanout_salience_test.go`](../internal/channels/fanout_salience_test.go) / [`grpc_dispatcher_salience_test.go`](../internal/channels/grpc_dispatcher_salience_test.go) — `TestFanout_StampsResolvedReasoningMode...` / `TestChannelMessageToProto_CarriesReasoning...`); the session + epoch structural-isolation gates; `cargo test`; the **Vitest** UI suite; the full Python unit suite incl. the deliberation/plan/reflexion + salience-reasoning suites (`test_reflexion`, `test_reflexion_metrics`, `test_deliberation_plan`, `test_salience_gate_deliberation_{metrics,audit}`, `test_salience_bid_reasoning{,_dispatch}`) and the RFC 0034 P3 cache/instrument suite ([`test_conversation_window_cache.py`](../tests/unit/python/test_conversation_window_cache.py) / [`test_conversation_window_metrics.py`](../tests/unit/python/test_conversation_window_metrics.py)); and the **privacy-wall no-leak integration test** ([`tests/integration/test_deliberation_no_leak.py`](../tests/integration/test_deliberation_no_leak.py)). **No store migrates this release** — the channel store stays at **v10** and the persona-memory store is untouched (no migration suite). PR 1 ran the reasoning structural suite live on the RC tip; **re-certified green live by PR 4 on the post-bump tip**. (PR 3/PR 4.)
- [ ] `make lint` clean — ruff + clippy live by PR 4 (`go vet ./internal/...` clean); golangci-lint + mypy carried by CI. Includes the **RFC 0045 fold-in** gate: `make imports-check` (the Python `import-linter` `MIT ← BUSL ← Private` contract) + the Go `internal/archpolicy` check stay green (part of the `lint-python: imports-check` lane).
- [ ] `make validate` passes — all configs valid + `instructions_file` refs resolve; [`config/channels.yaml`](../config/channels.yaml) (incl. the `planning` channel's `reasoning.mode: bid` governed default — the **F-1** fix from PR 1) validates; the `reasoning.{mode,model,depth,revise}` knob validates against the channel-config schema (`depth: deep` rejected as unbacked; `revise ≥ 1` accepted only under `mode: plan`, capped at 2); doc-audit (`doc_audit.py`) **0 violations**; `doc_links.py` clean.
- [ ] Proto staleness clean — `make proto && git diff --exit-code`. **v0.3.10 adds two additive protobuf fields** to `ChannelMessageEvent` in [`proto/task.proto`](../proto/task.proto): `reasoning_mode = 25` (string) and `reasoning_revise = 26` (int32), carrying the channel's resolved reasoning rung + revise count on the orchestrator → agent dispatch. New field numbers → backward/forward compatible (a pre-v0.3.10 producer leaves them empty/0 → `off` / single-pass). Confirm the committed generated [`internal/generated/taskpb/task.pb.go`](../internal/generated/taskpb/task.pb.go) + the Python stubs are in sync with `task.proto`. Regen with the **CI-pinned `protoc`/plugin versions**, not newer local brew tools (a local regen differs only in the generator version comment — revert it; CI re-confirms on the pinned toolchain).
- [ ] `make check-licenses` clean — Python + Rust (`cargo deny`) run live by PR 4; Go (`go-licenses`) carried by CI.
- [ ] `make notices` up to date — **delta-free by construction**: v0.3.10 adds **no third-party production runtime dependency** to the shipped Go/Python/Rust artifacts (persona-runtime + orchestrator behaviour: the structured salience verdict, the `CompositionPlan` compose threading, the reflexion loop, the resolved-rung router stamp, the `reasoning` config surface, the RFC 0034 P3 cache + OTEL instruments). The deliberation reuses the existing leased `fast`-model seam and the existing OTEL instrument shapes; the RFC 0045 `import-linter` gate is a **dev/CI** dependency, not a shipped-artifact one. Expected empty save for any in-window Dependabot / RUSTSEC hygiene bumps. Commit any genuine delta. (PR 4.)
- [ ] `make generate-sanitizer-patterns-check` passes — Go↔Python sanitizer-pattern parity in sync (reasoning adds no new pattern; the deliberation reads the existing RFC 0009 `<external_data>` envelope).
- [ ] **The privacy wall — no-leak integration (the load-bearing correctness gate)** — [`tests/integration/test_deliberation_no_leak.py`](../tests/integration/test_deliberation_no_leak.py): pins **zero** `messages` rows and **zero** appearance in a second persona's reconstructed `messages` for **both** the private `CompositionPlan` *and* a discarded reflexion draft + critic note (RFC 0051 PR 3 / PR 9) — the structural wall that makes "private deliberation" true. Ran green live by PR 1 (**3 passed**); the live arc cross-checked **0** plan markers in the transcript and in both peers' `?as_participant` recall.
- [ ] **The `off → bid` go-live + kill-switch + capability-gate + reconcile (Go, race)** — [`config_reasoning_test.go`](../internal/channels/config_reasoning_test.go) / [`channel_config_reasoning_test.go`](../internal/server/channel_config_reasoning_test.go): `TestLoadConfig_ReasoningGovernedDefaultsToBid`, `_ReasoningExplicitOffKillSwitch`, `_ReasoningModeRejectedUngoverned`, `_ReasoningDepthDeepRejected`, `_ReasoningReviseRequiresPlan` / `_ReviseRejected`; the boot-reconcile suite `TestReconcileRoundTrip_ReasoningSurvivesBoot` / `_GovernedKillSwitchOffSurvivesBoot`; the fan-out + proto-stamp suites `TestFanout_StampsResolvedReasoningModeOnEnvelope` / `_Revise` and `TestChannelMessageToProto_CarriesReasoningMode` / `_Revise`. The governed-channel default resolves to `bid` at load / boot / runtime-create / runtime-edit; an explicit `off` is preserved as a kill switch across all of them; `off` restores **byte-for-byte** the prior RFC 0030 scalar score gate. Ran green live by PR 1 (channels **74 PASS** race + server **15 PASS** race).
- [ ] **Deliberation verdict + plan + reflexion + salience-reasoning (Python)** — the structured-verdict parser + parse-failure counter, the `agent.deliberated` audit, the idle-cost gate (an idle turn still bills nothing — the RFC 0024 invariant), the `CompositionPlan` parser/renderer + the no-leak assertion, and the reflexion critic→revise loop's fail-soft + round-cap behaviour. Ran green live by PR 1 (**118 passed**).
- [ ] **`validate` capability-gating** — `reasoning.depth: deep` rejected as unbacked ("not yet deployed (native extended thinking is RFC 0051 Phase 4)"); `reasoning.revise ≥ 1` accepted only under `mode: plan` ("requires mode: plan") and capped at 2 (a `revise ≥ 1` on any other rung, or `> 2`, is a **400**) — **no silent downgrade**. Ran green live by PR 1 (CLI client-side clap enum + server REST 400, both legs).
- [ ] **Deliberation / reflexion + idle-cost telemetry** — the go-live `deliberation.{total,suppressed,duration,budget_starved}` + `reflexion.{runs,rounds}` instruments land on [`agents/observability/_metrics_salience.py`](../agents/observability/_metrics_salience.py) (not `metrics.py`, which is at the 500-line cap); the idle-cost invariant (an idle turn bills nothing) is explicitly pinned by the deliberation suite. The live run charted `deliberation.budget_starved = 0` and `deliberation.parse_failures = 0`. Ran green live by PR 1.
- [ ] **RFC 0034 Phase 3 fold-in** — the bounded-LRU cache ([`agents/persona_runtime/_conversation_window_cache.py`](../agents/persona_runtime/_conversation_window_cache.py)) eviction + the `conversation_window.{cache_access,cache_evictions,fetch_duration,fallback}` instrument tests; **defaults unchanged** (the `max_turns` / `max_tokens` retune is a deferred post-soak follow-up gated on collecting this telemetry). Ran green live by PR 1 (**20 passed**).
- [ ] **RFC 0045 fold-in** — `make imports-check` (the Python `import-linter` `MIT ← BUSL ← Private` contract) + the Go `internal/archpolicy` check stay green; they are part of the lint lane (`lint-python: imports-check`). Pure license-boundary CI infrastructure — moves no code.
- [ ] **Alias cost-attribution gate** (carry-forward) — `TestCostSummary_AliasRoutedAgent_ReportsNonZeroCost` (`internal/server`) + `TestLoadCostConfig_ShippedConfig_PricesAliasPhysicalModels` (`internal/cost`). Carried by CI / re-certified by PR 4.
- [ ] **Session + epoch structural-isolation gates** (carry-forward) — the `tests/integration/test_{session,epoch}_*.py` isolation gates. Carried by CI on the post-bump tip.
- [ ] **Cost-regression — reasoning is net-neutral-to-saving** — re-asserts the RFC 0023/0024 "uninvolved persona costs zero" invariant: the deliberation **reuses the existing leased `fast`-model seam** metered against the same `interaction_id` and **saves** cost on suppressed turns (a `should_post=false` ends the turn *before* the expensive compose — no compose call). The idle-cost invariant is explicitly pinned (an idle turn still bills nothing). Confirmed structurally by PR 1 (no new wallet origin; `budget_starved = 0` live).
- [ ] **Carried-forward v0.3.9 / v0.3.8 surface — regression** (against the v0.3.10 RC tip) — the recall + ledger + `?as_participant` window-filter suites, the Tier-B salience-bid + governance / interaction-id / convergence suites, the channel-config store/REST suites. Reasoning generalizes the *existing* salience seam and adds **no new store path**, so it must not regress recall, governance, or the publish hot path. Ran green live by PR 1 (Go packages `ok`; Python recall-tool + Tier-B salience **64 passed**; chair + escalation + response-gate **99 passed**; external-data + idle-cost **10 passed**).
- [ ] **No store-migration gate this release** — confirm instead that a pre-v0.3.10 **channel store** opens **unchanged** on the post-bump binary (no migration runs; an already-v10 store is a no-op open) and the **persona-memory store is untouched**. Unlike v0.3.9 (channel store v8 → v9 → v10), v0.3.10 advances **no store** — RFC 0051 is runtime + config + telemetry, RFC 0034 P3 is an in-process cache + OTEL instruments, and RFC 0045 is CI-only. (PR 4.)
- [ ] Docker smoke test — confirming on a clean clone with no host toolchain: (a) a governed channel defaults `reasoning.mode: bid`; (b) a `mode: plan` turn composes a considered post and a silence turn stays out of the transcript (the `deliberation.*` telemetry emits); (c) the kill switch (`mode: off`) restores the prior RFC 0030 scalar score-gate behaviour at runtime, no restart; (d) a DM turn still completes (no regression). *(The countable pile-on-suppression delta against a real provider is captured by PR 1 — the **80% semantic suppress-rate** under `bid`; the Docker smoke confirms the wiring carries through CLI + web without the live-provider count.)* (PR 4.)

> **Environment note.** The PR-1 host carried the **full toolchain** (Go 1.26.3 `darwin/arm64`, Docker) **and a valid Anthropic key** — so the headline `MT-REASON-001` arc ran live on Anthropic and the reasoning go-live Go/Python structural gates ran live on host (not carried to CI). See [execution report § Environment](manual-tests/v0.3.10-execution-report.md#environment).

---

## 2. Version Alignment

Before tagging, all shipped components must declare `0.3.10` consistently. PR 3 bumps every component to `0.3.10`; PR 4 re-confirms the values on the post-bump tip.

| Component | File | Required state for release |
|-----------|------|----------------------------|
| Python agents | `agents/pyproject.toml` | `version = "0.3.10"` |
| Python observability | `agents/observability/{metrics,tracing}.py` | `_DEFAULT_SERVICE_VERSION = "0.3.10"` (both; `tracing.py` also restates it in its module docstring) |
| Rust CLI | `cli/Cargo.toml` | `version = "0.3.10"` |
| Rust lockfile | `cli/Cargo.lock` | Regenerated after the CLI version bump |
| Go orchestrator service-version fallback | `internal/server/ui_handlers.go` (`const defaultServiceVersion`) | `defaultServiceVersion = "0.3.10"` |
| Go orchestrator | git tag | Release identified by `v0.3.10` tag |

`scripts/bump_version.py` bumps the `pyproject.toml`, `Cargo.toml`, observability `service.version` strings (incl. the `tracing.py` docstring restatement), and the Go `defaultServiceVersion`; `cli/Cargo.lock` is then regenerated via `cd cli && cargo update --workspace`. The observability defaults and the Go `defaultServiceVersion` are not build inputs, so a stale value never fails `make all` — they are bumped explicitly. The `web/package.json` JS workspace stays at `0.0.0` **by design** (not a release-versioned artifact; the bundle ships inside the Go binary).

Checklist (filled in by release-prep plan PR 3; PR 4 confirms on the post-bump tip):

- [ ] `agents/pyproject.toml` → `0.3.10` (PR 3)
- [ ] `agents/observability/{metrics,tracing}.py` → `_DEFAULT_SERVICE_VERSION = "0.3.10"` (both; the `tracing.py` docstring default bumped too)
- [ ] `cli/Cargo.toml` → `0.3.10`; `cli/Cargo.lock` regenerated (`cargo update --workspace`)
- [ ] `internal/server/ui_handlers.go` → `const defaultServiceVersion = "0.3.10"`
- [ ] `make all` + `make ui` build at the new version (PR 3 confirms `go build ./...` + `cargo build` clean at `0.3.10`; PR 4 re-certifies live, with the committed placeholder embed restored after `make ui` so the Go-only lane stays green with no Node)
- [ ] `web/package.json` intentionally **not** bumped (stays `0.0.0`)
- [ ] No docs describe v0.3.10 as unreleased WIP — **PR 4** flips the ROADMAP Version Map row, the README Roadmap row, this checklist's status, and the release-prep-plan status to released; the Phase 4 post-release follow-up then cuts the `v0.3.10` tag, publishes the GitHub Release, and backfills the post-tag artifacts (tag link, Release URL)

---

## 3. Changelog

`CHANGELOG.md` **already has a populated `[0.3.10] - Unreleased` section** ([CHANGELOG.md:5](../CHANGELOG.md)) — a Highlights entry (the semantic-silence-by-default win), a curated Upgrade Notes table (the `off → bid` governed-channel default flip + the one-flip kill switch; the per-member `threshold` supersession; the `depth: deep` rejection; the `revise: 0|1|2` reflexion opt-in gated to `mode: plan`; the additive `reasoning_mode` / `reasoning_revise` wire fields + upgrade-ordering), a `### 🚀 Features` block (the RFC 0051 go-live + the reflexion loop), and a `### ⚡ Performance` block (the RFC 0034 P3 bounded LRU + window instrumentation), drafted as the implementation PRs landed. Release-prep PR 3 **dates it into a tagged `[0.3.10]`** (temp-file-and-merge to preserve the curated prose), backfilling the PR ranges ([#692](https://github.com/mkhomutov/Persatrix/pull/692)–[#701](https://github.com/mkhomutov/Persatrix/pull/701)) per the [release-prep plan §RFC PR roll-up](v0.3.10-release-prep-plan.md#rfc-pr-roll-up). `[0.3.9]` and prior are preserved verbatim.

Checklist:

- [ ] `CHANGELOG.md` contains a dated `[0.3.10]` section (PR 3); `[Unreleased]` removed or emptied
- [ ] The existing curated `[0.3.9]` … `[0.1.0]` sections are preserved verbatim — not overwritten (manual rename + date-stamp; `git diff` shows zero change below the new section)
- [ ] The `[0.3.10]` section reshapes the `[Unreleased]` prose into:
  - **Highlights** — personas think before they speak: semantic silence on by default once a channel is governed (`mode: bid`, the structured `{ should_post }` verdict supersedes the numeric score, suppressing pile-on with a reason), the considered plan-threaded post one opt-in rung up (`mode: plan`), the opt-in critic→revise reflexion loop, and the walled, audit-only trace — "a group conversation reads as considered rather than reflexive."
  - **Features** — RFC 0051 reasoning before posting: the structured silence verdict + seam threading + `agent.deliberated` audit, the plan-threaded compose, the `reasoning` config backend + CLI/web surfaces, the `off → bid` go-live flip + telemetry, and the reflexion loop (default `revise: 0`) ([#692](https://github.com/mkhomutov/Persatrix/pull/692)–[#699](https://github.com/mkhomutov/Persatrix/pull/699)).
  - **Performance** — RFC 0034 Phase 3: the per-turn transcript fetch-cache is now a bounded LRU (was unbounded) + the `conversation_window.*` instrumentation ([#700](https://github.com/mkhomutov/Persatrix/pull/700)).
  - **Documentation** — README/ROADMAP + the persona-agents guide §Reasoning before posting + memory-architecture diagram verification + this checklist (PR 2); the MT execution report ([#703](https://github.com/mkhomutov/Persatrix/pull/703)); the RFC 0045 reserved-seams note + DCO scaffold ([#701](https://github.com/mkhomutov/Persatrix/pull/701)); plans.
  - **Testing** — the headline `MT-REASON-001` (live, Anthropic); the privacy-wall no-leak gate (plan + discarded reflexion draft), the `off → bid` + kill-switch + capability-gate + reconcile + proto-stamp suites, the deliberation/reflexion + idle-cost telemetry suites, the RFC 0034 P3 cache/instrument suite, the RFC 0045 `imports-check` + `archpolicy` gate, and the carried-forward recall / governance / cost-regression gates.
  - **Build / CI** — the RFC 0045 `MIT ← BUSL ← Private` dependency-direction CI gate (`make imports-check` Python `import-linter` contract + the Go `internal/archpolicy` check) + the reserved-seams note + the DCO scaffold ([#701](https://github.com/mkhomutov/Persatrix/pull/701)).
  - **Upgrade Notes** — see §3.1 below.

The curated Highlights + Upgrade Notes are **already written** in `[0.3.10] - Unreleased`, so the likely path is a manual rename + date-stamp (temp-file-and-merge to avoid clobbering curated prose) rather than a clean regenerate.

### 3.1 Required Upgrade Notes in Changelog

This is the canonical source for Upgrade Notes text (PR 3 reconciles it with the six-row table already in `[0.3.10] - Unreleased`). All six rows below stand as the shipped notes; plus the no-migration note and the version-skew / upgrade-ordering caution.

| Notable change | Detail |
|----------------|--------|
| **[Behaviour — default flip for governed channels]** `reasoning.mode` default `off → bid` | A channel with a salience-gated (`participant` / `chair`) member **defaults to `reasoning.mode: bid`** out of the box — semantic silence on open-floor turns (the structured `{ should_post }` verdict supersedes the numeric score; a turn with nothing to add ends in silence *with a reason*). An **ungoverned** channel is unchanged (the knob is inert there; a non-`off` mode is rejected at `validate`). The flip only fills an **absent** mode — an explicit `reasoning.mode: off` in config is preserved. See [RFC 0051 §G](rfcs/0051-reasoning-before-posting.md). |
| **[Kill switch — one flip]** `reasoning.mode: off` restores the prior score gate | Setting a channel to `reasoning.mode: off` restores **byte-for-byte** the prior RFC 0030 scalar score-gate behaviour at runtime (no restart) and is preserved across a boot. The deliberation egress goes flat (`deliberation.*` emits nothing; the scalar `below_threshold` gate runs instead). The one-flip kill switch is the escape hatch for any deployment that wants the old numeric gate back. |
| **[Behaviour — supersession under bid/plan]** Per-member `threshold` superseded | Under `mode: bid` / `plan`, the `{ should_post }` verdict **is** the silence decision — the per-member numeric salience `threshold` no longer governs the speak/skip call. A deployment that tuned `threshold` floors gets **semantic silence** instead; pin `reasoning.mode: off` to keep the numeric gate. |
| **[Capability-gated — rejected at validate]** `reasoning.depth: deep` not yet deployed | `reasoning.depth: deep` (native extended thinking) needs a provider-protocol change and is telemetry-gated on [RFC 0051 OQ 1](rfcs/0051-reasoning-before-posting.md#open-questions); it is **rejected at `validate`** as an unbacked enum value (a 400, no silent downgrade) until RFC 0051 Phase 4 builds it. `shallow` is the only backed depth this release. |
| **[Opt-in — gated to `mode: plan`]** `reasoning.revise: 0|1|2` reflexion loop | The critic→revise reflexion loop is **off by default** (`revise: 0`). `revise ≥ 1` is accepted **only under `mode: plan`** (the critic re-reads the draft against the plan) and **capped at 2** — a `revise ≥ 1` on any other rung, or `> 2`, is a 400. The loop is fail-soft: a hiccup keeps the last good draft and never blocks the post. |
| **[Wire — additive, forward/backward compatible]** `reasoning_mode` / `reasoning_revise` proto fields | `ChannelMessageEvent` gains two additive fields — `reasoning_mode = 25` (string) and `reasoning_revise = 26` (int32) — carrying the channel's resolved rung on the orchestrator → agent dispatch. New field numbers → a pre-v0.3.10 producer leaves them empty/0, which the agent maps to `off` / single-pass, so both are additive across a mixed-version deployment. |

**No store migrates this release.** Unlike v0.3.9 (two channel-store migrations), v0.3.10 advances **no store** — the channel store stays at **v10** and the persona-memory store is untouched. There is **no migration release gate** and no upgrade-on-open step; an already-v10 channel store is a no-op open.

**Version-skew / upgrade-ordering caution.** The orchestrator owns the resolved-rung stamp and the governed-channel default flip. **Upgrade the orchestrator together with (or before) the agents** to arm the flip and the reflexion opt-in; the reverse skew is safe (the fields are additive and default to the prior behaviour — a pre-v0.3.10 agent talking to a v0.3.10 orchestrator simply ignores the new fields). Note it in the release notes.

---

## 4. Manual Test Sign-off

Release sign-off requires the v0.3.10 surface executed and recorded in [`docs/manual-tests/v0.3.10-execution-report.md`](manual-tests/v0.3.10-execution-report.md) — the **headline `MT-REASON-001`** (authored during implementation at the RFC 0051 PR 9 closeout, executed in PR 1) + the carried-forward v0.3.9 / v0.3.8 surface, regression-checked against the v0.3.10 RC tip. The authoritative test index is [`docs/manual-tests/README.md`](manual-tests/README.md).

### Primary v0.3.10 Gate

| Area | Test ID |
|------|---------|
| Reasoning before posting — on the governed `planning` channel against a real provider: a persona on an already-answered / directed-elsewhere turn **stays silent with a stated reason** (observable at `reason_code` granularity); a persona on a turn it can add to posts a **plan-shaped contribution** under `mode: plan`; the private plan / `reason_note` / discarded reflexion draft are **absent** from every other participant's view and the channel transcript; the live `reasoning` knob edits with no restart; the `deliberation.*` / `reflexion.*` telemetry emits; the kill switch (`mode: off`) restores the scalar score gate in one flip; and `reasoning.depth: deep` / `revise ≥ 1` outside `mode: plan` (and `> 2`) are rejected at `validate` — **the v0.3.10 reasoning gate** | `MT-REASON-001` |

### Results Summary (from execution report, `main` RC tip + PR-1 F-1 fix)

**Clean pass** — executed **live on Anthropic**. The governed default is `bid` (semantic silence out of the box, after the F-1 demo-config fix), with an **80% semantic suppress-rate** under `bid` (`deliberation.suppressed{reason_code=nothing_to_add}` = 8 / `deliberation.total` = 10); the suppressed turns post nothing; the latency histogram emits with `budget_starved = 0` and `parse_failures = 0`. Plan-threaded considered posts ship with the private plan markers **0×** in the transcript **and 0×** in both peers' `?as_participant` recall; a reflexion `revised`×2 (rounds×2) walls the discarded draft; and the one-flip `off` kill switch zeroes the deliberation egress (the scalar `below_threshold` gate runs instead) and survives a boot. The deferred edges are rejected at `validate` (`depth: deep`; `revise ≥ 1` outside `mode: plan`; `revise > 2`). Every reasoning go-live structural release-blocker gate is green live on host. **Two findings** surfaced live: **F-1** (the demo `config/channels.yaml` pinned the headline `off` — **fixed in PR 1**) and **F-2** ([ISSUE-0108](issues/ISSUE-0108-reasoning-reason-note-no-operator-egress.md): the verbatim `reason_note` has no operator egress — **documented + deferred**, non-blocking; the §E wall is *stronger* than documented). **0 Fail, 0 Pending** on the v0.3.10 surface.

Checklist:

- [x] [`docs/manual-tests/v0.3.10-execution-report.md`](manual-tests/v0.3.10-execution-report.md) exists (release-prep PR 1 — [#703](https://github.com/mkhomutov/Persatrix/pull/703))
- [x] Each test records date, tester, environment, and evidence
- [x] `MT-REASON-001` recorded `Pass` (**live, Anthropic**) — governed default `bid` + ungoverned `off` + non-`off`-on-ungoverned rejected; `depth: deep` / `revise ≥ 1`-without-`plan` / `revise > 2` rejected at `validate`; **80% semantic suppression** under `bid` (`reason_code=nothing_to_add`); plan-threaded considered posts with the private plan **absent** from the transcript + both peers' `?as_participant` recall; reflexion `revised`×2 (rounds×2) with the discarded draft walled; kill switch restores the scalar gate (`deliberation.*` flat) and survives a boot
- [x] The privacy-wall no-leak (plan + discarded reflexion draft), the `off → bid` + kill-switch + capability-gate + reconcile + proto-stamp, the deliberation/reflexion + idle-cost telemetry, and the RFC 0034 P3 cache/instrument structural gates green on the RC tip (live on host)
- [x] The carried-forward recall / governance / convergence + alias cost-attribution + session/epoch isolation + **cost-regression** ("reasoning is net-neutral-to-saving; suppressed turns skip compose; the idle invariant holds") gates green on the RC tip
- [x] The kill-switch proof captured — `reasoning.mode: off` restores byte-for-byte the prior RFC 0030 scalar score gate at runtime (no restart) and survives a boot; an **ungoverned** channel is unchanged (the knob inert, a non-`off` mode forbidden at `validate`)
- [x] The reason-egress granularity recorded honestly — the silence reason is observable at **`reason_code`** granularity (the `deliberation.suppressed{reason_code}` metric label + the `agent.deliberated` audit line); the verbatim `reason_note` has **no operator egress** (F-2 / [ISSUE-0108](issues/ISSUE-0108-reasoning-reason-note-no-operator-egress.md)) — not a privacy defect (the §E wall is stronger than documented)
- [x] Every non-blocking finding links a tracked issue or follow-up bullet (F-1 → demo-config fix in PR 1; F-2 → [ISSUE-0108](issues/ISSUE-0108-reasoning-reason-note-no-operator-egress.md), deferred)

> The two findings from the execution run — **F-1** (the demo `config/channels.yaml` pinned the v0.3.10 headline `off` with a stale dark-phase comment, so `make demo-anthropic` shipped the feature off out of the box) was **fixed in PR 1** (`planning` now declares `reasoning.mode: bid`, the governed default; `make validate` clean). **F-2** ([ISSUE-0108](issues/ISSUE-0108-reasoning-reason-note-no-operator-egress.md): the verbatim `reason_note` is parsed but has no log egress) is **documented + deferred** — non-blocking, because the deliberation reason is still observable at `reason_code` granularity and wiring a new egress for a deliberately-walled field is feature work for its own reviewed PR. Gap A (the `agent.deliberated` audit's `extra=` payload was dropped by the structlog chain) was **fixed** in [#704](https://github.com/mkhomutov/Persatrix/pull/704); the remaining open scope is the verbatim `reason_note` egress + the §E/MT-REASON-001 doc correction (Gap B). No `Fail` / `Pending` on the v0.3.10 surface.

---

## 5. Tag and GitHub Release Procedure

Use a clean `main` checkout after the final release-prep PR (PR 4 of the prep plan) merges.

```bash
git checkout main
git pull --ff-only origin main
git tag -a v0.3.10 -m "v0.3.10 — Conversations worth posting into"
git push origin v0.3.10
```

GitHub Release checklist (post-merge manual steps after PR 4 merges, by [v0.3.10-plan Phase 4](v0.3.10-plan.md#phase-4--tag-and-post-release-follow-up)):

- [ ] Create the GitHub Release from tag `v0.3.10` — *v0.3.10 — Conversations worth posting into*
- [ ] Use the curated `[0.3.10]` changelog section as the release notes baseline
- [ ] Include upgrade notes (§3.1 — incl. the `off → bid` default flip, the one-flip kill switch, the per-member `threshold` supersession, the `depth: deep` rejection, the `revise` opt-in gated to `mode: plan`, the additive `reasoning_mode` / `reasoning_revise` wire fields + orchestrator-first upgrade ordering, and the **no-store-migration** note) in the release body
- [ ] Include known gaps (§6) in the release body
- [ ] Attach binaries or container references if produced for this release — none expected (matches the v0.3.9 / v0.3.8 … pattern)

---

## 6. Known Gaps to Document in Release Notes

These items remain intentionally incomplete and should be described as deferred scope, not regressions. Seeded from [v0.3.10-release-prep-plan §Out of scope](v0.3.10-release-prep-plan.md) and [§Known follow-up issues](v0.3.10-release-prep-plan.md#known-follow-up-issues).

### v0.3.10-scope gaps

| Area | Description | Deferred to |
|------|-------------|-------------|
| **Native extended-thinking `reasoning.depth: deep`** deferred | Needs a provider-protocol change and is telemetry-gated on [RFC 0051 OQ 1](rfcs/0051-reasoning-before-posting.md#open-questions) — only evaluable after the Phase 3 `deliberation.*` telemetry this release ships has run. `validate` rejects `depth: deep` as unbacked until it is built. | RFC 0051 Phase 4 / future |
| **Operator reasoning-reveal web panel (OQ 6(a)) cut** | The silence-with-reason leg reads the reason at `reason_code` granularity (the `deliberation.suppressed{reason_code}` metric label + the `agent.deliberated` audit line); a web reveal is net-new UI + a debug-egress path, droppable without affecting the headline. A future reveal is a separate egress, never a relaxation of the §E wall ([RFC 0051 OQ 6](rfcs/0051-reasoning-before-posting.md#open-questions)). | follow-on |
| **Verbatim `reason_note` has no operator egress** | The free-text `reason_note` is parsed and carried on the decision but no log statement writes it ([ISSUE-0108](issues/ISSUE-0108-reasoning-reason-note-no-operator-egress.md) Gap B). **Not a privacy defect — the §E wall is *stronger* than documented** (zero egress); it is an observability + doc gap. Gap A (the audit `extra=` drop) is fixed ([#704](https://github.com/mkhomutov/Persatrix/pull/704)); wiring an egress for a deliberately-walled field is feature work for its own reviewed PR. | own PR (Gap B) |
| **No end-user "watch them think" surface** | A separate explicit egress, by design — the §E wall is held inviolate ([RFC 0051 OQ 6(b)](rfcs/0051-reasoning-before-posting.md#open-questions)). | separate egress (never a wall relaxation) |
| **No durable / persistent reasoning store** | The trace is ephemeral + audit-only; a retention-bounded eval sink for offline eval is a separate concern. | possible follow-on |
| **The net-negative-cost claim is empirical** | RFC 0051's cost-delta counterfactual (`mode: bid` shadow arm) proves/refutes the §F saving; it gates the *claim*, not the *ship* ([v0.3.10-plan Risk table](v0.3.10-plan.md#risk-and-mitigations)). Measured, not gating — the live run charted `budget_starved = 0` and suppressed turns skip the compose. | measured, not gating |
| **RFC 0034 `max_turns` / `max_tokens` retune** deferred | Gated on collecting the Phase 3 `conversation_window.*` telemetry this release ships. Defaults unchanged ([RFC 0034 Phase 3](rfcs/0034-phase3-pr-plan.md)). | post-soak |
| **RFC 0045 per-extraction RFCs (0046/0047) + code movement** deferred | v0.3.10 lands only the policy + the `MIT ← BUSL ← Private` dependency-direction CI gate — it moves no code and stands up no private track. | v0.4.0+ |
| **RFC 0039 accounts / auth ("safe remote console")** deferred | A different user story (deployment safety, not conversation quality); a parallel track. | v0.3.11 candidate |
| RFC 0030 **Layer 5 — moderator / bid-and-select** deferred | Carried forward from v0.3.8 / v0.3.9; not a v0.3.10 dependency. | v0.4.0 |

### Carried forward from earlier releases

The v0.3.9-scope and earlier gaps remain open in v0.3.10 unchanged — RFC 0048 Slices 2–5 (memory inspector / isolation verifier / cost / control plane, Slice 5 hard-gated on [RFC 0039](rfcs/0039-user-accounts-authentication.md) auth), channel real-time push + chat token streaming, the unauthenticated channels/REST surface (closed structurally by RFC 0039), RFC 0029 Phases 2–6 Postgres backend, RFC 0009 Phases 3–4, the durable/retention-immune recall store + pre-upgrade closed-stint recall + multi-tenant recall-auth gaps (v0.3.9), and the further-back v0.3.5/v0.3.4 gaps (subject-scoped facts [ISSUE-0084](issues/ISSUE-0084-fact-scope-by-subject-not-uniform-session.md), the `--all-sessions` recall verb [ISSUE-0086](issues/ISSUE-0086-operator-all-sessions-recall-verb.md)). The v0.3.10 release notes link the [v0.3.9](v0.3.9-release-checklist.md#6-known-gaps-to-document-in-release-notes) / [v0.3.8](v0.3.8-release-checklist.md#6-known-gaps-to-document-in-release-notes) checklists for full detail rather than re-listing them.

---

## 7. Summary Checklist

Single-page go/no-go gate. All items must be checked before tagging.

```text
[ ] make test green on the post-bump RC tip — Go ./internal/... -race -cover all pkgs ok no races, incl. the reasoning config (off→bid governed default, explicit-off kill switch, ungoverned non-off rejection, depth:deep / revise≥1-without-plan / revise>2 validate rejections) + reconcile-survives-boot + fan-out/proto-stamp suites; the Python deliberation/plan/reflexion + salience-reasoning suites (118) + the privacy-wall no-leak integration (3) + the RFC 0034 P3 cache/instrument suite (20); session/epoch isolation; Rust + Vitest; NO store migrates this release (channel store stays v10, persona-memory untouched)
[ ] make lint clean — ruff + clippy + go vet; incl. the RFC 0045 imports-check (Python import-linter MIT←BUSL←Private contract) + the Go internal/archpolicy check; golangci-lint + mypy carried by CI
[ ] make validate passes — configs valid (incl. config/channels.yaml planning reasoning.mode: bid — the PR-1 F-1 fix); the reasoning.{mode,model,depth,revise} knob validates (depth:deep rejected; revise≥1 only under mode:plan, capped 2); doc-audit 0 violations; doc_links clean
[ ] make proto then git diff --exit-code shows no generated drift — v0.3.10 adds two ADDITIVE proto fields (ChannelMessageEvent reasoning_mode=25 string, reasoning_revise=26 int32; forward/backward compatible); confirm the committed generated stubs are in sync (CI-pinned protoc/plugin versions)
[ ] make check-licenses — Python + Rust cargo deny ok; Go go-licenses carried by CI
[ ] THIRD_PARTY_NOTICES.md up to date, no genuine delta (no new prod runtime dep — reasoning reuses the leased fast-model seam + existing OTEL shapes; the import-linter gate is a dev/CI dependency)
[ ] make generate-sanitizer-patterns-check passes (Go↔Python parity in sync — reasoning adds no new pattern)
[ ] agents/pyproject.toml updated to 0.3.10
[ ] agents/observability/{metrics,tracing}.py service.version defaults updated to 0.3.10 (incl. tracing.py docstring)
[ ] cli/Cargo.toml updated to 0.3.10
[ ] cli/Cargo.lock regenerated and committed
[ ] internal/server/ui_handlers.go defaultServiceVersion updated to 0.3.10
[ ] web/package.json intentionally NOT bumped (stays 0.0.0 — bundle ships inside the Go binary)
[ ] make all + make ui build successfully at 0.3.10 (PR 3 builds clean; PR 4 re-certifies live, placeholder embed restored)
[ ] CHANGELOG.md contains a curated, dated [0.3.10] section with the Upgrade Notes (§3.1 — incl. the off→bid default flip, the one-flip kill switch, the threshold supersession, the depth:deep rejection, the revise opt-in gated to mode:plan, the additive wire fields + orchestrator-first ordering, and the no-store-migration note)
[ ] Existing [0.3.9] … [0.1.0] sections preserved verbatim
[ ] MT-REASON-001 + the carried-forward v0.3.9/v0.3.8 surface recorded in docs/manual-tests/v0.3.10-execution-report.md (clean pass; 0 Fail / 0 Pending on the v0.3.10 surface; F-1 fixed in PR 1; F-2 / ISSUE-0108 documented + deferred, non-blocking)
[ ] MT-REASON-001 recorded Pass LIVE on Anthropic (governed default bid + ungoverned off; 80% semantic suppress-rate under bid; plan-threaded considered posts with the private plan absent from the transcript + both peers' recall; reflexion revised×2 with the discarded draft walled; one-flip off kill switch restores the scalar gate and survives a boot; depth:deep / revise≥1-without-plan / revise>2 rejected at validate) — the primary v0.3.10 reasoning gate
[ ] Privacy-wall no-leak + off→bid + kill-switch + capability-gate + reconcile + proto-stamp + deliberation/reflexion telemetry + RFC 0034 P3 structural gates green on the RC tip (live on host)
[ ] Alias cost-attribution + session/epoch isolation + cost-regression (reasoning is net-neutral-to-saving — suppressed turns skip compose; the idle invariant holds) gates green on the RC tip
[ ] No store-migration gate this release — a pre-v0.3.10 channel store opens unchanged on the post-bump binary (no migration runs; already-v10 is a no-op open); persona-memory store untouched (PR 4)
[ ] Docker smoke — a governed channel defaults reasoning.mode: bid; a mode:plan turn composes a considered post and a silence turn stays out of the transcript (deliberation.* telemetry emits); the kill switch (mode: off) restores the scalar score gate at runtime; a DM turn still completes (PR 4)
[ ] Known gaps documented for release notes (§6 above)
[ ] git tag v0.3.10 created and pushed (Phase 4)
[ ] GitHub Release published with changelog, upgrade notes, and known gaps (Phase 4)
```
