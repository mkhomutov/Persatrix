# v0.3.1 Manual Test Execution Report

**Version**: v0.3.1 (Memory Quality + Session Plumbing + Conversational Working Memory — RFC 0026 full + RFC 0031 Phase 1 + RFC 0034 Phase 1)
**Report status**: 🔄 In progress — automated pre-manual surface executed and **fully green**; the live-LLM / Docker manual surface is **blocked on `ANTHROPIC_API_KEY`** (see [§Execution constraint](#execution-constraint)). PR 1 of the [v0.3.1 release-prep plan](../v0.3.1-release-prep-plan.md#pr-1--manual-test-execution-report-v031-surface).
**Tester**: Claude (Opus 4.7) — automated suites + local builds
**Execution started**: 2026-05-16 (automated pass)
**Execution completed**: _Live manual surface pending — see [§Execution constraint](#execution-constraint)_
**Target commit**: `main` tip at [ea3bb4d](https://github.com/mkhomutov/Persatrix/commit/ea3bb4d) — release candidate (RFC 0026 / RFC 0031 Phase 1 / RFC 0034 Phase 1 all merged; see [v0.3.1 release-prep plan §Current state](../v0.3.1-release-prep-plan.md#current-state-baseline-2026-05-16)). Update this line if the candidate tip advances before execution completes.

---

## Execution constraint

This pass executed every gate that does **not** require a live LLM. The live-LLM / Docker manual surface could not be executed:

- **`ANTHROPIC_API_KEY` is not set.** The repo `.env` carries the variable but with an empty value. `docker compose` aborts before any container starts — `required variable ANTHROPIC_API_KEY is missing a value` — so the full-stack smoke test and every MT that depends on the composed orchestrator could not run.
- The persona is LLM-backed, so the manual tests asserting **model behaviour** (`MT-PERSONA-CONVERSATION-001`, `MT-MEMORY-005`, the live-reply leg of `MT-CHANNEL-004`, and the live legs of the chat / cost / workflow MTs) cannot be executed without the key.

What this pass **did** verify completely: the 8 automated pre-manual gates (`make validate`, `go test ./...`, Python unit + integration suites, `make lint`, sanitizer parity, proto staleness, `make build`) plus the automated integration tests that back the two new MTs. All green — see [§Automated Test Suites](#automated-test-suites-pre-manual). To complete the report, set `ANTHROPIC_API_KEY` in `.env` and re-run the 26 blocked rows (Docker stack + live MTs); `MT-MEMORY-005` additionally needs two ≥ 11-minute idle gaps.

---

## Scope

This report executes the **30-test v0.3.1 surface** per [v0.3.1 release-prep plan PR 1](../v0.3.1-release-prep-plan.md#pr-1--manual-test-execution-report-v031-surface):

- **3 primary gates** for the v0.3.1 user-facing surface:
  - [`MT-SESSION-001`](MT-SESSION-001.md) — **new** — `PERSATRIX_SESSION_ID` cross-process write contract (RFC 0031 Phase 1).
  - [`MT-PERSONA-CONVERSATION-001`](MT-PERSONA-CONVERSATION-001.md) — **new** — persona conversational continuity over a DM channel (RFC 0034 Phase 1).
  - [`MT-MEMORY-005`](MT-MEMORY-005-dementia-test.md) — the dementia test, **promoted** from a v0.3.0 baseline measurement to a **pass/fail acceptance gate** (RFC 0026).
- **27 carried-forward** v0.3.0 regression tests (21 v0.2.x carry-forward + 6 channel MTs), regression-checking the v0.3.0 surface under the new facts-tier, session-column, and conversation-window code paths.

Per the [v0.3.1 release-prep plan](../v0.3.1-release-prep-plan.md#manual-test-state) the carried-forward inventory is **28 tests** (the 27 listed here plus `MT-MEMORY-005`); with the 2 new MTs the total v0.3.1 execution is **30 tests**.

### Release gate

- Every row reaches ✅ Pass or ⚠️ Accepted-with-known-gap; **zero ❌ Fail; zero ⬜ Not run**.
- `MT-MEMORY-005` Legs 1, 2, 5 record ✅ Pass with per-leg evidence (persona-prompt assembly trace showing the recalled fact / prior turn). A ❌ Fail on Leg 1, 2, or 5 is a **release blocker**.
- `MT-SESSION-001` asserts the **write-path contract only** — a row surfacing across sessions at *recall* time is **not** a failure (RFC 0031 Phase 2 work); note it explicitly so it is not mis-triaged.
- `MT-PERSONA-CONVERSATION-001` is **DM-only** — a group-channel failure is expected (RFC 0034 Phase 2) and is not a v0.3.1 regression.
- Every ⚠️ Accepted-with-known-gap row links a tracked issue or an RFC-closeout follow-up bullet.
- On a green run, the `MT-PERSONA-CONVERSATION-001` and `MT-MEMORY-005` source docs are promoted from `Draft (scaffold)` to `Active` (RFCs 0034 / 0026 landed).

**Gate status this pass**: ❌ **Not met** — 26 of 30 rows are blocked on `ANTHROPIC_API_KEY` (see [§Execution constraint](#execution-constraint)). The automated pre-manual surface is fully green; the live manual surface is pending a re-run with the key set.

---

## Environment

| Component | Version |
|-----------|---------|
| OS | Windows 11 Pro x64 (10.0.26200) |
| Go | go version go1.26.1 windows/amd64 |
| Python | Python 3.13.13 (venv: `./.venv/Scripts/python.exe`) |
| Rust | rustc 1.93.0 (254b59607 2026-01-19) |
| Docker | 29.3.1 (build c2be9cc) |
| `make` | GNU Make 3.81 |
| Compose | Docker Desktop WSL2 backend — Compose v5.1.1 |

---

## Automated Test Suites (Pre-Manual)

All eight gates executed on the [ea3bb4d](https://github.com/mkhomutov/Persatrix/commit/ea3bb4d) release-candidate tip. **All green.**

| Suite | Command | Result | Details |
|-------|---------|--------|---------|
| Config validation | `make validate` | ✅ Pass | 5 file(s) checked, all valid; 3 `instructions_file` references resolve. |
| Go tests | `go test ./... -cover` | ✅ Pass | All packages with tests green — no `FAIL`. Coverage range 24.0% (`cmd/orchestrator`) – 100% (`registry`, `observability/grpcmeta`, `observability/redact`). |
| Python unit tests | `python -m pytest tests/unit/python/` (venv) | ✅ Pass | **2277 passed, 8 skipped** in 105.06s. |
| Python integration tests | `python -m pytest tests/integration/ -c agents/pyproject.toml` (venv) | ✅ Pass | **200 passed, 11 skipped** in 28.19s. |
| Lint (Go / Python / Rust) | `make lint` | ✅ Pass | golangci-lint clean; ruff "All checks passed!"; mypy "no issues found in 126 source files"; clippy no warnings. |
| Sanitizer parity | `make generate-sanitizer-patterns-check` | ✅ Pass | `agents/security_patterns.py` + `agents/security_enums.py` in sync with the Go canonical source. |
| Proto staleness | `make proto && git diff --exit-code` | ✅ Pass | Zero drift — empty `git diff` after regeneration. *Env note*: `protoc-gen-mypy` is in the venv but not on the default PATH; `make proto` needs `.venv/Scripts` on PATH (otherwise `proto-python` fails on the missing plugin — a toolchain-path gap, not code drift). |
| Build | `make build` | ✅ Pass | `bin/persatrix-server.exe` (orchestrator) + `bin/persatrix.exe` (CLI) built. `persatrix --version` reports `0.3.0` — the bump to `0.3.1` is release-prep PR 3. |

### Targeted integration tests — new-MT substrate

The automated substrate tests that back the two new MTs were run explicitly (they mock the LLM, so they run without an API key):

| File | Result | Backs |
|------|--------|-------|
| `tests/integration/test_session_id_cross_process.py` | ✅ 2 passed (`test_two_sessions_coexist_at_storage_layer`, `test_unset_env_lands_legacy`) | `MT-SESSION-001` — cross-process `session_id` write contract. |
| `tests/integration/test_conversational_continuity.py` | ✅ 5 passed (`test_persona_sees_its_own_prior_turn_on_the_next_dm_turn` + 4 degradation cases) | `MT-PERSONA-CONVERSATION-001` — the `messages`-array shape contract. |
| `agents/tests/test_persona_tick_shortcircuit.py` + `agents/tests/test_memory_budget.py` | ✅ 45 passed | `MT-PERSONA-003` (empty-context TICK short-circuit) + `MT-MEMORY-004` (memory injection budget). |

---

## Stack build

❌ **Blocked.** `docker compose` aborts before building any image:

```
error while interpolating services.agent-planner.environment.[]:
required variable ANTHROPIC_API_KEY is missing a value:
ANTHROPIC_API_KEY must be set — export it or add to .env
```

The repo `.env` declares `ANTHROPIC_API_KEY` with an empty value. The stack was not started; every MT that depends on the composed orchestrator is blocked (see [§Results Table](#results-table)).

---

## Summary

| Outcome | Count |
|---------|------:|
| ✅ Pass | 4 |
| ⚠️ Accepted-with-known-gap | 0 |
| ⬜ Not run (blocked — `ANTHROPIC_API_KEY` / Docker stack) | 26 |
| ❌ Fail | 0 |

**Release gate**: ❌ **Not met this pass.** Zero failures, but 26 of 30 rows are blocked on the missing API key. The 8 automated pre-manual gates are fully green and the new-MT substrate tests pass. The 26 blocked rows need a re-run with `ANTHROPIC_API_KEY` set (Docker stack up).

---

## Results Table

Rows are grouped: the **v0.3.1 surface** (the 3 primary gates) first, then the **carried-forward v0.3.0 surface** (regression). Outcome legend: ✅ Pass · ⚠️ Accepted-with-known-gap · ❌ Fail · ⬜ Not run (blocked).

### v0.3.1 surface — primary gates

| Test ID | Title | Outcome | Notes / Issue |
|---------|-------|---------|---------------|
| [MT-SESSION-001](MT-SESSION-001.md) | `PERSATRIX_SESSION_ID` cross-process write contract | ⬜ Not run | **New in v0.3.1** (RFC 0031 Phase 1). The doc-sanctioned automated substitute `tests/integration/test_session_id_cross_process.py` **✅ passed (2 tests)** — the within-process `session_id` write contract is verified. The full cross-process walkthrough (live orchestrator + persona-runtime, Steps 4/7 need the API key) was **not executed** — Docker stack blocked on `ANTHROPIC_API_KEY`. **Write-path only** — a prior-session row at *recall* time would be expected (Phase 2), not a failure. |
| [MT-PERSONA-CONVERSATION-001](MT-PERSONA-CONVERSATION-001.md) | Persona conversational continuity (DM) | ⬜ Not run | **New in v0.3.1** (RFC 0034 Phase 1) — closes [ISSUE-0052](../issues/ISSUE-0052-persona-conversational-working-memory-gap.md). The substrate `tests/integration/test_conversational_continuity.py` **✅ passed (5 tests)** — the `messages`-array shape contract holds. The manual prose-behaviour assertion (Leg 1 self-reference + Leg 2 referential follow-up) requires a **live LLM-backed persona** — **blocked on `ANTHROPIC_API_KEY`**. Source doc stays `Draft (scaffold)` until executed. |
| [MT-MEMORY-005](MT-MEMORY-005-dementia-test.md) | Persona Memory — Dementia Test | ⬜ Not run | **Promoted to a pass/fail acceptance gate in v0.3.1.** Requires a live LLM-backed persona and two ≥ 11-min idle gaps — **blocked on `ANTHROPIC_API_KEY`**. Per-leg table in [§MT-MEMORY-005 acceptance](#mt-memory-005-acceptance-pass-fail-gate) all ⬜ Not run. Source doc stays `Draft (scaffold)` until executed. |

### Carried-forward v0.3.0 surface — regression

The "v0.3.0 ref" is the outcome recorded in [`v0.3.0-execution-report.md`](v0.3.0-execution-report.md).

| Test ID | Title | Outcome | Notes / Issue |
|---------|-------|---------|---------------|
| [MT-CHANNEL-001](MT-CHANNEL-001.md) | `channel list` / `join` end-to-end | ⬜ Not run | Blocked — needs the composed orchestrator. v0.3.0 ref: ✅ Pass. |
| [MT-CHANNEL-002](MT-CHANNEL-002.md) | `channel send` / `reply` / `history` | ⬜ Not run | Blocked — needs the composed orchestrator. v0.3.0 ref: ✅ Pass. |
| [MT-CHANNEL-003](MT-CHANNEL-003.md) | `channel watch` polling, dedup, full-page warning | ⬜ Not run | Blocked — needs the composed orchestrator. v0.3.0 ref: ✅ Pass. |
| [MT-CHANNEL-004](MT-CHANNEL-004.md) | Human-mentions-agent end-to-end (live LLM reply) | ⬜ Not run | Blocked — needs the composed orchestrator **and** a live LLM reply. v0.3.0 ref: ✅ Pass. |
| [MT-CHANNEL-005](MT-CHANNEL-005.md) | DM canonicalization round-trip | ⬜ Not run | Blocked — needs the composed orchestrator. v0.3.0 ref: ✅ Pass. Recipe updated for the `session_id` → `chat_session_id` metadata-key rename. |
| [MT-CHANNEL-006](MT-CHANNEL-006.md) | Channel deletion + cascade | ⬜ Not run | Blocked — needs the composed orchestrator. v0.3.0 ref: ✅ Pass. |
| [MT-LOGS-001](MT-LOGS-001.md) | `persatrix logs` (REST + SSE follow + pretty + restart durability) | ⬜ Not run | Blocked — needs the composed orchestrator. v0.3.0 ref: ✅ (Step 4 ⚠️ — `Buffer.Seal` never invoked from production, RFC 0018 PR 7 follow-up). |
| [MT-OTEL-001](MT-OTEL-001.md) | OpenTelemetry traces + metrics + Collector tail-sampling | ⬜ Not run | Blocked — needs the composed observability stack. v0.3.0 ref: ✅ Pass. |
| [MT-INTEGRATION-001](MT-INTEGRATION-001.md) | Docker Compose full-stack smoke test end-to-end | ⬜ Not run | Blocked — `docker compose` aborts on the empty `ANTHROPIC_API_KEY` (see [§Stack build](#stack-build)). v0.3.0 ref: ✅ Pass. |
| [MT-WORKFLOW-001](MT-WORKFLOW-001.md) | Submit YAML workflow via REST, poll to completion | ⬜ Not run | Blocked — needs a running orchestrator + a live workflow run. v0.3.0 ref: ✅ Pass. |
| [MT-WORKFLOW-002](MT-WORKFLOW-002.md) | Submit invalid workflow, verify clean error response | ⬜ Not run | Blocked — needs a running orchestrator (error-path only, no LLM, but not executed this pass). v0.3.0 ref: ✅ Pass. |
| [MT-AGENT-001](MT-AGENT-001.md) | Task agent executes a builtin tool (no LLM required) | ✅ Pass | Covered by the **200-passing Python integration suite** (agent-server, persona-e2e, memory-budget, span-links, trace-propagation) — same evidence basis as the v0.3.0 report. v0.3.0 ref: ✅ Pass. |
| [MT-CLI-001](MT-CLI-001.md) | `persatrix run` end-to-end against a running orchestrator | ⬜ Not run | Blocked — needs a running orchestrator + a live run. CLI binary builds clean (`make build`). v0.3.0 ref: ✅ Pass. |
| [MT-CONFIG-001](MT-CONFIG-001.md) | `make validate` catches malformed `config/agents.yaml` | ✅ Pass | `make validate` executed — 5 files checked, all valid; `instructions_file` references resolve. The new `conversation_window` config block + `schemas/optimization.schema.json` validate cleanly. v0.3.0 ref: ✅ Pass. |
| [MT-PERSONA-001](MT-PERSONA-001.md) | Start semi-autonomous persona; verify tick loop and logged actions | ⬜ Not run | Blocked — needs a live LLM-backed persona. Backing persona-e2e integration suite passes (see [§Automated Test Suites](#automated-test-suites-pre-manual)). v0.3.0 ref: ✅ Pass. |
| [MT-PERSONA-002](MT-PERSONA-002.md) | Persona handles an inbound channel message and produces a logged response | ⬜ Not run | Blocked — needs a live LLM-backed persona. Backing persona-e2e integration suite passes. v0.3.0 ref: ✅ Pass. |
| [MT-PERSONA-003](MT-PERSONA-003.md) | Empty-context TICK short-circuit suppresses LLM calls (RFC 0017 §F) | ✅ Pass | `agents/tests/test_persona_tick_shortcircuit.py` executed — part of the 45-passing targeted run. The verification method *is* this pytest run. v0.3.0 ref: ✅ Pass. |
| [MT-MEMORY-001](MT-MEMORY-001.md) | Episodic memory: write and recall across agent restart | ⬜ Not run | Blocked — needs a live persona. Backing persona-e2e episodic-tier suite passes. v0.3.0 ref: ✅ Pass. |
| [MT-MEMORY-002](MT-MEMORY-002.md) | Relationship memory: trust score updates after N exchanges | ⬜ Not run | Blocked — needs a live persona. Backing persona-e2e relationship-tier suite passes. v0.3.0 ref: ✅ Pass. |
| [MT-MEMORY-003](MT-MEMORY-003.md) | Working memory: summarisation triggers near context-window threshold | ⬜ Not run | Blocked — needs a live LLM (summariser). Working-memory unit coverage passes (part of the 2277-test unit run). v0.3.0 ref: ✅ Pass. |
| [MT-MEMORY-004](MT-MEMORY-004.md) | Memory injection token budget: per-event bound holds (RFC 0017 §B) | ✅ Pass | `agents/tests/test_memory_budget.py` executed — part of the 45-passing targeted run. The verification method *is* this pytest run. v0.3.0 ref: ✅ Pass. |
| [MT-COST-001](MT-COST-001.md) | `GET /api/v1/cost/summary` reports token usage for a completed run | ⬜ Not run | Blocked — needs a live completed run against the orchestrator. v0.3.0 ref: ✅ Pass. |
| [MT-COST-002](MT-COST-002.md) | Workflow exceeding budget is aborted with the expected reason | ⬜ Not run | Blocked — needs a running orchestrator. v0.3.0 ref: ⚠️ Accepted-with-known-gap (no budget-enforcement changes in v0.3.1). |
| [MT-CHAT-001](MT-CHAT-001.md) | Chat REST endpoint: send message, receive reply | ⬜ Not run | Blocked — needs the composed orchestrator + a live LLM reply. v0.3.0 ref: ✅ Pass. Recipe updated for the `session_id` → `chat_session_id` JSON-key rename. |
| [MT-CHAT-002](MT-CHAT-002.md) | `persatrix chat` CLI interactive session | ⬜ Not run | Blocked — needs a running orchestrator + a live persona. v0.3.0 ref: ✅ Pass. |
| [MT-CHAT-003](MT-CHAT-003.md) | Chat session continuity: messages persist across agent restart | ⬜ Not run | Blocked — needs a live persona. Backing session-store suite passes. v0.3.0 ref: ✅ Pass. Recipe updated for the `chat_session_id` rename. |
| [MT-CHAT-004](MT-CHAT-004.md) | User-agent relationship: trust score evolves after chat exchanges | ⬜ Not run | Blocked — needs a live persona. Backing relationship-tier suite passes. v0.3.0 ref: ✅ Pass. Recipe updated for the `chat_session_id` rename. |

---

## MT-MEMORY-005 acceptance (pass/fail gate)

`MT-MEMORY-005` runs as **Variant V2** — a pass/fail acceptance gate, not the v0.3.0 baseline measurement. Canonical timing: a five-interaction scenario with **two ≥ 11-minute idle gaps** (between I2/I3 and I4/I5) to exercise [RFC 0020](../rfcs/0020-interaction-lifecycle.md) idle-gap closure. Run on a fresh memory namespace (`make reset` or a designated test user). Capture per-tier prompt-assembly provenance (`PERSATRIX_MEMORY_PROVENANCE=1`) so each leg fail is classified recall miss vs. reasoning miss.

**Status**: ⬜ **Not run** — blocked on `ANTHROPIC_API_KEY` (the persona is LLM-backed). To be executed on a re-run with the key set.

**User**: _TBD_ · **Persona**: `ember-owl` · **Timing (UTC)**: _TBD_

| Leg | Signal class | Established → Triggered | Expected v0.3.1 (V2) outcome | Result | Evidence |
|-----|--------------|--------------------------|------------------------------|--------|----------|
| 1 — Named Entity | named entity | I1 → I4 | ✅ **Pass** — facts tier admits `(sender, has_child_named, "Mira")` on the trigger turn | ⬜ Not run | _TBD_ |
| 2 — Stated Preference | preference | I2 → I5 | ✅ **Pass** — `(sender, prefers, "text or async")` / `(sender, dislikes, "phone calls")` admits | ⬜ Not run | _TBD_ |
| 3 — Explicit Commitment | commitment | I3 → I5 | Holds v0.3.0 outcome (✗ — structured commitment tracking is v0.4.0 / RFC 0021 P2; **not a regression**) | ⬜ Not run | _TBD_ |
| 4 — Paraphrase Recall | paraphrase | I1 → I5 | Holds v0.3.0 outcome (paraphrase recall is the MQ-8 / RFC 0024 signal — facts tier does not cover it) | ⬜ Not run | _TBD_ |
| 5 — Self-Consistency | self-consistency | I1 → I5 | ✅ **Pass** — `(self, has_preference, …)` admits via the RFC 0026 PR 4 `self`-subject seed | ⬜ Not run | _TBD_ |

**Acceptance**: Legs 1, 2, 5 record ✅ Pass with per-leg prompt-assembly evidence; Legs 3, 4 hold their v0.3.0 outcomes. A ❌ Fail on Leg 1, 2, or 5 is a **release blocker**. A Leg 3 or Leg 4 fail is **not** a v0.3.1 regression — it links the standing RFC 0021 P2 / MQ-8 (RFC 0024) follow-ups.

---

## Follow-ups

- **26 blocked rows pending a re-run with `ANTHROPIC_API_KEY` set.** Set the key in `.env`, `make docker-build && make docker-up`, then execute the Docker stack health check + the 26 blocked MTs (`MT-MEMORY-005` additionally needs two ≥ 11-min idle gaps). No tracked issue — this is an environment gap on the test host, not a product defect.
- No in-tree release-prep regressions surfaced this pass — all 8 automated gates were green on the first run.

---

## Notes

- The proto-staleness gate (`make proto`) needs the venv `Scripts` directory on PATH so `protoc` can find the `protoc-gen-mypy` plugin; without it, `proto-python` fails on a missing-plugin error. This is a host PATH gap, not proto drift — the regenerated stubs produced an empty `git diff`. Worth a one-line note in the release checklist's environment-setup section.
- The automated surface grew since v0.3.0 (PR 4 rerun: 1921 unit / 168 integration) to **2277 unit / 200 integration** — consistent with the RFC 0026 / 0031 / 0034 implementation PRs.

---

## Re-Execution

The clean-checkout re-run is [v0.3.1 release-prep plan §Track B PR 4](../v0.3.1-release-prep-plan.md#pr-4--final-pre-tag-verification--release-notes) (final pre-tag verification). PR 4 re-runs `MT-MEMORY-005` on the docker-composed orchestrator at the release-candidate tip and appends the per-leg result here as tag-time evidence.

The immediate next step for **this** PR is to set `ANTHROPIC_API_KEY` and execute the 26 blocked rows so the report can reach the green release gate.
