# v0.3.13 Manual Test Execution Report

**Version**: v0.3.13 (Deferred calls closed — [ISSUE-0118](../issues/ISSUE-0118-tool-recall-bypasses-epoch-session-scopes.md) tool-recall scope threading × [ISSUE-0121](../issues/ISSUE-0121-crossroom-person-identity-legs-never-run-live.md) person-identity legs live × [ISSUE-0114](../issues/ISSUE-0114-per-channel-cascade-depth-override.md) per-channel cascade depth, with the [ISSUE-0116](../issues/ISSUE-0116-fact-subject-renders-unquarantined.md) fold-in taken)
**Report status**: 🔄 In progress — the continuity bar is green on the RC tip (eval-replay 5/5); the live arcs (the release gate) and the offline smoke are pending
**Tester**: Claude (Fable 5) — live Docker stack (Anthropic) + host structural gates
**Execution date**: 2026-08-04 (in progress)
**Target commit**: `main` tip `20a667d3` ([#812](https://github.com/mkhomutov/Persatrix/pull/812) — release-prep PR 0). This is PR 1 of the [v0.3.13 release-prep sequence](../v0.3.13-release-prep-plan.md#pr-1--manual-test-execution-report).

> **Environment note.** Structural gates run live on host against the RC tip working tree; the memory and cascade arcs run on a freshly reset (`make reset`) Docker stack rebuilt at the RC tip on a real Anthropic provider. All recorded results are from real runs — none inferred.

---

## Scope

Per [release-prep plan PR 1](../v0.3.13-release-prep-plan.md#pr-1--manual-test-execution-report):

- **[`MT-MEMORY-CROSSROOM-001`](MT-MEMORY-CROSSROOM-001.md) — the whole v1.1 arc**, live on a real provider: Legs 1/2/3 re-confirmed; **Leg 1b** (relationship row keyed on the sender id, typed `user`) and **Leg 2b** (recognition in the group room) on their **first live run** — the [ISSUE-0121](../issues/ISSUE-0121-crossroom-person-identity-legs-never-run-live.md) close; **Leg 4** on a fresh channel under `--epoch mt-crossroom-fresh` with a tool-bearing persona — the [ISSUE-0118](../issues/ISSUE-0118-tool-recall-bypasses-epoch-session-scopes.md) proof **only with a memory-tool round evidenced in provenance on that turn** (the round is model-elected: a round-free green leg proves only the injection wall and mandates a re-probe, per the [master plan's acceptance](../v0.3.13-plan.md#acceptance-for-v0313)).
- **The per-channel cascade-depth arc** on a live autonomous roster — a channel capped below the fleet value closes at its own bound while the fleet default binds elsewhere, and a live PATCH edit above the fleet cap applies with the loud warning — the [ISSUE-0114](../issues/ISSUE-0114-per-channel-cascade-depth-override.md) close.
- **`make eval-replay`** — all five goldens on the RC tip (the continuity bar).
- **The carried-forward surface** — the offline `make demo-autonomous` smoke ($0) + regression spot checks.

Findings triage per the MT's note: wiring regressions are release-blocking; capture/reasoning misses become scoped fixes or `Accepted-with-known-gap` rows citing a tracked issue. Red legs 1b/2b become in-release fix PRs, not re-deferrals.

## Environment

| Component | Version |
|-----------|---------|
| OS | macOS 26.5.2 (`arm64`) |
| Go | 1.26.3 (`darwin/arm64`) |
| Python | 3.12.13 (repo `.venv`) |
| Rust / cargo | 1.93.1 — CLI built from tree at `0.3.12` (RC strings; PR 3 bumps) |
| Node | 22.16.0 |
| Docker | 29.5.2 — `make reset` (volumes purged) before each stack leg; images rebuilt at the RC tip |
| protoc | 35.0 (host; CI pins 34.1 — no regen this PR) |
| Provider | Anthropic (memory + cascade arcs); mock (offline smoke) |

---

## Structural / Automated Gates (live, on host)

| Gate | Command | Result |
|------|---------|--------|
| RFC 0044 golden replay — the five-recipe sweep, the continuity-bar acceptance item | `make eval-replay PYTHON=.venv/bin/python` | ✅ **5/5 recipes** — `EVAL-MEMORY-001` 6/6, `EVAL-MEMORY-002` 3/3, `EVAL-MEMORY-003` 4/4, `EVAL-MEMORY-004` 5/5, `EVAL-WORKING-001` 5/5 |

---

## MT-MEMORY-CROSSROOM-001 — the whole v1.1 arc (live, Anthropic)

**⬜ Pending.** Fresh `make reset` stack at the RC tip; the arc machine-paced as one script (teaches + introduction → shared idle window → bridges → asks — no human-paced gap racing a timing window); persona run with `PERSATRIX_MEMORY_PROVENANCE=1` throughout (the Leg 4 tool-round evidence *and* the Leg 2b wiring-vs-reasoning triage).

| Leg | Check | Observed | Verdict |
|-----|-------|----------|---------|
| 1 | Teach + close (DM) — facts extracted at close, `internal`, under the DM room's session id | — | ⬜ Pending |
| 1b | **First live run** — introduction in the DM writes the relationship row keyed on the sender id (`alex`), typed `user` | — | ⬜ Pending |
| 2 | The headline: standup ask — the DM-taught fact recalled cross-room via the live L2 widening | — | ⬜ Pending |
| 2b | **First live run** — recognition in the group room: the persona uses the DM-stated identity for the same `(principal, epoch)` | — | ⬜ Pending |
| 3 | DM re-ask — verbatim continuity | — | ⬜ Pending |
| 4 | Fresh-epoch wall (fresh probe channel, `--epoch mt-crossroom-fresh`, tool-bearing persona) — **counts as the ISSUE-0118 proof only with a memory-tool round evidenced in provenance on that turn, recorded verbatim below**; re-probe until a round occurs | — | ⬜ Pending |

**Leg 4 provenance record** (to be filled verbatim from the run): —

---

## Per-channel cascade-depth arc (live, Anthropic, autonomous roster)

**⬜ Pending.** A live roster with one channel capped below the fleet value; the [ISSUE-0114](../issues/ISSUE-0114-per-channel-cascade-depth-override.md) close.

| # | Check | Observed | Verdict |
|---|-------|----------|---------|
| 1 | The capped channel closes at its own bound (fanout suppression + inbound clamp + `closeOnCascadeBound` all reading the per-channel value) | — | ⬜ Pending |
| 2 | The fleet default still binds on an uncapped channel | — | ⬜ Pending |
| 3 | A live `PATCH …/config` edit above the fleet cap applies with the loud warning (the option (c) posture — warn, never brick) | — | ⬜ Pending |

---

## Offline smoke — `make demo-autonomous` (mock provider, $0)

**⬜ Pending.** Full arc on a clean store (`make reset`), zero keys, zero spend, zero human turns — boot → arm → convene → cadence ladder → structural bounded close → chair synthesis → all-N real summaries.

---

## Carried-forward surface — regression

**⬜ Pending.** Spot checks ride the arcs above: floor control + salience bids on the group turns, interaction idle rotation on schedule, chat-as-DM on the DM legs, wallet leases metered and reconciled, and the offline smoke re-proving the RFC 0052 arc at $0.

---

## Findings & follow-ups

None yet — populated as the arcs run.

---

*Structural template: [v0.3.12 execution report](v0.3.12-execution-report.md).*
