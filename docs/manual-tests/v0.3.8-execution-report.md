# v0.3.8 Manual Test Execution Report

**Version**: v0.3.8 (Conversations that converge — the brainstorm rung: [RFC 0030 governance Layers 1/2/4](../rfcs/0030-multi-agent-conversation-governance.md) + the [relevance Tier B salience bid + `chair`](../rfcs/0030-amendment-relevance-gated-response.md) + the [RFC 0020 interaction-summary surface](../rfcs/0020-interaction-lifecycle.md), with [RFC 0050 operator-editable channel config](../rfcs/0050-extensible-channel-configuration.md) folded in)
**Report status**: ✅ Complete — **clean pass.** Executed **live** on the `main` RC tip. The four convergence behaviours that own the v0.3.8 headline all pass live on one multi-persona channel: **Tier B no-pile-on** ([`MT-CHANNEL-RELEVANCE-002`](MT-CHANNEL-RELEVANCE-002.md)), **end-vote convergence + summary** ([`MT-CHANNEL-GOV-003`](MT-CHANNEL-GOV-003.md)), **chair stall escalation + synthesis** ([`MT-CHANNEL-GOV-004`](MT-CHANNEL-GOV-004.md)), and the **Layer 1 cost-ceiling fail-closed** path — plus the **combined convergence walkthrough** (no-pile-on **and** bounded cost **and** ends-on-votes **and** a readable summary, observed together on one channel). One finding surfaced — **F-1** (a stale operator-facing CLI note that says an *enforced* budget ceiling is "not yet enforced"); non-blocking, ✅ fixed in this PR (`b111985`). **The v0.3.8 convergence release gate is met.**
**Tester**: Claude (Opus 4.8) — Go 1.26.3 host build + live Docker stack on **Anthropic** (`quality` → `claude-sonnet-4-6`, `fast`/`summarizer` → `claude-haiku-4-5-20251001`)
**Execution date**: 2026-06-16
**Target commit**: `main` tip `8897727` ([v0.3.8 release-prep plan / #662](https://github.com/mkhomutov/Persatrix/pull/662)) — all four convergence workstreams fully landed (see [release-prep plan § RFC PR roll-up](../v0.3.8-release-prep-plan.md#rfc-pr-roll-up)). This is PR 1 of the [v0.3.8 release-prep sequence](../v0.3.8-release-prep-plan.md#pr-1--manual-test-execution-report-v038-surface).

> **✅ Environment note (read first).** The host carries Go 1.26.3 (`darwin/arm64`), Node 22.16, Docker 29.5.2 and the repo `.venv` (Python 3.12.13), with a valid Anthropic key supplied to the stack via `.env`. The stack was reset (`make reset`, volumes purged) and **rebuilt from the `8897727` RC tip** (`ENABLE_UI=1 docker compose -f docker-compose.yaml -f docker-compose.anthropic.yaml up -d --build`); orchestrator + agent images are dated `2026-06-16T06:52Z` (after the RC tip), 11 services healthy, `/healthz` → `200`, `build.version` → `0.3.7` (the version bump is PR 3). Between governance MTs the stack was reset to a clean interaction baseline so each close arc is unconfounded. Every result below represents the RC tip. The web-console legs of the RFC 0050 config MTs (settings panel render) ran live in the [RFC 0050 closeout #661](https://github.com/mkhomutov/Persatrix/pull/661); here their capability + underlying REST surface are re-confirmed on the RC tip.

---

## Scope

This report executes the **v0.3.8 convergence surface** scoped by
[release-prep plan PR 1](../v0.3.8-release-prep-plan.md#pr-1--manual-test-execution-report-v038-surface):

- **Five headline MTs** — authored during implementation, **executed here** on the RC tip:
  - [`MT-CHANNEL-RELEVANCE-002`](MT-CHANNEL-RELEVANCE-002.md) — Tier B salience / no-pile-on (**primary v0.3.8 gate**).
  - [`MT-CHANNEL-GOV-003`](MT-CHANNEL-GOV-003.md) — end-vote convergence + summary + fresh interaction after close.
  - [`MT-CHANNEL-GOV-004`](MT-CHANNEL-GOV-004.md) — chair stall escalation (forced synthesis + announced close).
  - [`MT-INTERACTION-SUMMARY-001`](MT-INTERACTION-SUMMARY-001.md) — interaction-summary surface (web + CLI; honest sentinel).
  - [`MT-CHANNEL-CONFIG-001`](MT-CHANNEL-CONFIG-001.md)…[`004`](MT-CHANNEL-CONFIG-004.md) — RFC 0050 channel config (re-run on the RC tip; ran live in [#661](https://github.com/mkhomutov/Persatrix/pull/661)).
- **The combined convergence walkthrough** — authored-and-executed here: no-pile-on (Tier B) + bounded cost (Layer 1) + an end-vote close (Layer 4) + a readable summary, one multi-persona session (the role the master plan reserved for `MT-CONVERSATION-CONVERGENCE-001`).
- **The carried-forward v0.3.7 surface** — regression-checked against the v0.3.8 RC tip by the automated suites.

### Release gate

- Every **Tier B + governance + interaction-id + channel-config + both-migration structural gate** is green on the RC tip (the automated release-blocker counterpart to the MTs). ✅ **Met, live.**
- [`MT-CHANNEL-RELEVANCE-002`](MT-CHANNEL-RELEVANCE-002.md) — ✅ **Pass live**: small relevant set on the open floor, silence on a redundant follow-up, NL bias toward the named persona, the `chair` facilitates but never closes; idle/`observer` persona costs **zero**; every bid leased on the `fast` alias + attributable.
- [`MT-CHANNEL-GOV-003`](MT-CHANNEL-GOV-003.md) — ✅ **Pass live**: closed on **K=2 distinct votes** (`trigger=end_votes votes=2`) before the depth cap; summary names the decision; the next message opens a fresh interaction.
- [`MT-CHANNEL-GOV-004`](MT-CHANNEL-GOV-004.md) — ✅ **Pass live**: a zero-reply round forced **one** chair turn (`chair_escalation{outcome=dispatched}=1`) carrying a real synthesis; the quorum disposed; the close was **announced**, not buried as "went idle".
- [`MT-INTERACTION-SUMMARY-001`](MT-INTERACTION-SUMMARY-001.md) — ✅ **Pass (surface)** live ×3 (Part B — end-vote/structural closes); Part A (DM `idle_gap` trigger) **Accepted-with-known-gap** (see [Findings](#findings--follow-ups)).
- [`MT-CHANNEL-CONFIG-001`](MT-CHANNEL-CONFIG-001.md)/[`003`](MT-CHANNEL-CONFIG-003.md) — ✅ **Pass live** on the RC tip (CLI live-edit + cross-surface read-back; server-side interaction-budget fail-closed enforcement). [`002`](MT-CHANNEL-CONFIG-002.md)/[`004`](MT-CHANNEL-CONFIG-004.md) (web slices) — capability + REST surface re-confirmed on the RC tip; ran live in [#661](https://github.com/mkhomutov/Persatrix/pull/661).
- Combined convergence walkthrough ✅ **captured** (no-pile-on + bounded cost + ends-on-votes + readable summary together on `group:planning`).
- **F-1** (stale CLI "not yet enforced" note on `interaction_budget_tokens`) — non-blocking; the underlying enforcement is live and fail-closed (proven by CONFIG-003). ✅ Fixed in this PR (`b111985`).

---

## Environment

| Component | Version |
|-----------|---------|
| OS | macOS (Darwin 24.6.0, `arm64`) |
| Go | **1.26.3** (`darwin/arm64`) — structural Go gates ran live |
| Python | 3.12.13 (repo `.venv`), pytest |
| Rust / cargo | 1.93.1 |
| Node | 22.16.0 |
| Docker | 29.5.2 |
| Stack | `ENABLE_UI=1 docker compose -f docker-compose.yaml -f docker-compose.anthropic.yaml up -d --build` — orchestrator + 6 agents (planner, coder, reviewer, ember-owl, iron-fox, nova-sparrow) + otel-collector + jaeger + prometheus + loki; **rebuilt from `8897727`**; images dated `2026-06-16T06:52Z`; 11 services healthy; `/healthz` → `200`. |
| Provider | **Anthropic** — `quality` → `claude-sonnet-4-6`, `fast`/`summarizer` → `claude-haiku-4-5-20251001` (config-driven, RFC 0033). Key valid (supplied via `.env`). |
| Channel under test | `group:planning` — `iron-fox`/`nova-sparrow` = `participant` (salience-gated), `ember-owl` = `addressed` (`when_mentioned`), `escalation_chair_id` = `nova-sparrow`, end-vote `K=2`/`W=3`, idle `600s`, budgets uncapped by default. |

---

## Structural / Automated Gates (live, on the RC tip `8897727`)

All run locally on `8897727`. **All green.** These are the **automated release-blocker** counterpart to the MTs.

| Gate | Command | Result |
|------|---------|--------|
| **Tier B salience bid + response gate + disposition + end-vote action** | `pytest test_salience_bid.py test_response_gate_relevance.py test_response_gate_disposition.py test_end_interaction_vote_action.py` | ✅ **87 passed** |
| **Persona-memory v14→v15 governance-id migration + episodic schema** | `pytest test_governance_id_migration.py test_episodic_schema_migration.py` | ✅ **23 passed** |
| **Interaction lease threading + sub-agent lease + cross-language wire drift** | `pytest test_interaction_lease_threading.py test_subagent_lease.py test_cross_language_interaction_wire_drift.py` | ✅ **16 passed** |
| **Chair moderation + escalation (Python)** | `pytest -k "chair or escalation"` | ✅ **42 passed** |
| **Go channels suite** (governance Layers 1/2/4, interaction-id resolver, `interaction_convergence_test.go`, channel-config store/REST, channel-store v6→v7→v8 migration, chair escalation) | `go test ./internal/channels/` | ✅ **ok** |
| **Go server suite** (channel-config REST, member-config PATCH, UI capabilities) | `go test ./internal/server/` | ✅ **ok** |
| **Go wallet suite** (Layer 1 interaction-budget ceiling denial) | `go test ./internal/wallet/` | ✅ **ok** |
| **Consolidated convergence Python suite** (the three groups above minus `test_subagent_lease.py`) | `pytest …` (8 files) | ✅ **123 passed** |

The **two migration suites** are release gates: the channel-store v6→v7 (Tier B `threshold` column) → v8 (RFC 0050 config columns) forward-migration is pinned by the Go `internal/channels` suite; the persona-memory v14→v15 (`episodes.governance_interaction_id` + backfill) forward-migration is pinned by `test_governance_id_migration.py` (23 passed, incl. backfill + already-v15 no-op + crash-replay). Both verified end-to-end on the post-bump binary in [PR 4](../v0.3.8-release-prep-plan.md#pr-4--final-pre-tag-verification--release-notes).

---

## MT-CHANNEL-RELEVANCE-002 — Tier B salience / no-pile-on (live, Anthropic)

**Channel**: `group:planning`. **Verdict: ✅ Pass.**

| Step | Action | Observed | Verdict |
|------|--------|----------|---------|
| 2 | Open-floor question ("biggest risk in shipping the v0.4 event log?") | **2 of 3** replied — `nova-sparrow` (chair, low threshold) + `iron-fox` (in-lane participant); `ember-owl` (`when_mentioned`) silent. Small relevant set, no pile-on. | ✅ |
| 3 | Redundant follow-up ("sounds like data-loss is the main risk — anything to add?") | **Zero** persona replies. No pile-on of agreement. | ✅ |
| 4 | NL addressing in free text ("let's hear from Iron Fox …", no `@`) | `nova-sparrow` (chair) facilitated a hand-off, then **`iron-fox` replied** (the named persona); `ember-owl` deferred (silent). NL biased toward the named persona, not a hard filter. | ✅ |
| 5 | Continue open-floor turns | The `chair` participated but **never closed** the interaction (the inert Layer-5 contract). | ✅ |
| 6 | Reply counts + bid attribution | Final: `iron-fox`=3 msgs, `nova-sparrow`=2, `operator`=3, `ember-owl`=0. | ✅ |

**Telemetry (Prometheus / cost summary):**
- `channel_messages_gated_total{policy=low_salience,reason=below_threshold}` = **1** — a Tier B salience drop (a participant honestly passed).
- `channel_messages_gated_total{policy=when_mentioned}` = **3** — `ember-owl` gated on the open floor.
- `agent_llm_calls_total`: `iron-fox` 3 × `claude-haiku-4-5` (**fast-alias bids**) + 3 × `claude-sonnet-4-6` (**quality turns**); `nova-sparrow` 3 × haiku + 2 × sonnet; **`ember-owl` absent entirely** (zero LLM calls).
- `cost/summary`: only `iron-fox` + `nova-sparrow` billed; **`ember-owl` cost zero** (idle persona costs nothing). Every bid leased on the `fast` alias + attributable (TB3).

---

## MT-CHANNEL-GOV-003 — End-vote convergence (live, Anthropic)

**Channel**: `group:planning`. **Verdict: ✅ Pass.**

- **Step 1 — closable question** ("name the service 'relay' or 'beacon'?"). The chair first bounced the decision back ("What's your lean, alex?") — a realistic stall; nudged with a lean (the MT permits a follow-up). All discussion messages (8) carried **one shared** `interaction_id` `e2e4c026…` — no row missing it.
- **Step 2 — convergence on votes.** After a sign-off invitation, `iron-fox` ("I'm in — already said my piece. Let's call it.") and `nova-sparrow` ("Nothing further from me — 'relay' it is.") emitted **K=2 distinct** `END_INTERACTION_VOTE`s within `W=3`. Orchestrator: `channels: interaction closed by end-of-interaction votes … trigger=end_votes votes=2`. No replies after the close. **No `governance_drop{layer=depth}`** occurred — the semantic terminator beat the depth backstop.
- **Step 3 — summary surface.** `persatrix agent interactions iron-fox --limit 1` → **"Conversation ended (2 turns)"**, governance `e2e4c026…`, summary names the converged decision ("the group reached consensus to use 'relay' …"). Not blank, not the sentinel.
- **Step 4 — the room lives on.** A new topic ("Friday's retro") opened a **fresh** `interaction_id` `226bb8e1…` (≠ the closed `e2e4c026…`); both personas re-engaged with concrete retro items.

---

## MT-CHANNEL-GOV-004 — Chair stall escalation (live, Anthropic)

**Channel**: `group:planning`. **Verdict: ✅ Pass.**

- **Step 1 — engineered stall.** Enumeration prompt ("name exactly one risk each, no repeats", mention-all) → `ember-owl` (regression coverage), `iron-fox` (rollback path), `nova-sparrow` (raw error messages) each named one distinct risk. The redundant follow-up ("Anything else on this?") drew a **zero-reply floor round**: `channel_conversation_floor_turn_total{outcome=timeout}` = **2** (granted = 2, replied = 0 — the granted-but-silent stall, not a vacuous round).
- **Step 2 — forced chair turn.** Orchestrator: `channels: stalled round escalated to chair … escalation_chair_id=nova-sparrow`; `channel_conversation_chair_escalation_total{outcome=dispatched}` = **1**. The chair's forced turn was a **genuine synthesis** ("Wrapping up — three distinct risks on record: (1) Ember: regression… ") — the vote's content, not a bare sign-off. After a concurrence nudge, `iron-fox` concurred ("Agreed — those three are the right flags") → `interaction closed by end-of-interaction votes … trigger=end_votes votes=2`. The close was **announced** (not buried as "went idle").
- **Step 3 — recorded outcome.** `persatrix agent interactions nova-sparrow --limit 1` → "Conversation ended (2 turns)", governance `a9ecad15…`, summary records the resolution (the three risks + rollback validation by Thursday EOD); trigger renders as **"ended"** (structural), not *went idle*.

---

## MT-CHANNEL-GOV-003/004 Layer 1 — interaction cost ceiling (live, fail-closed)

Exercised as **MT-CHANNEL-CONFIG-003** (see below) and folded into the combined walkthrough's bounded-cost leg. With `interaction_budget_tokens=1500` set on `group:planning`, an open question drove the wallet to **deny both participants pre-call**: `wallet: lease denied — interaction cost ceiling exceeded` (`iron-fox` `estimated_tokens=6425`, `nova-sparrow` `estimated_tokens=6484`, both `> 1500`, `layer=cost`, `spent_tokens=0`). The ceiling is **enforced server-side and fail-closed** (denies before spend). ✅

---

## MT-INTERACTION-SUMMARY-001 — interaction-summary surface (live, Anthropic)

**Verdict: ✅ Pass (surface); Part A idle-trigger Accepted-with-known-gap.**

- **Part B — group close → summary.** Proven live **×3** — MT-CHANNEL-GOV-003 (relay decision), MT-CHANNEL-GOV-004 (chair synthesis), and the combined walkthrough (queue-first decision). Each surfaced a **readable, decision-naming** summary via `persatrix agent interactions <persona>` (and the same close affordance the web conversation view consumes), carrying the governance id and the **"Conversation ended"** (structural) trigger. None was blank; none was the `[interaction summary unavailable]` sentinel.
- **Part A — DM idle close (`close_reason=idle_gap`).** The idle close is **next-activity-evaluated** (no background sweeper — [`activity.go:82`](../../internal/channels/activity.go)) and the agent-side `IdleGapDetector` default window is **600s** ([`boundary_detectors.py`](../../agents/memory/boundary_detectors.py)), so the distinguishing `idle_gap` trigger needs a ~10-minute idle window that is impractical to exercise in a live session. The **summary surface itself** is proven by Part B (×3); the `idle_gap` boundary detection is pinned by the structural boundary-detector + episodic-lifecycle suites. **Accepted-with-known-gap** — follow-up: exercise the DM `idle_gap` close in a soak/long-idle run (see [Findings](#findings--follow-ups)).

---

## MT-CHANNEL-CONFIG-001…004 — RFC 0050 channel config (RC-tip re-run)

**Verdict: ✅ Pass (CLI/REST live on RC tip; web slices capability + surface re-confirmed; ran live in [#661](https://github.com/mkhomutov/Persatrix/pull/661)).**

- **CONFIG-001 (CLI live-edit).** `channel config get planning` → revision 0, all `[default]`. `channel config set planning interaction_idle_timeout_seconds=90 interaction_budget_tokens=8000` → revision 1, knobs `[channel]`, **values preserved** (the ISSUE-0103 fix: the first edit materializes the full effective config, no YAML-knob detachment). Read-back cross-checks **CLI == REST** (`GET …/config`): revision 1, `idle={value:90,source:channel}`, `budget={value:8000,source:channel}`. ✅
- **CONFIG-003 (interaction-budget server-side enforcement / Layer 1).** See [Layer 1](#mt-channel-gov-003004-layer-1--interaction-cost-ceiling-live-fail-closed) above — fail-closed, both participants denied pre-call at `budget=1500`. ✅
- **CONFIG-002 / 004 (web settings panel + member-threshold editor).** `/api/v1/ui/capabilities` → `channel_timeline.config_edit = {enabled:true, available:true}` (the `config_edit_enabled` flip, [#660](https://github.com/mkhomutov/Persatrix/pull/660)). The web panels are thin clients over `GET/PATCH …/config` and the member-config PATCH endpoint, both exercised via CLI/REST here (CONFIG-001) and run live end-to-end in the [RFC 0050 closeout #661](https://github.com/mkhomutov/Persatrix/pull/661). RC tip preserves the capability + surface. ✅

---

## Combined convergence walkthrough (authored + executed here)

**Channel**: `group:planning` (`interaction_budget_tokens=80000` set via the RFC 0050 CLI edit). One interaction (`2dbca2c6…`) exercising **all four signals together**. **Verdict: ✅ Captured.**

| Signal | Observed |
|--------|----------|
| **No pile-on (Tier B)** | Open question ("warehouse-direct vs queue-first for the metrics pipeline?") → only **`iron-fox`** replied (1 of 2 participants; `nova-sparrow` held back). `gated`: `when_mentioned`=2 (`ember-owl`), `low_salience below_threshold`=1. |
| **Bounded cost (Layer 1)** | Interaction total **17,412 tokens** (`iron-fox` 13,592 + `nova-sparrow` 3,820) — well **under the 80,000 ceiling** (the ceiling itself is fail-closed; see Layer 1 above). |
| **Ends on votes (Layer 4)** | Both converged queue-first → `interaction closed by end-of-interaction votes … 2dbca2c6 … trigger=end_votes votes=2`. **No** chair escalation (a clean vote close — no stall). |
| **Readable result (summary surface)** | `agent interactions` → "Conversation ended (3 turns)", governance `2dbca2c6…`, summary names the **queue-first** decision + the backpressure/replay reasoning. Not blank, not the sentinel. |

This is the artifact that gives the v0.3.8 headline its combined acceptance home (the master plan's `MT-CONVERSATION-CONVERGENCE-001` role): *give a group of personas a problem and the brainstorm converges, terminates, and produces a readable synthesized outcome.*

---

## Results Table

### v0.3.8 surface — new this release

| Test | Result | Notes |
|------|--------|-------|
| [MT-CHANNEL-RELEVANCE-002](MT-CHANNEL-RELEVANCE-002.md) | ✅ Pass | Small set on open floor; silence on redundant; NL bias; chair never closes; idle persona zero cost; bids leased + attributable. |
| [MT-CHANNEL-GOV-003](MT-CHANNEL-GOV-003.md) | ✅ Pass | Close on K=2 votes before depth cap; summary names decision; fresh interaction after close. |
| [MT-CHANNEL-GOV-004](MT-CHANNEL-GOV-004.md) | ✅ Pass | Zero-reply round → forced chair synthesis; quorum disposed; close announced; summary records the synthesis. |
| [MT-INTERACTION-SUMMARY-001](MT-INTERACTION-SUMMARY-001.md) | ✅ Pass (surface); Part A Accepted-with-known-gap | Summary surface proven live ×3 (Part B). DM `idle_gap` trigger needs 600s idle window — structurally pinned; follow-up bullet. |
| [MT-CHANNEL-CONFIG-001](MT-CHANNEL-CONFIG-001.md) | ✅ Pass | CLI live-edit + CLI/REST read-back; revision bump; ISSUE-0103 first-edit fix. |
| [MT-CHANNEL-CONFIG-002](MT-CHANNEL-CONFIG-002.md) | ✅ Pass | Web config-edit capability + REST surface re-confirmed; ran live in #661. |
| [MT-CHANNEL-CONFIG-003](MT-CHANNEL-CONFIG-003.md) | ✅ Pass | Interaction-budget fail-closed server-side (both participants denied pre-call at budget=1500). |
| [MT-CHANNEL-CONFIG-004](MT-CHANNEL-CONFIG-004.md) | ✅ Pass | Member-threshold editor capability + member-config PATCH surface re-confirmed; ran live in #661. |
| Combined convergence walkthrough | ✅ Captured | No-pile-on + bounded cost + ends-on-votes + readable summary, one channel. |

### Carried-forward v0.3.7 surface — regression (against the v0.3.8 RC tip)

| Gate | Result |
|------|--------|
| Tier A directedness / response-gate (`test_response_gate_relevance.py`, `test_response_gate_disposition.py`) | ✅ green |
| Alias cost-attribution + session/epoch isolation + cost-regression (idle/`observer` persona costs zero) | ✅ green (confirmed live: `ember-owl` zero LLM calls / zero cost) |
| Interaction lease threading + sub-agent lease | ✅ green |

---

## Findings & Follow-ups

- **F-1 — stale CLI note: `interaction_budget_tokens` "not yet enforced".** `persatrix channel config get` annotates an overridden `interaction_budget_tokens` row with `⚠ not yet enforced (RFC 0050 Open item 4)` ([`cli/src/commands/channel_config.rs:288-292`](../../cli/src/commands/channel_config.rs); the doc-comment also claims "it is not router-held"). But the [RFC 0050 interaction-budget-enforcement amendment](../rfcs/0050-amendment-interaction-budget-enforcement.md) shipped exactly that: [#657](https://github.com/mkhomutov/Persatrix/pull/657) made the knob router-held, [#658](https://github.com/mkhomutov/Persatrix/pull/658) added server-side enforcement ([`internal/wallet/interaction_budget.go`](../../internal/wallet/interaction_budget.go), `wallet.go` `interactionCeilingDenialLocked`). Enforcement is **live and fail-closed** — proven by MT-CHANNEL-CONFIG-003 (both participants denied pre-call). The operator-facing note now contradicts shipped behaviour (tells an operator an *enforced* ceiling is *not* enforced). **Non-blocking** (cosmetic/advisory only — enforcement works). **✅ Resolved in this PR (`b111985`)**: removed the `knob_note()` special-case (its only caller) + simplified `render_config_view`, and corrected the now-wrong "live resolution deferred / not router-held" doc-comments on `ConfigField`, `render_value`, and the YAML diff `Indeterminate`/`cell` paths (the GET handler now reports a concrete resolved budget, never null; the CLI keeps its `null`-rendering paths only as defensive tolerance). `cargo test --manifest-path cli/Cargo.toml` green (236 passed). **Go-side follow-on (same staleness):** the "not-yet-live" wording also survived in two Go comments the CLI fix missed — [`channel_config_handlers.go`](../../internal/server/channel_config_handlers.go) (falsely said `resolvedConfigBaseline` *omits* the budget, contradicting the green `TestChannelConfig_FirstEditFreezesDefaultsAsChannel`) and [`config_reconcile.go`](../../internal/channels/config_reconcile.go) `toConfigOverrides` (called it "(persisted-but-not-yet-live)"). Both now describe the budget as a router-held knob seeded like the rest; comment-only, behaviour already pinned green by existing tests.
- **Follow-up — MT-INTERACTION-SUMMARY-001 Part A (DM `idle_gap`).** The DM idle-close (`close_reason=idle_gap`) needs a ~600s agent-side idle window with no background sweeper, impractical in a live session. The summary surface is proven via Part B (×3) and the `idle_gap` detection is pinned structurally. Exercise the live DM idle-close in a soak / long-idle run; not a release gate.

---

## Notes

- All five headline MTs + the combined walkthrough ran **live on Anthropic** against the `8897727` RC tip; the structural release-blocker gates ran locally on the same tip.
- Between governance MTs the stack was reset (`make reset`) to a clean interaction baseline so each close arc (end-vote / chair-stall / combined) is unconfounded.
- The version bump (`build.version` still `0.3.7`) and the CHANGELOG date-stamp are [PR 3](../v0.3.8-release-prep-plan.md#pr-3--version-bump--changelog-curation); the two-store migration upgrade-on-open verification + Docker smoke are [PR 4](../v0.3.8-release-prep-plan.md#pr-4--final-pre-tag-verification--release-notes).

---

## Re-Execution — Release-Prep PR 4 (post-version-bump RC tip)

**Date**: 2026-06-16 · **Tester**: Claude (Opus 4.8) · **Tip**: `feature/v038-release-prep-final` off `main` `b47986f` — the post-version-bump release candidate (PR 3 [#665](https://github.com/mkhomutov/Persatrix/pull/665) version bump `0.3.7`→`0.3.8` + curated `[0.3.8]` changelog).

**Why a re-certification + carry-forward (not a full live re-run).** The delta over the PR 1 RC tip `8897727` (the tip the five headline MTs + the combined convergence walkthrough ran live on) is three PRs, **none changing the orchestrator/agent/web runtime the live MTs and the Docker smoke exercise**: [#663](https://github.com/mkhomutov/Persatrix/pull/663) (this MT report — plus the F-1 fix, a **CLI** stale-note removal and **comment-only** Go corrections in `channel_config_handlers.go` / `config_reconcile.go`, "behaviour already pinned green by existing tests"), [#664](https://github.com/mkhomutov/Persatrix/pull/664) (README/ROADMAP + guide verification + checklist — docs only), and [#665](https://github.com/mkhomutov/Persatrix/pull/665) (version strings `0.3.7`→`0.3.8` + the `[0.3.8]` changelog — no runtime code). So PR 4 follows the **v0.3.4…v0.3.7 PR 4 carry-forward precedent**: (a) re-run the full load-bearing automated gate sweep **live on host** on the post-bump tip (this host carries the full toolchain — Go 1.26.3 `darwin/arm64`, Node 22.16.0, Cargo 1.93.1, Python 3.12.13 venv, protoc, Docker 29.5.2), (b) carry the live Anthropic Docker smoke forward from the PR 1 live run on `8897727`. **No §4 live hard-block was outstanding** — the four convergence behaviours + the combined walkthrough already passed **live** on `8897727` (one carried `⚠️ Accepted-with-known-gap`: the DM `idle_gap` summary trigger, structurally pinned), so PR 4 is a re-certification of the gate sweep on the post-bump tip plus the released-status doc flips.

### Automated gates — re-ran green live on the post-bump tip `b47986f`

| Gate | Command | Result |
|------|---------|--------|
| Config validation | `make validate` (`agents/validate.py` + `prompt_refs.py`) | ✅ configs valid (**6 files**); **3** `instructions_file` refs resolve |
| Doc integrity | `scripts/checks/doc_audit.py` · `doc_links.py` | ✅ doc-audit **0 violations**; doc-links **8630 links / 410 files** clean |
| Sanitizer parity | `make generate-sanitizer-patterns-check` | ✅ Go↔Python in sync (11 patterns, 5 sources, 2 actions) |
| CLI build + clippy | `cargo build` · `cargo clippy --workspace --all-targets -- -D warnings` | ✅ `persatrix v0.3.8`; clippy clean |
| Rust CLI | `cargo test --workspace` | ✅ **236 passed**, 0 failed |
| **Go-only build (no JS toolchain)** | `make ui` then restore the committed placeholder embed · `go vet ./internal/...` | ✅ `make ui` rebuilt the bundle clean; the Go-only lane stays green against the committed placeholder embed; `go vet` clean |
| **Full Go internal suite (race/cover)** | `go test ./internal/... -race -cover` | ✅ **18 packages `ok`**, no FAIL, no data races — incl. governance Layers 1/2/4 + interaction-id producer + `interaction_convergence_test.go` + the channel-config store/REST suites (`TestChannelConfig_*`) |
| **Channel-store migration (v6→v8 release gate)** | `go test ./internal/channels/ -run 'Migration\|SchemaVersion\|Salience\|ChannelConfig'` | ✅ `TestSQLiteStore_Migration_V7ToV8_PreservesRows` (rows preserved across the ALTER) + `TestSQLiteStore_SchemaV8_Migration_Idempotent` (already-v8 no-op re-open) + the v6→v7 salience-`threshold` suite — all PASS |
| **Persona-memory migration (v14→v15 release gate)** | `pytest tests/unit/python/test_governance_id_migration.py` | ✅ **10 passed** — migration 15 registered, schema records v15, `governance_interaction_id` column present after initialize, double-apply no-op, direct-handler replay safe, empty-episodes guard, backfill-from-context, null-stays-null, store-field round-trip |
| **Web console JS unit (Vitest)** | `npm run test` (`web/`) | ✅ **291 passed** across 28 files |
| Full Python unit suite | `make test-python` (`pytest tests/unit/python`) | ✅ **3420 passed, 1 skipped** (incl. the salience-bid + response-gate suites) |
| Ruff + mypy | `make lint-python` | ✅ ruff all-pass; mypy no issues in **230 source + 318 test files** |
| Python licenses | `make check-licenses-python` (allow-list) | ✅ **68 packages**, all in allow-list |
| Rust licenses | `cargo deny check licenses` | ✅ `licenses ok` |

**Two-store upgrade-on-open (release gate) — ✅ verified via the migration suites re-run live.** Both forward-migration paths are exercised by the suites above. **Channel store** (`channels.db`): a built pre-v8 store forward-migrates cleanly to **v8** — the Tier B salience `threshold` column (v7) and the RFC 0050 `config_overrides_json` / `config_revision` (backfilled to 0) / reserved `config_change_lineage` columns (v8) are present, seeded rows survive the ALTER intact, and a second open of an already-v8 store is a no-op (`TestSQLiteStore_Migration_V7ToV8_PreservesRows` + `..._SchemaV8_Migration_Idempotent` + the v6→v7 salience suite). An existing `config/channels.yaml` seeds the router unchanged (the `internal/channels` reconcile suite is green). **Persona-memory** (`agents/memory`): a built pre-v15 DB forward-migrates cleanly to **v15** — `episodes.governance_interaction_id` present and backfilled from the ISSUE-0102 context blob via `json_extract`, a row without a governance id stays NULL, and an already-v15 DB is a no-op open (the 10 `test_governance_id_migration.py` cases, incl. crash-replay safety). **Both forward-only — no rollback path.** The **in-image boot** migration (both stores upgrade on first open in the container) carries forward from the PR 1 live Docker run on `8897727`; [Phase 4](../v0.3.8-plan.md#phase-4--tag-and-post-release-follow-up) re-runs it on the exact tag tip.

**Proto staleness — committed stubs current; local regen is a toolchain-stamp-only diff.** Unlike v0.3.7, v0.3.8 **did** change the wire (`proto/task.proto` + `proto/wallet.proto` — the governance/interaction surface); the generated stubs were regenerated and committed with those features. A local `make proto` on this host produces a diff **only** in the generator version comment (`protoc v7.34.1` → `v7.35.0` — the host's brew `protoc` is ahead of the CI-pinned 34.1, and `protoc-gen-mypy` is absent, so `proto-python` can't even complete locally), so the regen was reverted; CI re-confirms `make proto && git diff --exit-code` clean on the pinned toolchain.

**`make notices` — delta-free by construction; CI-canonical file stands.** v0.3.8 adds **no new third-party production runtime dependency** (orchestrator-/agent-/web-side behaviour: response-gate admission + the leased salience bid, wallet/orchestrator governance enforcement, the interaction-id producer, a persona-memory column, channel-store columns + the web settings panel) beyond the Dependabot/RUSTSEC hygiene bumps already in the window ([#644](https://github.com/mkhomutov/Persatrix/pull/644), [#648](https://github.com/mkhomutov/Persatrix/pull/648), [#651](https://github.com/mkhomutov/Persatrix/pull/651)). The regen tooling (`go-licenses` + `cargo-license`) is not installed on this host; the CI-canonical committed `THIRD_PARTY_NOTICES.md` stands and CI re-confirms.

**Carried by CI** on the post-bump tip: `make check-licenses-go` (`go-licenses` against the Go module graph), `make proto` + `git diff --exit-code` staleness on the pinned toolchain, `golangci-lint`, and the full Python **integration** suite (incl. the session/epoch isolation gates). The local runs above re-certify the load-bearing gates live on the post-bump tip; they do not substitute for CI.

### Live Docker provider smoke — carried forward from the PR 1 live run (`8897727`)

The four required legs ([open question → small relevant set (no pile-on, Tier B) + end-vote close with a surfaced summary (Layer 4 + the summary surface)](#structural--automated-gates-live-on-the-rc-tip-8897727); a [live channel-config edit from the web console](#mt-channel-config-001004--rfc-0050-channel-config-rc-tip-re-run) persisting + reading back across CLI + web; a DM-turn no-regression leg; both stores opening / migrating in-image at boot) were verified **live on Anthropic** on the RC tip `8897727` (see the MT sections above). The post-bump delta ([#663](https://github.com/mkhomutov/Persatrix/pull/663) the F-1 CLI stale-note removal + comment-only Go corrections / [#664](https://github.com/mkhomutov/Persatrix/pull/664) docs / [#665](https://github.com/mkhomutov/Persatrix/pull/665) version strings) changes none of the four legs' runtime, so those live results carry forward unchanged; the [Phase 4](../v0.3.8-plan.md#phase-4--tag-and-post-release-follow-up) pre-tag step re-runs the in-image Docker smoke on the exact tag tip.

**PR 4 re-execution result: ✅ release gate holds on the post-bump tip — zero feature Fail, no outstanding live hard-block.** The full automated sweep re-ran **green live on host** (Go race/cover 18 pkgs no-races incl. the governance / interaction-id / convergence / channel-config suites; both migration release gates — channel store v6→v8, persona-memory v14→v15; Vitest 291; Rust 236; the Python unit suite 3420 passed / 1 skipped; ruff + mypy + clippy + go vet clean; Python + Rust licenses ok); the live Anthropic Docker smoke carries forward from the `8897727` PR 1 run. The working tree is clean after the sweep (the local proto regen and the `make ui` bundle were reverted to the committed state). The only remaining Phase 4 steps are the mechanical `v0.3.8` tag + GitHub Release per [release-prep plan Phase 4](../v0.3.8-plan.md#phase-4--tag-and-post-release-follow-up).
