# v0.3.4 Release Preparation Plan

**Status**: ⬜ Not started — four release-prep PRs scoped below; this document (PR 0) opens the sequence
**Target version**: v0.3.4 (Any Model, Any Provider — RFC 0033 Phases 1–2 + the already-merged offline / Ollama provider work)
**Created**: 2026-05-26
**Branch prefix**: `feature/v034-release-prep-`
**Target**: `main`
**Merge strategy**: Squash merge per [BRANCHING.md](BRANCHING.md)
**Master plan**: [v0.3.4-plan.md Phase 3 + Phase 4](v0.3.4-plan.md#phase-3--v034-release-prep-plan)
**Amendment carried**: [v0.3.4-plan-amendment-2026-05-24.md](v0.3.4-plan-amendment-2026-05-24.md) — the provider-parity [Phase 4 additions](v0.3.4-plan-amendment-2026-05-24.md#phase-4-additions) (provider-swap MT, provider-neutral onboarding, `make demo-openai`) fold into the PRs below.

**Scope**: MT execution report for the v0.3.4 surface — the new alias-routing and provider-swap MTs plus the offline / Ollama runs (**authored *and* executed in PR 1**, per [v0.3.4-plan Phase 4 PR 1](v0.3.4-plan.md#phase-4--v034-release-prep-execution)), on the 33-row v0.3.3 carry-forward; documentation refresh (README / ROADMAP, a model-alias/provider guide, provider-neutral onboarding — `.env.example` / compose guard / `make demo-openai` — and the release checklist); version bump `0.3.3` → `0.3.4`; changelog curation (the `[Unreleased]` offline / Ollama entries fold into `[0.3.4]`); final pre-tag verification.

**Out of scope**: new feature work. The single v0.3.4 RFC workstream is fully landed (RFC 0033 `⚠️ Partially Implemented` Phases 1–2) with no outstanding PRs. Deferred per [v0.3.4-plan §Out of scope](v0.3.4-plan.md) — documented as Known Gaps:

- **RFC 0033 Phase 3** (raw-ID pass-through removal, `_infer_provider` retirement, schema `"0.3"`) → v0.3.5+, **gated on the `persatrix.llm.alias.raw_id_usage` counter ([#432](https://github.com/mkhomutov/Persatrix/pull/432)) reading zero** — observed traffic, not a date.
- **RFC 0031 Phases 2–4** (recall filtering, `persatrix session …` CLI — the F-3 fix, [ISSUE-0051](issues/ISSUE-0051-per-session-memory-namespacing-channels.md)) → v0.3.5; drafted in [`0031-phase2-pr-plan.md`](rfcs/0031-phase2-pr-plan.md). **RFC 0030 Phase 1 / 0034 Phases 2–3 / 0039** — not on this contract.
- **RFC 0045 / 0046 / 0047** ([#427](https://github.com/mkhomutov/Persatrix/pull/427)–[#429](https://github.com/mkhomutov/Persatrix/pull/429)) — open-core library-extraction drafts, all `🔨 Draft`; RFC-tracking only.

**Documentation timing policy** (carry-forward from [v0.3.3 prep](v0.3.3-release-prep-plan.md)): public docs updated *before* tag; post-tag-only artifacts (tag link, release URLs, "Released" stamps) → [v0.3.4-plan Phase 5](v0.3.4-plan.md#phase-5--tag-and-post-release-follow-up).

---

## Table of Contents

- [Progress Overview](#progress-overview)
- [Dependency Graph](#dependency-graph)
- [Current state (baseline, 2026-05-26)](#current-state-baseline-2026-05-26)
- [Track A — Documentation + Manual Tests](#track-a--documentation--manual-tests)
- [Track B — Release Engineering](#track-b--release-engineering)
- [Status hygiene](#status-hygiene)
- [Related documentation](#related-documentation)

---

## Progress Overview

Update this table as each PR merges. Flip **Status** and add GitHub PR number + merge date. This document is the single source of truth for v0.3.4 release-prep PR sequencing and is the concrete sub-PR breakdown for [v0.3.4-plan Master Progress Overview rows 3–4](v0.3.4-plan.md#master-progress-overview).

| PR | Track | Title | Branch | Status | GitHub PR | Merged |
|----|-------|-------|--------|--------|-----------|--------|
| 0 | — | v0.3.4 release-prep plan (this document) | `feature/v034-release-prep-plan` | 🔀 PR open | — | — |
| 1 | A | MT execution report (alias routing + provider-swap + offline + Ollama) | `feature/v034-release-prep-mt-exec` | ⬜ Not started | — | — |
| 2 | A | README + ROADMAP + model-alias/provider guide + provider-neutral onboarding (`.env` / compose / `demo-openai`) + release checklist | `feature/v034-release-prep-docs` | ⬜ Not started | — | — |
| 3 | B | Version bump (`agents/pyproject.toml`, `agents/observability/{metrics,tracing}.py`, `cli/Cargo.toml`, `cli/Cargo.lock`) + changelog `[0.3.4]` | `feature/v034-release-prep-version-bump` | ⬜ Not started | — | — |
| 4 | B | Final pre-tag verification & release notes | `feature/v034-release-prep-final` | ⬜ Not started | — | — |

**Status legend**: ⬜ Not started · 🔄 In progress · 🔀 PR open · ✅ Merged

---

## Dependency Graph

```
PR 0 (this plan) ──► PR 1 (MT execution report — authors + executes the new MTs) ──► PR 2 (docs + onboarding + checklist)
                                                                                            │
                                                                                            ▼
                                                                          PR 3 (version bump + changelog) ──► PR 4 (final)
```

Hard dependencies: **PR 1** waits only on PR 0 — the RFC 0033 workstream is fully merged ([#431](https://github.com/mkhomutov/Persatrix/pull/431)–[#437](https://github.com/mkhomutov/Persatrix/pull/437)), so `main` is a usable RC tip. **PR 2** waits on PR 1 (the checklist references the execution-report path; ROADMAP Known Gaps and the provider guide's swap example pull from PR 1's results). **PR 3** → PR 2; **PR 4** → PR 3.

**Difference from the v0.3.3 prep**: there the headline MT was authored during implementation; here v0.3.4's new MTs were deliberately **not** ([RFC 0033 PR plan §PR 6](rfcs/0033-pr-plan.md) defers the alias-routing MT), so **PR 1 both authors and executes them**. The alias cost-attribution gate ([`cost_alias_gate_test.go`](../internal/server/cost_alias_gate_test.go) + [`cost_alias_pricing_test.go`](../internal/cost/cost_alias_pricing_test.go)) is already green on `main` ([#435](https://github.com/mkhomutov/Persatrix/pull/435)), so PR 1's cost capture is a confirmatory live run, not a first execution.

---

## Current state (baseline, 2026-05-26)

### RFC PR roll-up

The single v0.3.4 workstream is complete, plus the two already-merged provider modes the release ships:

| RFC / work | Status | PR cluster |
|------------|--------|------------|
| [0033](rfcs/0033-model-alias-layer.md) | ⚠️ Partially Implemented (Phases 1–2; Phase 3 → v0.3.5+, observed-traffic gated) | Plan [#430](https://github.com/mkhomutov/Persatrix/pull/430); PRs 1–7 [#431](https://github.com/mkhomutov/Persatrix/pull/431) (resolver + `models.aliases` block + OpenAI peer alias), [#432](https://github.com/mkhomutov/Persatrix/pull/432) (factory tuple + raw-ID warning/counter), [#433](https://github.com/mkhomutov/Persatrix/pull/433) (config migration — Sonnet 4→4.6 via one `quality` edit), [#434](https://github.com/mkhomutov/Persatrix/pull/434) (missing-price guard), [#435](https://github.com/mkhomutov/Persatrix/pull/435) (`model_alias` span + derived pricing + cost gate), [#436](https://github.com/mkhomutov/Persatrix/pull/436) (doc sweep), [#437](https://github.com/mkhomutov/Persatrix/pull/437) (closeout). Plan: [`0033-pr-plan.md`](rfcs/0033-pr-plan.md). |
| Offline `MockProvider` | Merged, parked in [`CHANGELOG.md` §Unreleased](../CHANGELOG.md) | [#422](https://github.com/mkhomutov/Persatrix/pull/422) — `PERSATRIX_OFFLINE=1` / `provider: mock` + `make demo-offline`. |
| Ollama local-model provider | Merged, parked in [`CHANGELOG.md` §Unreleased](../CHANGELOG.md) | [#423](https://github.com/mkhomutov/Persatrix/pull/423) — `PERSATRIX_OLLAMA=1` / `provider: ollama` (thin `OpenAIProvider` subclass) + `make demo-ollama`. |

**No tracked-issue bug-fix PRs shipped in `v0.3.3..HEAD`** — every commit is an RFC 0033 PR, a provider-mode feature, or a docs/plan/RFC-draft PR. The `[0.3.4]` **Bug Fixes** section is empty; PR 3 omits it (as v0.3.3). The three RFC 0033 review findings deferred to issues ([ISSUE-0069](issues/ISSUE-0069-task-agent-evaluator-routing-defaults-unconsumed.md) / [ISSUE-0070](issues/ISSUE-0070-sub-agent-request-post-init-systemexit-construction.md) / [ISSUE-0072](issues/ISSUE-0072-memory-compression-hardcoded-model-literals.md)) are **deferred, not fixed** — Known Gaps, not Bug Fixes.

### Version state

Both shipped components are at `0.3.3` — bump to `0.3.4`:

| Component | File | Current |
|-----------|------|---------|
| Python agents | `agents/pyproject.toml` | `0.3.3` |
| Python observability defaults | `agents/observability/{metrics,tracing}.py` (`_DEFAULT_SERVICE_VERSION`) | `0.3.3` |
| Rust CLI | `cli/Cargo.toml` | `0.3.3` |
| Go orchestrator | git tag | `v0.3.3` |

### Documentation state

All of the following land in PR 2.

- **README.md** — the **"Try it free" (offline) and "Run a real local model — Ollama" sections already exist** ([#422](https://github.com/mkhomutov/Persatrix/pull/422) / [#423](https://github.com/mkhomutov/Persatrix/pull/423)); PR 2 verifies them (no re-author), adds a v0.3.4 [Roadmap table](../README.md#roadmap) row, and runs the provider-neutral onboarding pass ([amendment item 4](v0.3.4-plan-amendment-2026-05-24.md#phase-4-additions)) — a vendor-neutral "an LLM provider" quick-start opener. The [§Cost Warning](../README.md#-cost-warning) "pauses itself at the cap" claim is reconciled with the local-pricing decision ([§Known follow-up §2](#known-follow-up-issues)).
- **ROADMAP.md** — [Version Map](../ROADMAP.md#version-map) v0.3.4 `🚧 Planning` → PR 2 `🚧 Release prep` → PR 4 `✅ Released`. The [RFC Master Index](../ROADMAP.md#rfc-master-index) 0033 row is **already current** (`⚠️ Partially Implemented (Phases 1–2)`) — verify, do not re-edit. Trim Current phase/milestone prose; refresh `Last updated`.
- **Provider-neutral onboarding** ([amendment item 4](v0.3.4-plan-amendment-2026-05-24.md#phase-4-additions)) — `.env.example` (today `ANTHROPIC_API_KEY` *Required* / `OPENAI_API_KEY` *Optional*) reframed as "pick one"; the `docker-compose.yaml` startup guard (today names only `ANTHROPIC_API_KEY`) generalized to accept any configured provider and name them all; **`make demo-openai` added** (`demo-offline` / `demo-ollama` already exist — [Makefile:248](../Makefile)/[:263](../Makefile), `demo-openai` does not); the [`v0.3.0-demo.md`](guides/v0.3.0-demo.md) opener reframed vendor-neutral too.
- **Model-alias / provider guide** — **no dedicated provider guide exists** (`docs/guides/` has `channels.md`, `persona-agents.md`, `v0.3.0-demo.md`, `version-bump.md`). PR 2 authors `docs/guides/model-providers.md` (aliases → `{provider, model_id, pricing}`; the four provider classes as peers; the one-line swap; the missing-price guard's local-vs-unpriced distinction; the `model_alias` rollup) and cross-links the alias-swept [`persona-agents.md`](guides/persona-agents.md) `model:` examples.
- **observability.md** — `persatrix.llm.model_alias` landed during implementation ([#435](https://github.com/mkhomutov/Persatrix/pull/435)); PR 2 **verifies** it (no second authoring pass — the main scope reduction, mirroring v0.3.3).
- **docs/v0.3.4-release-checklist.md** — created from the [v0.3.3 checklist](v0.3.3-release-checklist.md) (§1 Pre-release Verification, §2 Version Alignment, §3 Changelog + §3.1 Upgrade Notes, §4 Manual Test Sign-off, §5 Tag/Release, §6 Known Gaps, §7 Summary).

### Manual test state

The new v0.3.4 MTs are **authored *and* executed in PR 1** (intentionally not authored during implementation — [RFC 0033 PR plan §PR 6](rfcs/0033-pr-plan.md) defers the alias-routing MT here). Proposed IDs (PR 1 finalizes naming):

- **`MT-ALIAS-001`** — *alias routing, live* (**primary v0.3.4 gate**). A `quality`-aliased agent resolves to physical `claude-sonnet-4-6`, completes a turn, and reports correctly-keyed **non-zero** cost via `GET /api/v1/cost/summary`; the `agent.llm.call` span carries `persatrix.llm.model_alias=quality` while `gen_ai.request.model` stays the physical ID ([RFC 0033 §G](rfcs/0033-model-alias-layer.md#g-telemetry)) — the [master-plan §Acceptance](v0.3.4-plan.md#acceptance-for-v034) cost gate.
- **`MT-ALIAS-002`** — *one-line provider swap* (**headline-claim test**, [amendment item 3](v0.3.4-plan-amendment-2026-05-24.md#phase-4-additions)). Flip `quality.provider` (Anthropic → OpenAI / → Ollama), run the same agent unchanged, confirm it routes there with correct non-zero (or documented-$0 local) cost — validating *"a provider swap is a one-line edit."*
- **`MT-OFFLINE-001`** — *offline, $0* ([#422](https://github.com/mkhomutov/Persatrix/pull/422)). `PERSATRIX_OFFLINE=1` / `make demo-offline`: full round-trip, zero provider calls / network / spend, populated `gen_ai.*` spans + the RFC 0023 lease settling at `$0`.
- **`MT-OLLAMA-001`** — *Ollama local model* ([#423](https://github.com/mkhomutov/Persatrix/pull/423)). `PERSATRIX_OLLAMA=1` / `make demo-ollama`: real token counts, `$0` cloud spend (offline wins if both flags set).

The full **33-row v0.3.3 surface carries forward** (the `MT-IDLE-001` gate + the 32-row v0.3.2 surface), regression-checked against the alias-routed config. **Total: 33 carried + 4 new = 37 rows** (PR 1 confirms once authored).

The alias **cost-attribution gate** ([`cost_alias_gate_test.go`](../internal/server/cost_alias_gate_test.go) — alias-routed agent → correctly-keyed non-zero cost; [`cost_alias_pricing_test.go`](../internal/cost/cost_alias_pricing_test.go) — derived-pricing drift guard) is the **automated release-blocker** counterpart to `MT-ALIAS-001`, as `cost-regression-gate` was to `MT-IDLE-001`. The offline/Ollama factory-interplay regression ([`test_llm_offline.py`](../tests/unit/python/test_llm_offline.py) / [`test_llm_ollama.py`](../tests/unit/python/test_llm_ollama.py)) keeps [#422](https://github.com/mkhomutov/Persatrix/pull/422) / [#423](https://github.com/mkhomutov/Persatrix/pull/423) green after the factory rewrite.

Carry-forward notes: `MT-CHANNEL-004` prose was de-staled to `quality` in the [#436](https://github.com/mkhomutov/Persatrix/pull/436) doc sweep — re-run on the alias config. `MT-MEMORY-003` keeps its raw `claude-haiku-4-5` references (memory-compression is **not** aliased — [ISSUE-0072](issues/ISSUE-0072-memory-compression-hardcoded-model-literals.md), still on the §E path). `MT-MEMORY-005` ([dementia-test gate](manual-tests/MT-MEMORY-005-dementia-test.md)) is **not re-run** — no recall-tier change; prior result carries forward.

### Changelog state

Unlike v0.3.3, `CHANGELOG.md` **has an `[Unreleased]` section** ([CHANGELOG.md:5](../CHANGELOG.md)) carrying curated 🚀 Features (offline + Ollama) and 📝 Config / Schema entries from [#422](https://github.com/mkhomutov/Persatrix/pull/422) / [#423](https://github.com/mkhomutov/Persatrix/pull/423). PR 3 **folds that curated content into `[0.3.4]` rather than re-authoring it** ([master-plan Phase 3](v0.3.4-plan.md#phase-3--v034-release-prep-plan)), then generates the RFC 0033 + plan rows over `v0.3.3..HEAD` and reshapes into Highlights / Features / Documentation / Testing / Upgrade Notes (no Bug Fixes). `[0.3.3]` and prior preserved verbatim. The RFC 0045–0047 drafts ([#427](https://github.com/mkhomutov/Persatrix/pull/427)–[#429](https://github.com/mkhomutov/Persatrix/pull/429), `🔨 Draft`) stay on an RFC-tracking line under Documentation.

### Dependency notices state

RFC 0033 (pure-Python resolver/config), the in-process offline `MockProvider`, and the Ollama provider (a thin `OpenAIProvider` subclass reusing the OpenAI SDK already in the tree) add **no new third-party production dependency**. PR 4 runs `make notices` — expected empty save for any Dependabot bump in the window.

### Known follow-up issues

All Known Gaps for the release notes, not release gates:

1. **Phase 3 is observed-traffic gated, not dropped.** The §E raw-ID fall-through still works but fires a deprecation warning + the `persatrix.llm.alias.raw_id_usage` counter ([#432](https://github.com/mkhomutov/Persatrix/pull/432)). Phase 3 (raw-ID rejection, `_infer_provider` retirement, schema `"0.3"`) opens only when that counter reads zero across dogfood — **observed state, not a date** ([RFC 0033 §Phased Implementation Plan](rfcs/0033-model-alias-layer.md#phased-implementation-plan)).
2. **Local-pricing vs. the README wallet-cap claim ([amendment item 6](v0.3.4-plan-amendment-2026-05-24.md#what-changes)).** A genuinely-$0 local alias never trips the simulated wallet, contradicting the README's *"pauses itself at the cap."* The local-pricing decision ([#431](https://github.com/mkhomutov/Persatrix/pull/431)) + missing-price guard ([#434](https://github.com/mkhomutov/Persatrix/pull/434)) tell `$0`-on-purpose from forgot-the-price; PR 2 reconciles the README wording (pin the cap demo to a priced alias, or document the `$0`-local case), informed by PR 1's offline/Ollama evidence.
3. **Three model-identity literals remain on the §E raw-ID path ([ISSUE-0072](issues/ISSUE-0072-memory-compression-hardcoded-model-literals.md)).** The memory-compression surfaces still hardcode raw `claude-haiku-4-5*` defaults; correctly **not** swept (`MT-MEMORY-003` keeps its references) — a fold-in candidate for the RFC 0023 un-leased-origin migration ([ISSUE-0063](issues/ISSUE-0063-workflow-step-unleased-llm-spend-uncounted.md)). Not a regression.
4. **Two RFC 0033 PR-3 review findings.** [ISSUE-0069](issues/ISSUE-0069-task-agent-evaluator-routing-defaults-unconsumed.md) (`task_agents`/`evaluators` routing defaults surfaced but unconsumed) and [ISSUE-0070](issues/ISSUE-0070-sub-agent-request-post-init-systemexit-construction.md) (`SubAgentRequest.__post_init__` `SystemExit`, latent while `SPAWN_SUB_AGENT` is a stub). Pre-existing/latent; neither affects the release surface.

Any `Accepted-with-known-gap` MT outcome in PR 1 must cite a tracked issue or an RFC-closeout / sequencing follow-up bullet.

---

## Track A — Documentation + Manual Tests

### PR 1 — Manual Test Execution Report (v0.3.4 surface)

**Branch**: `feature/v034-release-prep-mt-exec` · **Opens**: once PR 0 merges (RFC 0033 fully merged — no closeout dependency).

Scope:

- **Author** the four new MTs (`MT-ALIAS-001` / `MT-ALIAS-002` / `MT-OFFLINE-001` / `MT-OLLAMA-001`, per [§Manual test state](#manual-test-state)) and add them to the [`manual-tests/README.md`](manual-tests/README.md) index.
- Create `docs/manual-tests/v0.3.4-execution-report.md` from the [v0.3.3 template](manual-tests/v0.3.3-execution-report.md); **execute all 37 rows** (33 carried-forward + 4 new) on a clean checkout against the `main` RC tip with date / tester / environment / commands / evidence.
- Record the alias cost-attribution gate ([`cost_alias_gate_test.go`](../internal/server/cost_alias_gate_test.go) / [`cost_alias_pricing_test.go`](../internal/cost/cost_alias_pricing_test.go)) result alongside `MT-ALIAS-001`; re-run the carried-forward v0.3.3 surface against the alias-routed config (`MT-IDLE-001` + `cost-regression-gate` stay green; `MT-CHANNEL-004` on the de-staled `quality` prose; `MT-MEMORY-005`/`MT-MEMORY-003` per [§Manual test state](#manual-test-state)).

Acceptance:

- Report `✅ Complete`; every row `Pass` / `Accepted-with-known-gap` / `Deprecated` — zero `Fail`, zero `Pending`.
- `MT-ALIAS-001` `Pass` (non-zero correctly-keyed `/cost/summary` + `model_alias` span evidence); `MT-ALIAS-002` `Pass` (one-line swap re-routes the same agent); `MT-OFFLINE-001` / `MT-OLLAMA-001` `Pass`.
- Alias cost-attribution gate + offline/Ollama factory-interplay regression captured green on the RC tip.
- Every `Accepted-with-known-gap` row links a tracked issue or follow-up bullet.

---

### PR 2 — README + ROADMAP + Model-Alias/Provider Guide + Provider-Neutral Onboarding + Release Checklist

**Branch**: `feature/v034-release-prep-docs`

Scope: the doc surfaces enumerated in [§Documentation state](#documentation-state) — README (v0.3.4 Roadmap row + vendor-neutral opener + cost-warning reconciliation), ROADMAP (Version Map → `🚧 Release prep`, prose trim, `Last updated`; 0033 row verified-not-edited), provider-neutral onboarding (`.env.example` "pick one", any-provider compose guard, `make demo-openai`, `v0.3.0-demo.md` opener), the new `docs/guides/model-providers.md`, observability.md verification, and the new `docs/v0.3.4-release-checklist.md`. No other doc edits.

Acceptance:

- `make validate` + doc-link integrity (`doc_links.py`) clean against the new MT files (landed by PR 1) + the new provider guide.
- ROADMAP Version Map v0.3.4 at `🚧 Release prep`; README Roadmap row added; cost-warning wording reconciled ([§Known follow-up §2](#known-follow-up-issues)).
- **Provider-neutral onboarding verified** — `.env.example` "pick one"; compose guard accepts any provider and names them in its error; `make demo-openai` runs end-to-end alongside `demo-offline` / `demo-ollama`; README + `v0.3.0-demo.md` openers vendor-neutral.
- `docs/guides/model-providers.md` treats Anthropic / OpenAI / Ollama / offline as peers; swap section cites the `MT-ALIAS-002` result. `observability.md` `model_alias` verified (no re-author).
- Release checklist lists every pre-tag gate — `make notices`, the alias cost-attribution gate, the carried-forward `cost-regression-gate`, and a Docker smoke (alias-routed agent + one-line swap + offline + Ollama).

---

## Track B — Release Engineering

### PR 3 — Version Bump + Changelog Curation

**Branch**: `feature/v034-release-prep-version-bump`

Scope:

- Bump `agents/pyproject.toml`, `agents/observability/{metrics,tracing}.py` (`_DEFAULT_SERVICE_VERSION`), `cli/Cargo.toml` to `0.3.4` via `scripts/bump_version.py`; then `cd cli && cargo update --workspace` to regenerate `cli/Cargo.lock`. Run `make all` at the new version.
- **Fold the `[Unreleased]` offline / Ollama curated content into `[0.3.4]`** (do not re-author); generate the rest via `git-cliff --tag v0.3.4 --unreleased --prepend CHANGELOG.md` over `v0.3.3..HEAD`; reshape into Highlights / Features / Documentation / Testing / Upgrade Notes (no Bug Fixes); PR ranges per [§RFC PR roll-up](#rfc-pr-roll-up). Preserve `[0.3.3]` and prior verbatim (temp-file-and-merge if `git-cliff` would overwrite curated content, as in v0.3.3 / v0.3.2). The RFC 0045–0047 drafts stay on an RFC-tracking line under **Documentation**, not Features.
- **Upgrade Notes** — canonical text in the release checklist §3.1 (PR 2), copied here; section covers: alias-routed defaults (`quality`/`fast`/`summarizer` → `{provider, model_id, pricing}`); new `models.aliases` block + `schema_version` `0.1`→`0.2` with a priced OpenAI peer alias (`agents.yaml` `model:` / routing defaults / `summarization.model` all aliased); raw-ID back-compat (warn + `alias.raw_id_usage` counter, Phase 3 rejection v0.3.5+); missing-price guard (fails closed for non-local); `cost.pricing.models` derived + `persatrix.llm.model_alias` span; offline / Ollama providers + `provider_config.base_url` schema.

Acceptance:

- `make all` builds at `0.3.4`.
- `CHANGELOG.md` has a curated `[0.3.4]` section with the Upgrade Notes above; the `[Unreleased]` offline / Ollama content is **folded in, not duplicated** (the `[Unreleased]` header is removed once its content lands in `[0.3.4]`).
- All prior version sections untouched (`git diff` below the new section shows zero diff).

---

### PR 4 — Final Pre-Tag Verification & Release Notes

**Branch**: `feature/v034-release-prep-final`

Scope:

- Full release-checklist sweep on a clean checkout: `make test`, `make lint`, `make validate`, `make proto && git diff --exit-code`, `make check-licenses`, `make notices` (commit any delta), `make generate-sanitizer-patterns-check`.
- Confirm the **alias cost-attribution gate** ([`cost_alias_gate_test.go`](../internal/server/cost_alias_gate_test.go) / [`cost_alias_pricing_test.go`](../internal/cost/cost_alias_pricing_test.go)) and the **bored-persona `cost-regression-gate`** (carry-forward) are green on the post-version-bump RC tip.
- Re-run the personal-tier latency regression gate ([`tests/perf/personal_tier_latency.py`](../tests/perf/personal_tier_latency.py)) on the RC tip (carry-forward; informational baseline).
- Docker smoke test (`make docker-build && make docker-up`) confirming: (a) an alias-routed `quality` agent completes a turn with correct **non-zero** `/cost/summary` + a `model_alias` span attribute; (b) a one-line `quality.provider` swap re-routes the same agent (the headline claim, live); (c) `make demo-offline` round-trips at `$0` with zero provider calls; (d) `make demo-ollama` routes to a local model with real tokens and `$0` cloud. (`make demo-openai` is exercised here if an OpenAI key is available; otherwise leg (b) covers cloud-peer parity.)
- Flip the ROADMAP Version Map v0.3.4 row to `✅ Released`. Update this prep-plan status to `✅ Complete`; update the release-checklist `Status` to `✅ Released`.

Tag procedure (executed after PR 4 merges, by [v0.3.4-plan Phase 5](v0.3.4-plan.md#phase-5--tag-and-post-release-follow-up)):

```bash
git checkout main
git pull --ff-only origin main
git tag -a v0.3.4 -m "v0.3.4 — Any Model, Any Provider"
git push origin v0.3.4
```

GitHub Release follows the v0.3.3 pattern: curated `[0.3.4]` changelog as the body; append Upgrade Notes (§3.1) and Known Gaps (§6 — drawn from the header **Out of scope** list and [§Known follow-up issues](#known-follow-up-issues), plus carried-forward v0.3.3 / v0.3.2 gaps).

---

## Status hygiene

Single source of truth for v0.3.4 release-prep PR sequencing — update the **Progress Overview** on every merge. Per [v0.3.4-plan §ROADMAP hygiene](v0.3.4-plan.md#roadmap-hygiene):

- **PR 0 merges** → [v0.3.4-plan Master Progress Overview](v0.3.4-plan.md#master-progress-overview) row 3 → `✅ Merged`; row 4 `⬜` until PR 1 opens.
- **PR 1 opens** → row 4 → `🔄 In progress`. **PR 2 merges** → ROADMAP Version Map v0.3.4 → `🚧 Release prep`; `Last updated` refreshed.
- **PR 4 merges** → row 4 → `✅ Merged`; Version Map v0.3.4 → `✅ Released`; this document → `✅ Complete`.

The [RFC Master Index](../ROADMAP.md#rfc-master-index) 0033 row is **already** `⚠️ Partially Implemented (Phases 1–2)` from the closeout ([#437](https://github.com/mkhomutov/Persatrix/pull/437)) — release-prep does not re-touch it.

---

## Related documentation

- [v0.3.4-plan.md](v0.3.4-plan.md) — master plan; Phase 3 authors this document, Phase 4 executes its PRs (rows 3–4). [v0.3.4-plan-amendment-2026-05-24.md](v0.3.4-plan-amendment-2026-05-24.md) — provider-parity hardening; its [Phase 4 additions](v0.3.4-plan-amendment-2026-05-24.md#phase-4-additions) widen PR 1 / PR 2 scope.
- [v0.3.3-release-prep-plan.md](v0.3.3-release-prep-plan.md) / [v0.3.3-release-checklist.md](v0.3.3-release-checklist.md) — structural templates (this plan; the checklist PR 2 creates). [BRANCHING.md](BRANCHING.md) — branch naming, merge strategy.
- v0.3.4 RFC: [0033](rfcs/0033-model-alias-layer.md) (PR plan [`0033-pr-plan.md`](rfcs/0033-pr-plan.md)). Provider work shipping here: offline ([#422](https://github.com/mkhomutov/Persatrix/pull/422)), Ollama ([#423](https://github.com/mkhomutov/Persatrix/pull/423)).
- Cost/telemetry surfaces this release must not regress: [0023 — LLM Call Leasing](rfcs/0023-llm-call-leasing.md), [0019 — OpenTelemetry Completion](rfcs/0019-opentelemetry-completion.md).
- Deferred follow-ups documented as Known Gaps: [ISSUE-0069](issues/ISSUE-0069-task-agent-evaluator-routing-defaults-unconsumed.md), [ISSUE-0070](issues/ISSUE-0070-sub-agent-request-post-init-systemexit-construction.md), [ISSUE-0072](issues/ISSUE-0072-memory-compression-hardcoded-model-literals.md).
