# v0.3.15 Plan — Who Said What (the derived, the relayed, and the replayed write)

**Status**: 🔄 Phase 1 — implementation (C1, residuals PRs 1–4 and B1 merged; workstream B open at [B2](https://github.com/mkhomutov/Persatrix/pull/851))
**Target version**: v0.3.15 (the *attribution* release — one of two patches before the v0.4.0 Agent Organizations train)
**Created**: 2026-08-23
**Branch prefix**: `feature/v0315-`
**Target**: `main`
**Merge strategy**: Squash merge per [BRANCHING.md](BRANCHING.md)
**Codename**: *Who said what*

**Goal**: v0.3.14 made the caller's verified principal a per-request axis, and that boundary holds for **every turn's own write**. It does not hold for the three writes that are not turns:

* the **derived** write — the RFC 0020 interaction scope is the ROOM, so a group channel accumulates every speaker into one record, summarised and fact-extracted under whichever principal closed it ([ISSUE-0082](issues/ISSUE-0082-orchestrator-per-request-session-principal-emission.md) **R-1** / [ISSUE-0123](issues/ISSUE-0123-per-speaker-interaction-scope.md));
* the **relayed** write — a persona's reply re-enters as a fresh unauthenticated publish, so the cascade below it loses the tenant (**R-2** / [ISSUE-0124](issues/ISSUE-0124-orchestrator-hop-drops-tenant-on-agent-cascade.md));
* the **replayed** write — `messages` has no principal column, so nothing catch-up derives at boot can be attributed ([ISSUE-0130](issues/ISSUE-0130-catchup-replay-rederives-memory-under-default-principal.md) shape **(b)**).

Underneath all three sits the axis none supplies: the principal is a *tenant* identifier and only authenticated humans have one, so a room of personas collapses into the shared `local` bucket ([ISSUE-0131](issues/ISSUE-0131-derived-memory-has-no-speaker-attribution.md)). This release closes all four, plus the operational failure that voids the live verification ([ISSUE-0125](issues/ISSUE-0125-agents-never-reregister-after-orchestrator-restart.md)).

Scope was ratified by the [Amendment 2026-08-19](v0.3.x-sequencing.md#amendment-2026-08-19--v0315--v0316-attribution-and-audience-before-the-v040-train), whose next-steps item 3 this executes. The per-PR breakdown mostly **already exists** — [#822](https://github.com/mkhomutov/Persatrix/pull/822) landed the [residuals PR plan](issues/ISSUE-0082-residuals-pr-plan.md) and the [Phase 0 gate](issues/ISSUE-0082-residuals-phase0-gate.md) — so this is a **thin orchestration overlay**, owning what that plan does not: ISSUE-0130(b) ("no PR slot in it and is owned by the v0.3.15 milestone plan"), ISSUE-0125, the sequencing, and the live arc. Each lock states a decision and its binding consequence; evidence sits in the issue it belongs to.

## Scope decisions locked at plan opening (2026-08-23)

Five, in [v0.3.15 — the scope locks](v0.3.15-scope-locks.md) — three the amendment named, two the surface audit forced. Binding for the cycle; re-opened by an amendment, not by a PR. In one line each: the record shape is `(principal, speaker, scope)`, resolved not re-opened; ISSUE-0130(b) is **not** gated on R-2 (two stores, two migrations); ISSUE-0125 rides in shape (4), triggered by any `READY` departure and return; **B2 carries a dedup or watermark check**, not just the narrowed skip; and one live arc, one MT — the extended MT-MEMORY-GROUP-TENANT-001, whose Leg 4 widening and restart leg are B2's to author.

**Out of scope** — deferred explicitly so they do not pressure the cut:

- **Registry persistence / registration as a property of a live stream** — ISSUE-0125's shape (5). It needs an [RFC 0040](rfcs/0040-agent-orchestrator-transport-unification.md) §C amendment, since Phase 2 sketches a *unary* `RegisterAgent`: v0.4.0 work. ISSUE-0125 closes on shape (4) or cuts; it does not grow — **but not silently.** RFC 0040 routes that ask through ISSUE-0125 in both places it appears (§C and OQ 6, which says resolve it before the proto lands, the shape being non-additive), so closing this orphans both pointers. **Phase 4 re-files it as its own issue and re-points RFC 0040.**
- **The v0.3.16 bundle** — [ISSUE-0132](issues/ISSUE-0132-memory-egress-gate-blind-to-room-audience.md) (audience as a gate input, which needs this release's attribution), [ISSUE-0122](issues/ISSUE-0122-relationship-tier-emits-no-provenance.md) / [ISSUE-0108](issues/ISSUE-0108-reasoning-reason-note-no-operator-egress.md), [RFC 0044](rfcs/0044-eval-set-golden-traces.md) Phase 2.
- **Per-principal capacity sweeps and quota policy** (the v0.3.14 Known Gap stands) and **the v0.4.0 on-ramp** — RFC 0038 §C–§E, RFC 0049 Phases 2–4, RFC 0039 Phase 3, RFC 0041 Phase 1.
- **Model-elected attribution.** The speaker axis is a projection of the record key; asking the model which speaker a fact came from stays forbidden.

---

## Acceptance for v0.3.15

The release ships when **all** hold:

- **ISSUE-0123 (R-1) and ISSUE-0124 (R-2) close together** — a relayed turn carries the causal principal *and* lands in that principal's own per-speaker record. The release-note claim is unsafe with either half missing.
- **ISSUE-0131 is closed** — `episodes` and `facts` carry the speaker, frozen at interaction open from the triggering event's `sender_id`. The Phase 0b regression is the gate: one `local` principal, three agent speakers, one room scope → **three** records.
- **ISSUE-0130 shape (b) is delivered** — `messages` persists a server-stamped `principal_id` (v11 → v12), `_build_replay_event` seeds it, the shape-(a) skip narrows to genuinely unattributable spans, and RFC 0037's replayed-rotation stamping returns. **Replay is idempotent across restarts** — row counts in the *attributed* partition are flat across the restart leg, not just in `local`.
- **ISSUE-0125 is closed and ISSUE-0126 retired** — a restart self-heals without touching an agent process, demonstrated on a transition that is *not* `TRANSIENT_FAILURE`; `Register` is an upsert; a reconnect does not re-run catch-up; zero registered agents while channels have members is visible without reading dispatch WARNs; **both** MT warnings are deleted. Or it is **cut**, citing the amendment's clause.
- **No migration lands after its consumer** — v11 → v12 and 17 → 18 each ship in their own PR, ahead of the code reading them, neither touching the other store. Two more were added at the B2 review, neither an exception to it. Persona-memory **19** is an index: it adds no column and changes no read *result*, only the plan, so it has no reader to be ahead of — both its consumers (`update_episode_summary`, on every close since the re-key, and the shape-(b) re-derivation guard) already existed. Channel-store **12 → 13** is a *repair* that ships **with** its consumer, not after it: the review inverted v12's backfill from `'local'` to `''`, and editing an applied migration in place reaches only stores that have not run it — `applyMigration` dispatches on `user_version`, so a store already at v12 keeps the bad backfill forever and its first post-upgrade catch-up derives an authenticated person's content into the shared tenant, which the new re-derivation guard would then make permanent. v13 detects an affected store exactly (the column's recorded DEFAULT is `'local'` there and `''` elsewhere) and rewrites those rows. The release therefore ships **four** migrations across the two stores, not two; the scope lock's "two stores, two migrations" is a statement that the stores are **disjoint**, not a budget.
- **`auth.mode: disabled` stays byte-identical on the principal axis** — and demonstrably **not** on the speaker axis: MT Leg 8's all-agent room is where the speaker split does its work.
- **The live proof exists** — the extended arc on a live provider, with Leg 2's per-dispatch `principal.id` table (storage cannot observe R-2) and Leg 4's `(principal_id, speaker_id, summary)` triples verbatim. The MT collects pairs today; B2 widens it, before Phase 3 rather than after the paid arc.
- **The coherence trade is stated, not discovered** — release notes carry the per-speaker-per-tenant fragmentation of close-derived group memory, the `agent.interactions.closed.by_<reason>` metric-shape change, and the reserve re-size.
- **The full gate sweep is green live on host** — Go `-race`, `cargo test`, the Python suites (including the separate `mypy tests/` leg), Vitest, linters, `make validate`, eval-replay, licenses. **Four** store migrations are expected (persona-memory 18 and 19, channel-store 12 and 13); the checklist names all four.

---

## Master Progress Overview

| # | Workstream | Owner | Status |
|---|-----------|-------|--------|
| A | R-1 + R-2 + ISSUE-0131 — derived and relayed writes; 17 → 18 | [residuals PR plan](issues/ISSUE-0082-residuals-pr-plan.md) PRs 1–5 | 🔄 PRs 1–4 merged ([#844](https://github.com/mkhomutov/Persatrix/pull/844), [#845](https://github.com/mkhomutov/Persatrix/pull/845), [#846](https://github.com/mkhomutov/Persatrix/pull/846), [#849](https://github.com/mkhomutov/Persatrix/pull/849)) — **4b** (reserve re-size + Go asymmetry) and **5** (live MT) remain |
| B | ISSUE-0130 **(b)** — the replayed write; v11 → v12 | [B1](#pr-b1--featurev0315-issue0130-messages-principal) → [B2](#pr-b2--featurev0315-issue0130-replay-seed) | 🔀 B1 merged ([#850](https://github.com/mkhomutov/Persatrix/pull/850), `c95a53d6`); [B2 open](https://github.com/mkhomutov/Persatrix/pull/851) — the seed, the narrowed skip, the idempotence half |
| C | ISSUE-0125 + 0126 — fleet re-registration *(cuttable)* | [C1](#pr-c1--featurev0315-issue0125-reregister-cuttable) | ✅ Merged 2026-08-24 ([#842](https://github.com/mkhomutov/Persatrix/pull/842), `a279fd75`) — **taken, not cut**; both issues stay open until the Phase 3 restart legs verify it live |
| — | release-prep + tag | `v0.3.15-release-prep-plan.md` | ⬜ Not started |

**Legend**: ⬜ · 🔄 In progress · 🔀 PR open · ✅ Merged · ✂️ Cut

---

## Dependency graph

```
v0.3.14 (released 2026-08-19)  +  #822 Phase 0 gate → (principal, speaker, scope)
   │
   └── Phase 0: this planning PR
           │
           ├── C1: fleet re-registration ────────────┐ before any live
           │        (+ both ISSUE-0126 warnings)     │ restart leg
           │                                         │
           ├── A: residuals PR plan                  │
           │      PR 1 → PR 2 → PR 3 → PR 4 ──┐      │
           │      (R-2 Go first, then R-1 +   │      │
           │       0131, 17 → 18)             │      │
           │                                  ↓      │
           │                            PR 5 (live MT + closeout)
           │                                  ↑      │
           └── B1 (v11 → v12, stamp) → B2 ────┘      │
                    (replay seed, dedup, MT edits) ──┤
                                                     │
                     Phase 2: release-prep plan ←────┘
                        → Phase 3: the extended MT arc, live
                        → Phase 4: tag + follow-up
```

Hard edges: C1 before any live restart leg; B1 before B2; **B2 before residuals PR 5**; and residuals PR 1 → 2 → **3** → 4 as one chain. That PR 2 → 3 edge is easy to miss and not optional — R-2 removes the `local` pollution R-1 would otherwise systematize into every group room. A and B have no edge between them.

---

## Phase 0 — This planning PR

**This PR.** This doc; ROADMAP hygiene (below); plan-opening notes on [ISSUE-0125](issues/ISSUE-0125-agents-never-reregister-after-orchestrator-restart.md), [ISSUE-0130](issues/ISSUE-0130-catchup-replay-rederives-memory-under-default-principal.md) (reopened for shape (b)) and [ISSUE-0131](issues/ISSUE-0131-derived-memory-has-no-speaker-attribution.md); the [residuals PR plan](issues/ISSUE-0082-residuals-pr-plan.md) amended for its two inherited obligations; the stale `messages`-migrates-once row corrected on the v0.3.14 checklist; [FILEMAP.md](../FILEMAP.md) regen.

---

## Phase 1 — Implementation PRs

### Workstream A — the derived and relayed writes

Delegated whole to the [residuals PR plan](issues/ISSUE-0082-residuals-pr-plan.md) (PRs 1–5), not restated here — including the half-cap-clamp obligation it already carries. Two things this milestone adds, and that plan is **amended** to state rather than left pointing here: PR 5 runs the *extended* MT, landing after B2; and the threshold-basis half of the reserve change is Go, so it does not ride a Python close-path PR.

### Workstream B — the replayed write

#### PR B1 — `feature/v0315-issue0130-messages-principal`

The column and its producer, landing with **no persona-side consumer** — the dormant-rail split v0.3.14 PR 1 / PR 2 established. Channel store `v11 → v12` adds `principal_id` to `messages` ([`sqlite_schema.go`](../internal/channels/sqlite_schema.go)), stamped **server-side** at publish from the authenticated request context — never from the request body, an agent-supplied principal being the cross-tenant read primitive ISSUE-0130 rules out — and surfaced on `channelMessageResponse`. Backfill is the **empty string** — corrected from `'local'` at the B2 review, and load-bearing: `'local'` is a real answer a v12 writer stamps, and B2's consumer reads a *present* principal as attribution, so backfilling it would make every pre-upgrade row read as attributed. `''` is absent to both Python readers. (Stores that already ran the original spelling are repaired by v13; see the migration criterion above.) Go has no `local` constant to lock-step against and an unauthenticated request yields `""`, so the stamp site coerces explicitly. Tests: migration round-trip; an authenticated publish persists the principal, an unauthenticated one `'local'`; the DTO carries the field.

#### PR B2 — `feature/v0315-issue0130-replay-seed`

The consumer — **open at [#851](https://github.com/mkhomutov/Persatrix/pull/851)**. `build_replay_event` ([`agents/channel_replay_event.py`](../agents/channel_replay_event.py), split out of `channel_catchup.py` at the 500-line cap) seeds the principal from the history payload; the shape-(a) skip narrows from *every* replayed span to genuinely unattributable ones, restoring single-tenant derivation and RFC 0037's replayed-rotation stamping (withdrawn in #834, whose tests flip back). `REASON_CATCHUP_COMPLETE` and the replay-opened-scope boundary are untouched. **Narrowing is only safe with the idempotence half**, so this PR lands the dedup lock 4 names — a derivation-time guard keyed on a boot-stable span identity, since ingest dedup cannot see the previous boot (the tracker is in-memory). The identity's axes are the record key, the agent and the **epoch**, each read off the record rather than the ambient context, plus the ordered wire ids of every turn the span holds. MT work: the restart leg reading both partitions, Leg 4 widened to `(principal_id, speaker_id, summary)`, the sign-off leg count corrected. Tests: an attributed span derives under its own principal; an unattributed one skips; **a span replayed twice derives once**.

### Workstream C — the fleet

#### PR C1 — `feature/v0315-issue0125-reregister` *(cuttable)*

`InMemoryRegistry.Register` becomes an upsert ([`internal/registry/registry.go`](../internal/registry/registry.go)) — the precondition every shape shares, and on its own the fix for a stale advertised address. Agent-side, a watcher on `AgentServer._orchestrator_channel`'s connectivity state re-runs `_self_register()` on **any departure from `READY` and return** (`IDLE` and `CONNECTING` included) **and nothing else** — the no-replay corollary. The watched channel and the registration endpoint (`orchestrator_url`, aiohttp) are different addresses. Plus the observability the issue asks for on its own merit: zero registered agents while channels have members, at ERROR or as a metric. Finally ISSUE-0126 option 1 as corrected — delete **both** warnings. Tests: re-register against a populated registry updates the address rather than `409`-ing; **an `IDLE` cycle re-registers** and issues no history fetch; a restart leaves the fleet dispatchable. Upsert and watcher are pinned separately — the upsert passes even when the watcher never fires.

---

## Phase 2 — v0.3.15 release-prep plan

Open `docs/v0.3.15-release-prep-plan.md` modeled on [v0.3.14-release-prep-plan.md](v0.3.14-release-prep-plan.md): MT-exec PR → docs/checklist PR → version bump + curated `[0.3.15]` CHANGELOG PR → final verification → tag. The release-note obligations are the coherence-trade bullet above, plus both migrations named with their stores and the narrowed catch-up skip. The v0.3.14 residual list is retired only for what closed — its checklist row claiming `messages` migrates once for R-2 too is corrected by this PR.

## Phase 3 — Release-prep execution

Execute the prep plan. The live deliverable is the extended MT-MEMORY-GROUP-TENANT-001: all **nine** legs (0–8) plus the restart leg, machine-paced in one script — written from the leg count here, not the MT's sign-off. The arc runs **once**, here; PR 5 and B2 prepare it and neither runs it early. A green leg that never exercised the contested surface is not proof, so the report records the per-dispatch principals for R-2, the `(principal_id, speaker_id, summary)` triples for R-1 + 0131, and both partitions' row counts across the restart.

## Phase 4 — Tag and post-release follow-up

Mechanical `v0.3.15` tag + Release once Phase 3 is green (body = curated changelog + Known Gaps); row → ✅ Released. Follow-up PR: closures reflected (0123, 0124, 0130, 0131, 0125/0126, and ISSUE-0082's Part 2 note), **the RFC 0040 §C amendment re-filed as its own issue** with §C and OQ 6 re-pointed at it, header refresh → **v0.3.16**, and that plan opened.

---

## ROADMAP hygiene

Generic rules: [.github/copilot-instructions.md §Status Hygiene](../.github/copilot-instructions.md). Release-specific only:

- **First implementation PR opens** → row → 🔄 In progress (on *open*, per v0.3.13/v0.3.14); seed CHANGELOG `[Unreleased]` on its merge.
- **RFC 0020 §G** → amended by residuals PR 3; the RFC's status does not advance — an amendment is not a phase.
- **If ISSUE-0125 cuts** → row → ✂️ Cut, the issue keeps `status: open` with a dated note citing the amendment's clause, and the ISSUE-0126 deletions do not land alone.
- **ISSUE-0130** → reopened by this PR, shape (b) being a scheduled deliverable; it closes at Phase 4 like the rest.

---

## Risk and mitigations

| Risk | Mitigation |
|------|------------|
| The reserve is under-sized and the failure is **silent** — a denied lease commits `SUMMARY_UNAVAILABLE_TEXT` and the janitor never retries it. | Calibration is rightly deferred (a constant guessed off no telemetry is worse than none) — **but the signal is not**: the clamp site returns silently today, so a WARN or counter there and at the `SUMMARY_UNAVAILABLE_TEXT` commit ships with the re-size. [Reserve-sizing record](issues/ISSUE-0082-residuals-reserve-sizing.md). |
| B2 narrows the derivation skip without the idempotence half, and replay growth returns under the attributed principal. | Scope lock 4, and an acceptance condition — the restart leg reads both partitions, and a test pins that a twice-replayed span derives once. |
| C1's watcher is written to a transition that never occurs, and ships green. | Lock 3 names the trigger as *any* `READY` departure and return; the C1 test exercises an `IDLE` cycle, and the upsert test is pinned separately so it cannot stand in as evidence. |
| The release ships half the boundary and the note over-claims. | Acceptance requires R-1 and R-2 together; any write that slips is stated as an open residual. |
| Group-memory fragmentation reads as a regression. | This *is* the promise applied inside a room — the trade v0.3.14 made per person. Room continuity is untouched; stated in the notes, observed by an MT leg. |
| The live arc cannot run because the fleet goes mute at the first restart — the failure that already voided one arc on a paid provider. | C1 lands before any live restart leg. If it cuts, the arc keeps the manual-restart step and both warnings stay. |

---

## Decision / next steps

**Status**: 🔄 Phase 1 — implementation open.

1. ~~Land this planning PR.~~ Landed 2026-08-23 ([#841](https://github.com/mkhomutov/Persatrix/pull/841)).
2. ~~Open [C1](#pr-c1--featurev0315-issue0125-reregister-cuttable).~~ Merged 2026-08-24 ([#842](https://github.com/mkhomutov/Persatrix/pull/842)), taken not cut. Workstream A is through PR 4 (4b and 5 remain); workstream B is open at **[B2](https://github.com/mkhomutov/Persatrix/pull/851)**, B1 merged. C1 gated neither, so both run in parallel.
3. Amend the residuals plan for the two obligations above.
4. The live arc runs once, at Phase 3.
5. At the tag, open `v0.3.16-plan.md` per the [amendment's next-steps item 4](v0.3.x-sequencing.md#next-steps-7).

---

## Related documentation

- [v0.3.x-sequencing.md](v0.3.x-sequencing.md) ([Amendment 2026-08-19](v0.3.x-sequencing.md#amendment-2026-08-19--v0315--v0316-attribution-and-audience-before-the-v040-train) — this plan is its next-steps item 3).
- [v0.3.15 — the scope locks](v0.3.15-scope-locks.md) — the five, in full; split out at B2 when this doc reached the word cap.
- [ISSUE-0082 residuals PR plan](issues/ISSUE-0082-residuals-pr-plan.md) + the [Phase 0 gate](issues/ISSUE-0082-residuals-phase0-gate.md) — workstream A; it carries its own §Related list, not re-glossed here.
- Scoped issues: [0123](issues/ISSUE-0123-per-speaker-interaction-scope.md) · [0124](issues/ISSUE-0124-orchestrator-hop-drops-tenant-on-agent-cascade.md) · [0130](issues/ISSUE-0130-catchup-replay-rederives-memory-under-default-principal.md) · [0131](issues/ISSUE-0131-derived-memory-has-no-speaker-attribution.md) · [0125](issues/ISSUE-0125-agents-never-reregister-after-orchestrator-restart.md) · [0126](issues/ISSUE-0126-mt-orchestrator-restart-registry-note-missing.md).
- [RFC 0011](rfcs/0011-channels-bridges.md) OQ #8 (no catch-up watermark — what makes lock 4 load-bearing) · [RFC 0040](rfcs/0040-agent-orchestrator-transport-unification.md) §C / OQ 6 (the amendment Phase 4 re-files).
- [MT-MEMORY-GROUP-TENANT-001](manual-tests/MT-MEMORY-GROUP-TENANT-001.md) — the release gate; [MT-MEMORY-MULTIUSER-001](manual-tests/MT-MEMORY-MULTIUSER-001.md) — the per-turn boundary it builds on.
- [memory-scope-axes.md](memory-scope-axes.md) — its Decision 2 drops *sender* from the **session** unit; the interaction key locked here is a different axis (RFC 0020 §G `scope`) and does not reopen it.
