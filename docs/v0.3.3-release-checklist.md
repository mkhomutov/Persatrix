# v0.3.3 Release Checklist

> **Status**: ✅ Released — all four release-prep PRs merged (plan [#415](https://github.com/mkhomutov/Persatrix/pull/415), MT execution report [#416](https://github.com/mkhomutov/Persatrix/pull/416), docs refresh [#418](https://github.com/mkhomutov/Persatrix/pull/418), version bump + changelog + `MemoryFacade` removal [#419](https://github.com/mkhomutov/Persatrix/pull/419)). Release-prep PR 4 (final pre-tag verification) re-ran the full gate sweep on the post-bump tip — all green, incl. a live Docker smoke of the four release behaviours — and flipped the status docs to released. The `v0.3.3` git tag and GitHub Release are pushed after PR 4 merges per [release-prep plan Phase 5](v0.3.3-plan.md#phase-5--tag-and-post-release-follow-up).

> v0.3.3 — "Idle Truly Idle" — ships [RFC 0024](rfcs/0024-event-driven-scheduling.md) (Event-Driven Agent Scheduling — Phases 1–4). The user-facing promise: *a persona with no scheduled work and no inbound traffic costs nothing — no SQLite recall, no `_inject_memory_context`, no provider call, no wallet lease; the autonomy loop parks on `queue.get()` and wakes only on an inbound RPC, a scheduled timer, or a salience memory write.* The polling-loop cost-leak class is closed structurally, guarded by the bored-persona `cost-regression-gate` CI job.

**Based on test report**: [`docs/manual-tests/v0.3.3-execution-report.md`](manual-tests/v0.3.3-execution-report.md)
**Release prep plan**: [`docs/v0.3.3-release-prep-plan.md`](v0.3.3-release-prep-plan.md)
**Master plan**: [`docs/v0.3.3-plan.md`](v0.3.3-plan.md)
**RFC**: [`docs/rfcs/0024-event-driven-scheduling.md`](rfcs/0024-event-driven-scheduling.md) (PR plan: [`0024-pr-plan.md`](rfcs/0024-pr-plan.md))

---

## 1. Pre-release Verification

All build gates run on a clean checkout of the release-candidate commit, executed by PR 4 of the release-prep plan. PR 1 already ran the full automated suite green on the `ea2b86d` RC tip (2396 unit / 212 integration incl. `cost-regression-gate` / 387 agents / 102 Rust / Go race-cover); PR 4 re-certifies on the post-version-bump tip.

- [x] `make test` green on the post-bump tip — Go `go test ./internal/... -race -cover` → 17 packages `ok`, 0 fail, no races; Python unit **2399 passed, 6 skipped**; Python integration **212 passed, 11 skipped** (incl. `cost-regression-gate` `test_bored_persona_cost.py`); agents-runtime **387 passed**; Rust **102 passed, 0 failed**. Per-suite detail in the [execution report § Re-Execution](manual-tests/v0.3.3-execution-report.md#re-execution--release-prep-pr-4-post-version-bump-rc-tip).
- [x] `make lint` clean — golangci-lint; ruff (agents + repo-root `tests/`); mypy whole-package (181 + 224 source files, as CI runs it); clippy on `persatrix v0.3.3`.
- [x] `make validate` passes — 5 configs + `instructions_file` refs resolve (re-validated after each temporary smoke edit + revert). The new `autonomy.timers` / `salience_threshold` / `salience_rate_max_per_sec` keys in [`schemas/agent.schema.json`](../schemas/agent.schema.json) validate.
- [x] Proto staleness clean — **both** Go and Python stubs regenerated, `git diff --exit-code` shows no drift; orphan-check clean. RFC 0024 adds **no** proto change (`wake.kind` is an OTEL dimension, not a `LeaseRequest` field; [`proto/wallet.proto`](../proto/wallet.proto)'s `Cause cause` unchanged). Improves on v0.3.2 PR 4, which skipped `proto-python` locally.
- [x] `make check-licenses` clean — Go OK; Python 71 packages all in allow-list (project `.venv`); Rust "licenses ok".
- [x] `make notices` up to date — **no delta** (RFC 0024 + `MemoryFacade` removal add no new third-party prod dependency).
- [x] `make generate-sanitizer-patterns-check` passes — Go↔Python parity (11 patterns / 5 sources / 2 actions).
- [x] **MT-IDLE-001** (idle persona costs nothing) — **primary release gate** `Pass` live on the [`ea2b86d`](https://github.com/mkhomutov/Persatrix/commit/ea2b86d) RC tip ([#416](https://github.com/mkhomutov/Persatrix/pull/416)): over a 60 s window an `autonomy.timers: []` persona (ember-owl) showed **zero** provider activity, **zero** wallet activity, and **zero** `agent.wake.*` increments; a single chat turn then woke the parked loop (`reply_status="ok"`, `agent_wake_inbound_total` absent → 1), proving the idle zeros were genuine. PR 4 re-runs this MT on the post-version-bump tip.
- [x] **Bored-persona `cost-regression-gate` CI job** — release-blocker per [RFC 0024 §Test Strategy](rfcs/0024-event-driven-scheduling.md#test-strategy). `tests/integration/test_bored_persona_cost.py` passes on `ea2b86d` (part of the 212-passing integration run). PR 4 confirms it green on the post-version-bump tip; it must stay a **PR trigger**, not a nightly.
- [x] **MT-COST-004** (TICK budget exhaustion → idle) — `Pass` (with one ⚠️ Accepted-with-known-gap on Edge Case 2 — RFC 0017 §F empty-context dominance — tracked as [F-2](manual-tests/v0.3.3-execution-report.md#follow-ups)). The TICK path now routes through the legacy-adapter `ScheduledWake(timer_id="legacy_tick")`; `idle_reason=budget_denied` is confirmed by the passing `tests/integration/test_tick_budget_denied_idle.py`. PR 4 re-runs on the post-version-bump tip.
- [x] **MT-COST-003** (chat budget exceed) — `Pass` (carry-forward + automated). The chat path is **unchanged** in v0.3.3 — it keeps its synchronous-return contract via `SyncDispatchHandle` (only `ReceiveChannelMessage` moved to fire-and-forget); regression-covered by `tests/integration/test_chat_budget_exhaustion.py` (passing) and the live `reply_status="ok"` chat in MT-IDLE-001 Step 5.
- [x] **Personal-tier latency regression gate** — [`tests/perf/personal_tier_latency.py`](../tests/perf/personal_tier_latency.py) re-run on the post-bump tip: **p99 = 0.92 ms, p50 = 0.28 ms** (corpus 500, 500 iterations) — informational only (no committed baseline yet, exit 0), well under the 5 ms target.
- [x] Docker smoke test on the post-bump tip — **all four behaviours verified live** on a freshly-rebuilt stack (11 services healthy; one `docker builder prune` cleared a stale containerd snapshot first). Full evidence in the [execution report § Re-Execution](manual-tests/v0.3.3-execution-report.md#re-execution--release-prep-pr-4-post-version-bump-rc-tip):
  - [x] Bored `timers: []` persona (ember-owl) ~60 s idle → `cost/summary` flat at `0/0/$0`, **zero** `agent.wake.*`, zero LLM/memory/wallet/dispatch lines.
  - [x] Temporary `autonomy.timers` entry → `agent_wake_scheduled_total{timer_id="smoke_timer"}` advances on the 5 s cadence; ticks short-circuit (`empty_context_tick`), cost flat. Reverted.
  - [x] Channel message → **HTTP 201 in 81 ms** (immediate, non-blocking); agent processes the `InboundEventWake` async (`agent_wake_inbound_total` absent → 1). RFC 0024 PR 4 fire-and-forget.
  - [x] Exhausted `per_agent` budget → chat **HTTP 200**, `reply_status="error"`, reply `"per_agent budget exceeded: …"` (RFC 0023 carry-forward; chat path unchanged via `SyncDispatchHandle`). Reverted.

> **Environment note ([F-3], carry-forward from v0.3.2).** An empty `ANTHROPIC_API_KEY` exported in the operator's shell **overrides** a populated `.env` under Docker Compose interpolation precedence — `docker compose build`/`up` then aborts with `required variable ANTHROPIC_API_KEY is missing a value`. Unset the empty shell variable (or give it the real key) before running the stack. Plain `docker …` commands (logs, restart, ps) do not interpolate and need no prefix.

---

## 2. Version Alignment

Before tagging, all shipped components must declare `0.3.3` consistently. All components are at `0.3.2`.

| Component | File | Required state for release |
|-----------|------|----------------------------|
| Python agents | `agents/pyproject.toml` | `version = "0.3.3"` |
| Python observability | `agents/observability/{metrics,tracing}.py` | `_DEFAULT_SERVICE_VERSION = "0.3.3"` (+ `tracing.py` docstring default) |
| Rust CLI | `cli/Cargo.toml` | `version = "0.3.3"` |
| Rust lockfile | `cli/Cargo.lock` | Regenerated after the CLI version bump |
| Go orchestrator | git tag | Release identified by `v0.3.3` tag |

`scripts/bump_version.py` bumps the `pyproject.toml`, `Cargo.toml`, and observability `service.version` strings; `cli/Cargo.lock` is then regenerated via `cargo update --workspace`. The observability defaults are not build inputs, so a stale value never fails `make all` — it must be bumped explicitly rather than relying on the build to catch it.

Checklist (filled in by release-prep plan PR 3):

All bumped by PR 3 ([#419](https://github.com/mkhomutov/Persatrix/pull/419)); PR 4 confirms each on the post-bump tip:

- [x] `agents/pyproject.toml` → `0.3.3`
- [x] `agents/observability/{metrics,tracing}.py` → `_DEFAULT_SERVICE_VERSION = "0.3.3"` (both)
- [x] `cli/Cargo.toml` → `0.3.3`; `cli/Cargo.lock` regenerated (CLI builds as `persatrix v0.3.3`)
- [x] `make all` builds at the new version — orchestrator (`go build`) + CLI (`cargo build --release`) both at `0.3.3`
- [x] No docs describe v0.3.3 as unreleased WIP — README flipped in PR 2 ([#418](https://github.com/mkhomutov/Persatrix/pull/418)); PR 4 flips the ROADMAP Version Map row, this checklist's status, and the release-prep plan status. Only post-tag artifacts (tag link, Release URL, "Released" stamps) remain for the Phase 5 follow-up.

---

## 3. Changelog

The release changelog must preserve every existing curated section in [`CHANGELOG.md`](../CHANGELOG.md) (`[0.3.2]` and prior) and add a new `[0.3.3]` section. `CHANGELOG.md` has **no `[Unreleased]` section** — it goes from `# Changelog` directly to `## [0.3.2]`; release-prep PR 3 generates the curated `[0.3.3]` section directly via `git-cliff` over the `v0.3.2..HEAD` range (13 commits at plan opening).

Checklist:

- [x] `CHANGELOG.md` contains a `[0.3.3] - 2026-05-22` section (PR 3 [#419](https://github.com/mkhomutov/Persatrix/pull/419))
- [x] The existing curated `[0.3.2]`, `[0.3.1]`, `[0.3.0]`, `[0.2.3]`, `[0.2.2]`, `[0.2.1]`, `[0.2.0]`, and `[0.1.0]` sections are preserved verbatim — not overwritten (`[0.3.2]` … `[0.1.0]` headers all present below the new section)
- [x] The `[0.3.3]` section includes:
  - **Highlights**: the persona autonomy loop is now event-driven — a persona with `autonomy.timers: []` and no inbound traffic parks on `queue.get()` and pays nothing; the polling-loop cost-leak class is closed structurally, guarded by `cost-regression-gate`
  - **Features**: RFC 0024 PRs 1–6 (incl. 2.1 / 3a / 3b / 5.1) — `EventLoop` + `WakeEvent` + `SyncDispatchHandle` → `autonomy.timers` + `scheduled_wakes` cache → `ScheduledWakesCache` init wiring → write-side `salience` + `source_span_id` → `SalienceWake` + threshold + loop-back guard → channel-message fire-and-forget dispatch + cost-regression CI gate → EventLoop lifecycle hardening → review-follow-up cleanups → Phases-1–4 closeout; PR ranges per [release-prep plan §RFC PR roll-up](v0.3.3-release-prep-plan.md#rfc-pr-roll-up)
  - **(No Bug Fixes section)** — no tracked-issue bug-fix PRs shipped in `v0.3.2..HEAD`; every commit since the tag is an RFC 0024 PR-plan PR or a docs/plan PR
  - **Documentation**: MT execution report (release-prep PR 1, [#416](https://github.com/mkhomutov/Persatrix/pull/416)), this docs refresh (release-prep PR 2), release-prep + master plans, MT-CHAT-003/004 interaction-close refresh ([#417](https://github.com/mkhomutov/Persatrix/pull/417))
  - **Testing**: 1 new MT (`MT-IDLE-001`), the bored-persona `cost-regression-gate` CI job, and the EventLoop / scheduled-wakes / salience / wake-counter suites
  - **Upgrade Notes** subsection (see §3.1 below)
- [x] RFC 0041–0044 drafts ([#399](https://github.com/mkhomutov/Persatrix/pull/399), all `🔨 Draft`) stay under an RFC-tracking line in **Documentation** ("RFC-tracking only"), **not** under Features

Suggested generation command:

```bash
git-cliff --tag v0.3.3 --unreleased --prepend CHANGELOG.md
```

If `git-cliff` would overwrite curated content, generate into a temporary file and merge manually — same pattern as v0.3.2 PR 3 / v0.3.1 PR 3.

### 3.1 Required Upgrade Notes in Changelog

This is the canonical source for Upgrade Notes text.

| Notable change | Detail |
|----------------|--------|
| **[Behaviour change — idle is truly idle]** Event-driven autonomy loop | The persona autonomy loop is now event-driven ([RFC 0024](rfcs/0024-event-driven-scheduling.md) Phases 1–4). A persona with `autonomy.timers: []` and no inbound traffic parks on `queue.get()` and pays nothing — no SQLite recall, no `_inject_memory_context`, no provider activity, no wallet lease. The polling-loop cost-leak class is closed structurally, guarded by `cost-regression-gate`. |
| **[Behaviour change — channel dispatch]** Fire-and-forget channel messages | `ReceiveChannelMessage` now enqueues `InboundEventWake(event)` fire-and-forget via `event_loop.enqueue` (not `scheduler.wake()`) and returns `TaskAck` immediately. Chat, sub-agent, and workflow-task paths are unchanged — they keep their synchronous-return contract via `SyncDispatchHandle`. |
| New `autonomy.timers` config block | Per-agent timer list in `agents.yaml` / [`schemas/agent.schema.json`](../schemas/agent.schema.json): `id`, `interval_seconds` (≥ `1.0`), `kind` (a `callback_kind` label), optional `jitter_max_seconds`. `tick_interval_seconds` **continues to work** — synthesised as one `ScheduledWake(timer_id="legacy_tick", callback_kind="tick")`; if both are set, `timers` wins (one-time INFO log). Absent block → `timers: []`. Deprecation **warning** is Phase 5 (v0.4.0); removal is Phase 6 (v0.5+). |
| New per-agent SQLite `scheduled_wakes` cache | Derived from `agents.yaml` (source of truth), rebuilt on startup; restores `next_fire_at_ms` so a persona restarted mid-jitter-window resumes its schedule instead of re-randomising. Orphan rows deleted on next startup. No operator action. |
| New `autonomy.salience_threshold` (default `0.95`) + `autonomy.salience_rate_max_per_sec` (default `10`) | Salience-triggered wakes from the memory-write path. **Ships disabled by default** — the threshold is strictly above stock conservative scoring (`REFLECTION_CONTRADICTION_SALIENCE = 0.6`). Calibration is a data-gated follow-up ([RFC 0024 §OQ §2](rfcs/0024-event-driven-scheduling.md#open-questions)). The rate-limit is a DoS guard, not a calibration knob. |
| New `agent.wake.{inbound,scheduled,salience,dropped}` metric counters | Carry `wake.kind` (plus `timer_id` on scheduled, `tier` + `suppressed_reason` on salience). `wake.kind` is an **OTEL/metric dimension only** — *not* a new `LeaseRequest` proto field; [`proto/wallet.proto`](../proto/wallet.proto)'s `Cause cause` is unchanged, so v0.3.2 cost-attribution dashboards keep working (`trace_id` already links the lease to the wake span). See [observability.md §10.5](observability.md#105-persatrix-specific-attribute-namespace). |
| RFC 0017 §F empty-context TICK guard is now structurally unreachable but stays in place | Documented as vestigial via an [`action_loop.py`](../agents/persona_runtime/action_loop.py) cross-link naming Phase 5/6 as the deletion path. No behaviour change. |
| `MemoryFacade` alias removal — PR 3 decision | The [v0.3.2 Upgrade Notes](v0.3.2-release-checklist.md#31-required-upgrade-notes-in-changelog) committed to removing the `agents.memory.MemoryFacade` alias "in v0.3.3". PR 3 reconciles per [release-prep plan §Known follow-up §2](v0.3.3-release-prep-plan.md#known-follow-up-issues): either land the one-symbol removal as published-deprecation hygiene (recommended) or restate the horizon to v0.4.0; PR 3 records the chosen path in its description. |

---

## 4. Manual Test Sign-off

Release sign-off requires the full **v0.3.3 surface — 34 rows** executed and recorded in [`docs/manual-tests/v0.3.3-execution-report.md`](manual-tests/v0.3.3-execution-report.md) — 1 new primary gate (`MT-IDLE-001`) plus the 33-row v0.3.2 surface carried forward and regression-checked under the event-driven loop.

The authoritative test index is [`docs/manual-tests/README.md`](manual-tests/README.md).

### Primary v0.3.3 Gate

| Area | Test ID |
|------|---------|
| Idle persona costs nothing — `timers: []` + no inbound traffic → zero provider/wallet/`agent.wake.*` activity over 60 s; a single inbound event wakes the parked loop (RFC 0024 Phases 1–4) — **new** | `MT-IDLE-001` |

### Carried-Forward Suite (v0.1 + v0.2.x + v0.3.0 + v0.3.1 + v0.3.2 surface)

| Area | Test IDs |
|------|----------|
| Workflow | `MT-WORKFLOW-001`, `MT-WORKFLOW-002` |
| Agent | `MT-AGENT-001` |
| CLI | `MT-CLI-001` |
| Config | `MT-CONFIG-001` |
| Persona | `MT-PERSONA-001`, `MT-PERSONA-002` (deprecated — superseded), `MT-PERSONA-003`, `MT-PERSONA-004`, `MT-PERSONA-CONVERSATION-001` |
| Memory | `MT-MEMORY-001`, `MT-MEMORY-002`, `MT-MEMORY-003`, `MT-MEMORY-004`, `MT-MEMORY-005` |
| Cost | `MT-COST-001`, `MT-COST-002`, `MT-COST-003`, `MT-COST-004` |
| Integration | `MT-INTEGRATION-001` |
| Chat | `MT-CHAT-001`, `MT-CHAT-002`, `MT-CHAT-003`, `MT-CHAT-004` |
| Logs / OTEL | `MT-LOGS-001`, `MT-OTEL-001` |
| Channels | `MT-CHANNEL-001` … `MT-CHANNEL-006` |
| Sessions | `MT-SESSION-001` |

### Results Summary (from execution report, `ea2b86d` RC tip)

| Outcome | Count |
|---------|------:|
| ✅ Pass | 29 |
| ⚠️ Accepted-with-known-gap | 4 |
| ⊘ Deprecated (superseded) | 1 (`MT-PERSONA-002` — coverage carried by `MT-CHANNEL-002` + `MT-CHANNEL-004`) |
| ❌ Fail | 0 |
| ⬜ Not run | 0 |

Checklist:

- [x] [`docs/manual-tests/v0.3.3-execution-report.md`](manual-tests/v0.3.3-execution-report.md) exists and is marked ✅ Complete (release-prep PR 1 — [#416](https://github.com/mkhomutov/Persatrix/pull/416))
- [x] Each test records date, tester, environment, and evidence
- [x] Every test outcome is `Pass`, `Accepted-with-known-gap`, or `Deprecated` — zero `Fail`, zero `Not run`
- [x] `MT-IDLE-001` recorded `Pass` with per-observation evidence (60 s window: zero cost delta, zero `agent.wake.*`, zero `_inject_memory_context`/`AcquireLease` log lines; chat turn then woke the loop with `agent_wake_inbound_total` absent → 1)
- [x] Bored-persona `cost-regression-gate` (`test_bored_persona_cost.py`) green on the RC tip (part of the 212-passing integration run)
- [x] `MT-COST-004` recorded `Pass` with the legacy-adapter `ScheduledWake(timer_id="legacy_tick")` routing live-confirmed (`agent_wake_scheduled_total{timer_id="legacy_tick"}` fired through the `EventLoop`); `idle_reason=budget_denied` confirmed by `test_tick_budget_denied_idle.py`; Edge Case 2 (RFC 0017 §F empty-context dominance) ⚠️ Accepted-with-known-gap per [execution report F-2](manual-tests/v0.3.3-execution-report.md#follow-ups)
- [x] `MT-COST-003` recorded `Pass` — chat path unchanged (synchronous-return via `SyncDispatchHandle`); regression-covered by `test_chat_budget_exhaustion.py`
- [x] `MT-MEMORY-005` is **not re-run** — no recall-tier change in v0.3.3 (PR 3a added write-side `salience`/`source_span_id` only; RFC 0017 BM25 recall untouched); the v0.3.1 result carries forward
- [x] `MT-CHAT-003` / `MT-CHAT-004` recipes rewritten for the RFC 0020 interaction-close model and validated live ([#417](https://github.com/mkhomutov/Persatrix/pull/417))
- [x] Every `Accepted-with-known-gap` row links a tracked issue or an RFC-closeout / sequencing follow-up bullet

---

## 5. Tag and GitHub Release Procedure

Use a clean `main` checkout after the final release-prep PR (PR 4 of the prep plan) merges.

```bash
git checkout main
git pull --ff-only origin main
git tag -a v0.3.3 -m "v0.3.3 — Idle Truly Idle"
git push origin v0.3.3
```

GitHub Release checklist (post-merge manual steps after PR 4 merges):

- [ ] Create the GitHub Release from tag `v0.3.3`
- [ ] Use the curated `[0.3.3]` changelog section as the release notes baseline
- [ ] Include upgrade notes (§3.1) in the release body
- [ ] Include known gaps (§6) in the release body
- [ ] Attach binaries or container references if produced for this release — none planned (matches the v0.3.2 / v0.3.1 / v0.3.0 pattern)

---

## 6. Known Gaps to Document in Release Notes

These items remain intentionally incomplete and should be described as deferred scope, not regressions. Seeded from [v0.3.3-plan §Out of scope](v0.3.3-plan.md) and the [release-prep plan §Known follow-up issues](v0.3.3-release-prep-plan.md#known-follow-up-issues).

### v0.3.3-scope gaps

| Area | Description | Deferred to |
|------|-------------|-------------|
| `tick_interval_seconds` deprecation warning | Phases 1–4 keep `tick_interval_seconds` working silently via the `legacy_tick` adapter. The deprecation **warning** on the field is RFC 0024 Phase 5. | RFC 0024 Phase 5 (v0.4.0) |
| `tick_interval_seconds` removal + §F guard deletion + `EventType.TICK` removal | The legacy field, the now-vestigial RFC 0017 §F empty-context short-circuit, and the `EventType.TICK` enum value are removed together once the Phase 6 entrance criteria are met. | RFC 0024 Phase 6 (v0.5+) |
| Salience-threshold calibration | `autonomy.salience_threshold` ships at `0.95`, strictly above stock conservative scoring (`0.6`), so no stock-config write produces a `SalienceWake`. Lowering it is data-gated — lands once an observed salience-distribution sample exists. | [RFC 0024 §OQ §2](rfcs/0024-event-driven-scheduling.md#open-questions) / [v0.3.x-sequencing §OQ §3](v0.3.x-sequencing.md#open-questions) |
| Stale "consume LLM tokens continuously" cost-warning ([F-1]) | A startup `WARN COST: …will consume LLM tokens continuously` fires even for `autonomy.timers: []` personas (the live 60 s window proved zero consumption). Cosmetic, not a regression — it predates RFC 0024 and is accurate only when timers are configured. Fix: gate the warning on a non-empty timer set. | Tracked-issue / RFC 0024 closeout follow-up ([execution report F-1](manual-tests/v0.3.3-execution-report.md#follow-ups)) |
| MT-COST-004 Edge Case 2 (RFC 0017 §F empty-context dominance) ([F-2]) | The empty-context short-circuit fires before lease acquisition, masking the `budget_denied` idle reason in that recipe leg. The user-facing surface — autonomous wakes record idle, no provider call — is still correct; the recipe needs a fixture that forces the LLM-call branch under budget pressure. `idle_reason=budget_denied` is confirmed by `test_tick_budget_denied_idle.py`. | MT recipe rewrite follow-up ([execution report F-2](manual-tests/v0.3.3-execution-report.md#follow-ups)) |
| Multi-agent conversation governance — Phase 1 | RFC 0030 Phase 1 (deterministic termination + cost + reply-budget layers) is unblocked since RFC 0023 shipped but not absorbed into v0.3.3 (one-story discipline). | RFC 0030 Phase 1 (dedicated v0.3.x plan or v0.4.0) |
| Per-session recall isolation, `persatrix session …` CLI, sessions operator guide | Carried forward from v0.3.1/v0.3.2. RFC 0031 Phase 1 remains write-path only; cross-run isolation still relies on `make reset`. Phases 2–4 re-homed to a dedicated v0.3.4 patch (the recall-filtering F-3 fix is their own story). | RFC 0031 Phases 2–4 (v0.3.4 — see [RFC 0031 Phase 2 PR plan](rfcs/0031-phase2-pr-plan.md)) |
| Personal/society storage split — Phases 2–6 | Carried forward from v0.3.2. The Postgres society backend, multi-agent fan-out, and the personal-tier migration. | RFC 0029 Phases 2–6 (v0.4.0) |
| Conversational memory for group channels + window instrumentation/tuning | Carried forward from v0.3.1. RFC 0034 Phase 1 covers DM channels only. | RFC 0034 Phases 2–3 (v0.3.x) |
| Chat peer recorded as `other_participant_type='agent'`, not `'user'` ([F-4] / ISSUE-0068) | **Pre-existing, not a v0.3.3 regression.** REST chat reaches the agent as a `ChannelMessageEvent` with no `participant_type` field, so the relationship tier can't distinguish a human-user peer from an agent peer on the chat path. Does not affect the MT-IDLE-001 gate or the release decision. | [ISSUE-0068](issues/ISSUE-0068-chat-peer-recorded-as-agent-participant-type.md) / [#417](https://github.com/mkhomutov/Persatrix/pull/417) |

### Carried forward from earlier releases

The v0.3.2-scope and earlier gaps documented in [v0.3.2 release checklist §6](v0.3.2-release-checklist.md#6-known-gaps-to-document-in-release-notes) remain open in v0.3.3 unchanged (per-cause budget policies, distributed wallet, channels REST surface unauthenticated, external bridges deferred, RFC 0009 Phases 3–4 sandbox + token auth, RFC 0021 Phases 2–4 temporal awareness, RFC 0008 eviction-parameter calibration, watermark catch-up, SSE streaming, `persatrix logs` restart durability, `code-writer` iteration ceiling, `MT-COST-002` workflow-abort sentinel). The v0.3.3 release notes link the v0.3.2 checklist for full detail rather than re-listing them here.

> **Closed in v0.3.3**: the polling-loop cost-leak class — a persona with no scheduled work and no inbound traffic now parks on `queue.get()` and pays nothing, structurally rather than per-release patch (RFC 0024 Phases 1–4, [#406](https://github.com/mkhomutov/Persatrix/pull/406)–[#414](https://github.com/mkhomutov/Persatrix/pull/414)). The promise is guarded by the `cost-regression-gate` CI job on every PR touching the wake-path file set.

---

## 7. Summary Checklist

Single-page go/no-go gate. All items must be checked before tagging.

```text
[x] make test is green on the post-bump RC tip (PR 4: Go 17 pkgs ok race-cover / 2399 unit / 212 integration incl. cost-regression-gate / 387 agents / 102 Rust — 0 fail)
[x] make lint is clean (golangci-lint / ruff agents+tests / mypy agents 181 + tests 224 / clippy v0.3.3)
[x] make validate passes (incl. new autonomy.timers / salience_threshold / salience_rate_max_per_sec keys in agent.schema.json)
[x] make proto then git diff --exit-code shows no generated drift — both Go + Python stubs regenerated, no drift (RFC 0024 adds no proto change)
[x] make check-licenses passes (Go OK / Python 71 pkgs OK / Rust licenses ok)
[x] THIRD_PARTY_NOTICES.md regenerated via make notices — up to date, no delta (no new prod dep)
[x] make generate-sanitizer-patterns-check passes (Go ↔ Python pattern parity)
[x] agents/pyproject.toml updated to 0.3.3
[x] agents/observability/{metrics,tracing}.py service.version defaults updated to 0.3.3
[x] cli/Cargo.toml updated to 0.3.3
[x] cli/Cargo.lock regenerated and committed
[x] make all builds successfully (orchestrator + CLI both at 0.3.3)
[x] CHANGELOG.md contains a curated [0.3.3] section with upgrade notes (no Bug Fixes section)
[x] Existing [0.3.2], [0.3.1], [0.3.0], [0.2.3], [0.2.2], [0.2.1], [0.2.0], [0.1.0] sections preserved verbatim
[x] All 34 manual-test rows recorded in docs/manual-tests/v0.3.3-execution-report.md
[x] No unresolved Fail in manual tests; zero Not run
[x] MT-IDLE-001 recorded Pass with per-observation idle-window evidence (primary release gate)
[x] Bored-persona cost-regression-gate green on the RC tip (release-blocker; PR trigger, not nightly)
[x] MT-COST-004 recorded Pass with legacy_tick ScheduledWake routing live-confirmed; Edge Case 2 ⚠️ Accepted-with-known-gap (F-2)
[x] MT-COST-003 recorded Pass (chat path unchanged via SyncDispatchHandle)
[x] Personal-tier latency regression gate re-run on the RC tip (p99=0.92 ms, p50=0.28 ms; informational, exit 0)
[x] Known gaps documented for release notes (§6 above)
[x] Docker smoke test — bored persona (zero spend) + scheduled-timer wake + channel fire-and-forget TaskAck + denied chat lease all verified on the post-bump tip
[ ] git tag v0.3.3 created and pushed (Phase 5, post-merge)
[ ] GitHub Release published with changelog, upgrade notes, and known gaps (Phase 5, post-merge)
```
