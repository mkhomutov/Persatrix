# v0.3.7 Release Checklist

> **Status**: 🚧 Release prep — **in progress.** This checklist is created by [release-prep plan PR 2](v0.3.7-release-prep-plan.md#pr-2--readme--roadmap--channelspersona-guide-verification--release-checklist) (docs surface). §1 (Pre-release Verification) and §2 (Version Alignment) are executed by [PR 3](v0.3.7-release-prep-plan.md#pr-3--version-bump--changelog-curation) (version bump + changelog) and re-certified live by [PR 4](v0.3.7-release-prep-plan.md#pr-4--final-pre-tag-verification--release-notes) on the post-version-bump tip; §3.1 (Upgrade Notes) is the canonical source PR 3 reconciles into `CHANGELOG.md`. §4 (Manual Test Sign-off) references the [MT execution report](manual-tests/v0.3.7-execution-report.md) landed by [PR 1](https://github.com/mkhomutov/Persatrix/pull/561) (re-run on the post-fix tip, [#564](https://github.com/mkhomutov/Persatrix/pull/564)) — a **clean pass**: both v0.3.7 headline gates and the combined realism walkthrough passed **live on Anthropic** (RC tip `92a5a00`), so no live hard-block is outstanding.

> v0.3.7 — "Conversations worth watching" — ships the **realism rung**: the three workstreams that make a group channel read like colleagues rather than bots. **[RFC 0034 Phase 2](rfcs/0034-persona-conversational-working-memory.md) group-channel working memory** — each persona sees the in-progress transcript with per-peer `[<peer_id>]: ` attribution and builds on a specific peer's contribution. **[RFC 0030 relevance Tier A](rfcs/0030-amendment-relevance-gated-response.md) + the `respond_policy → disposition` reframe** — a directed `@`-question is answered by exactly the addressee (others suppressed `directed_elsewhere`, zero-cost), open-floor admits all participants, and an `@everyone` broadcast disables the directed filter. **The peer-voice prompt** — personas address each other by name and build on the round rather than performing assistant-helpfulness. Folded in: the **F-1…F-7 conversation test-findings cluster**, capped by the **[F-7 Option D](issues/ISSUE-0093-person-identity-cross-room-tier.md) person-identity-on-the-cross-room-tier** migration. The user-facing promise: *open a multi-persona group channel, ask one persona a question, and exactly that persona answers — then ask the room and watch an ordered, mutually-aware round that reads like colleagues.*

**Based on test report**: [`docs/manual-tests/v0.3.7-execution-report.md`](manual-tests/v0.3.7-execution-report.md)
**Release prep plan**: [`docs/v0.3.7-release-prep-plan.md`](v0.3.7-release-prep-plan.md)
**Master plan**: [`docs/v0.3.7-plan.md`](v0.3.7-plan.md)
**RFCs**: [`docs/rfcs/0034-persona-conversational-working-memory.md`](rfcs/0034-persona-conversational-working-memory.md) ([Phase 2 PR plan](rfcs/0034-phase2-pr-plan.md)) · [`docs/rfcs/0030-multi-agent-conversation-governance.md`](rfcs/0030-multi-agent-conversation-governance.md) (the [relevance-gated-response amendment](rfcs/0030-amendment-relevance-gated-response.md) + its [PR plan](rfcs/0030-amendment-relevance-gated-response-pr-plan.md)) · [`docs/rfcs/0031-amendment-person-identity-cross-room-tier.md`](rfcs/0031-amendment-person-identity-cross-room-tier.md) (the F-7 Option D identity migration) · [`docs/rfcs/0049-memory-consolidation-gradient.md`](rfcs/0049-memory-consolidation-gradient.md) (design ratified docs-only; code → v0.4.0)

---

## 1. Pre-release Verification

All build gates run on a clean checkout, executed by PR 3 at the new version and re-certified live by PR 4 on the post-version-bump tip. PR 1 already ran the **full automated suite green live on this host** on the `92a5a00` RC tip (Go 1.26.3 + Node 22.16 + Docker on host — so the Go gates ran live, not carried to CI): the directedness receiver-gate + Go candidate-set parity + disposition-normalization gates, the RFC 0034 Phase 2 conversation-window suite, the identity-migration suite, the full Go internal suite `-race -cover`, the session/epoch isolation gates, and a live Docker stack on Anthropic — see the [execution report § Structural / Automated Gates](manual-tests/v0.3.7-execution-report.md).

- [ ] `make test` green on the post-bump tip — the full **Go `./internal/... -race -cover` suite** (all packages `ok`, no data races) incl. the directedness candidate-set parity + disposition-normalization gates (next rows); the session + epoch structural-isolation gates (**20 passed**); `cargo test`; the **Vitest** UI suite; the full Python unit suite incl. the **identity-migration suite** (v12→v14 + backfill). PR 1 ran the same suite live on `92a5a00` — per-gate detail in the [execution report § Structural / Automated Gates](manual-tests/v0.3.7-execution-report.md).
- [ ] `make lint` clean — ruff + clippy live by PR 4; golangci-lint + mypy carried by CI.
- [ ] `make validate` passes — all configs valid + `instructions_file` refs resolve; the `config/channels.yaml` disposition vocabulary validates against [`schemas/channel.schema.json`](../schemas/channel.schema.json) (incl. the reserved/no-op `threshold` field). Doc-audit (`doc_audit.py`) **0 violations**.
- [ ] Proto staleness clean — carried by CI (`protoc` not on host). v0.3.7 touches **no `.proto`** (Tier A keys on the existing RFC 0011 `mentions` payload), so no drift is introduced; CI confirms `git diff --exit-code` clean.
- [ ] `make check-licenses` clean — Python + Rust (`cargo deny`) run live by PR 4; Go (`go-licenses`) carried by CI.
- [ ] `make notices` up to date — **delta-free by construction**: v0.3.7 adds **no third-party production runtime dependency** (orchestrator-/agent-side behaviour: response-gate admission, conversation-window assembly, a prompt snippet, a persona-memory column + backfill). Commit any genuine Dependabot delta.
- [ ] `make generate-sanitizer-patterns-check` passes — Go↔Python sanitizer-pattern parity in sync.
- [ ] **Directedness structural gates** — **primary release-blocker (conversation governance)**: the receiver-gate unit suites (`tests/unit/python/test_response_gate_relevance.py` + `test_response_gate_disposition.py` — directed-elsewhere suppresses other `participant`s; mentioned admitted; open-floor admits all; `@everyone` disables the filter; disposition aliases resolve to legacy constants) **and** the Go candidate-set parity + disposition-normalization gates (`go test ./internal/channels/` — `TestOrderResponders_DirectedElsewhere`, `TestOrderResponders_BroadcastDisablesDirectedFilter`, `TestLoadConfig_NormalizesDispositionVocabulary`, `TestSQLiteStore_*_NormalizesDisposition`). Ran green live by PR 1 on `92a5a00`.
- [ ] **RFC 0034 Phase 2 conversation-window gate** — `tests/integration/test_conversational_continuity.py` (group-channel per-peer `[<peer_id>]: ` prefix + cross-peer substrate + the `(channel_id, limit)` fetch-cache-key shape). Green live by PR 1.
- [ ] **Identity-migration gate (release-gate)** — the persona-memory `MIGRATIONS` head is **v14** (13 = relationship `identity` column, 14 = pre-cutover contact-note backfill); `pytest tests/unit/python -k "migration_identity or identity_backfill"` + the full migration suite green (forward-migrate v12→v14, backfill correctness, crash-replay safety). Green live by PR 1.
- [ ] **F-8 inbound `@everyone` carve-out gate** — `tests/unit/python/test_channel_validation.py` + `tests/integration/test_channel_relevance.py` (`test_broadcast_passes_inbound_validation_then_admits` — the full validate→gate chain a broadcast traverses) + the CLI `cargo test … expand_mentions validate_send_inputs_accepts_everyone_sentinel`. Green live by PR 1 ([ISSUE-0094](issues/ISSUE-0094-everyone-broadcast-rejected-by-agent-inbound-validation.md) resolved).
- [ ] **Alias cost-attribution gate** (carry-forward) — `TestCostSummary_AliasRoutedAgent_ReportsNonZeroCost` (`internal/server`) + `TestLoadCostConfig_ShippedConfig_PricesAliasPhysicalModels` (`internal/cost`). Green live by PR 1.
- [ ] **Session + epoch structural-isolation gates** (carry-forward) — the five `tests/integration/test_{session,epoch}_*.py` isolation gates — **20 passed** live by PR 1.
- [ ] **Cost-regression — uninvolved persona costs zero** — re-asserts the RFC 0023/0024 "uninvolved persona costs zero" invariant: Tier A is **free** (no new LLM-call origin — suppressed `participant`s make **0** provider calls on a directed turn). Confirmed live by PR 1 (0 Anthropic calls for the two suppressed personas) + `test_response_gate_relevance::test_participant_not_mentioned_is_suppressed_when_message_directed`.
- [ ] **Persona-memory upgrade-on-open check (release-gate)** — start from a pre-v0.3.7 (≤ v12) persona-memory DB, open it on the post-bump binary, and confirm it forward-migrates cleanly to **v14** — relationship `identity` column present, pre-cutover `contact:<id>` notes backfilled onto the relationship tier, no data loss; an already-v14 DB is a no-op open. Confirm a legacy `config/channels.yaml` using `respond_policy: always|when_mentioned|never` loads unchanged. **Forward-only — no rollback path.** (PR 4.)
- [ ] Docker smoke test — confirming on a clean clone with no host toolchain (PR 4 re-runs on the exact tag tip):
  - [ ] **Multi-persona group channel — a directed `@`-question draws exactly one reply** (the other personas silent, zero-cost). (PR 1 ran live on `92a5a00`; carried.)
  - [ ] **An open question yields ordered, mutually-aware replies** that reference peers by name (working memory + floor control + peer voice) — reads as colleagues, not assistants. (PR 1, carried.)
  - [ ] **An `@everyone` broadcast draws all responders** (the directed filter disabled) via both the REST `@everyone` wire shape and the `channel send --mention-all` CLI path. (PR 1, carried — the F-8 fix.)
  - [ ] **A DM turn still completes** (no regression to the v0.3.6 conversation surface). (PR 1, carried.)
  - [ ] **The persona-memory store opens / migrates in-image without error** (v14 applied at boot). (PR 1, carried.)

> **Environment note.** The PR-1 host carried the **full toolchain** (Go 1.26.3 `darwin/arm64`, Node 22.16, Docker 29.5.2) **and a valid Anthropic key** — so both headline MTs ran live on Anthropic and the Go structural gates ran live on host (not carried to CI). v0.3.7 ships **no web-console change**, so the SPA render legs are out of scope for this surface. See [execution report § Environment](manual-tests/v0.3.7-execution-report.md#environment).

---

## 2. Version Alignment

Before tagging, all shipped components must declare `0.3.7` consistently. All components are at `0.3.6`.

| Component | File | Required state for release |
|-----------|------|----------------------------|
| Python agents | `agents/pyproject.toml` | `version = "0.3.7"` |
| Python observability | `agents/observability/{metrics,tracing}.py` | `_DEFAULT_SERVICE_VERSION = "0.3.7"` (both) |
| Rust CLI | `cli/Cargo.toml` | `version = "0.3.7"` |
| Rust lockfile | `cli/Cargo.lock` | Regenerated after the CLI version bump |
| Go orchestrator service-version fallback | `internal/server/ui_handlers.go` (`const defaultServiceVersion`) | `defaultServiceVersion = "0.3.7"` |
| Go orchestrator | git tag | Release identified by `v0.3.7` tag |

`scripts/bump_version.py` bumps the `pyproject.toml`, `Cargo.toml`, observability `service.version` strings, and the Go `defaultServiceVersion`; `cli/Cargo.lock` is then regenerated via `cd cli && cargo update --workspace`. The observability defaults and the Go `defaultServiceVersion` are not build inputs, so a stale value never fails `make all` — they are bumped explicitly. The `web/package.json` JS workspace stays at `0.0.0` **by design** (not a release-versioned artifact; the bundle ships inside the Go binary, and v0.3.7 ships no web-console change).

Checklist (filled in by release-prep plan PR 3; PR 4 confirms on the post-bump tip):

- [ ] `agents/pyproject.toml` → `0.3.7` (PR 3)
- [ ] `agents/observability/{metrics,tracing}.py` → `_DEFAULT_SERVICE_VERSION = "0.3.7"` (both)
- [ ] `cli/Cargo.toml` → `0.3.7`; `cli/Cargo.lock` regenerated (`cargo update --workspace`)
- [ ] `internal/server/ui_handlers.go` → `const defaultServiceVersion = "0.3.7"`
- [ ] `make all` + `make ui` build at the new version
- [ ] `web/package.json` intentionally **not** bumped (stays `0.0.0`)
- [ ] No docs describe v0.3.7 as unreleased WIP — **PR 4** flips the ROADMAP Version Map row, the README Roadmap row, this checklist's status, and the release-prep-plan status to released; the Phase 4 post-release follow-up then cuts the `v0.3.7` tag, publishes the GitHub Release, and backfills the post-tag artifacts (tag link, Release URL)

---

## 3. Changelog

`CHANGELOG.md` **already has a populated `[0.3.7] - Unreleased` section** ([CHANGELOG.md:5](../CHANGELOG.md)) — a two-row Upgrade Notes table (the `respond_policy → disposition` reframe; the addressing-aware directedness fix) and three `🚀 Features` bullets, drafted as the implementation PRs landed. Release-prep PR 3 **dates it into a tagged `[0.3.7]`** (temp-file-and-merge to preserve the curated prose) and backfills PR ranges per [§RFC PR roll-up](v0.3.7-release-prep-plan.md#rfc-pr-roll-up). `[0.3.6]` and prior are preserved verbatim.

Checklist:

- [ ] `CHANGELOG.md` contains a dated `[0.3.7]` section (PR 3); `[Unreleased]` removed or emptied
- [ ] The existing curated `[0.3.6]` … `[0.1.0]` sections are preserved verbatim — not overwritten (temp-file-and-merge if `git-cliff` would overwrite curated content)
- [ ] The `[0.3.7]` section reshapes the `[Unreleased]` prose into:
  - **Highlights** — the realism trio: RFC 0034 Phase 2 group working memory + RFC 0030 relevance Tier A directedness + the peer-voice prompt ("a group channel reads like colleagues, not bots").
  - **Features** — RFC 0034 Phase 2 ([#533](https://github.com/mkhomutov/Persatrix/pull/533)–[#535](https://github.com/mkhomutov/Persatrix/pull/535)); RFC 0030 relevance Tier A + the `respond_policy → disposition` reframe ([#536](https://github.com/mkhomutov/Persatrix/pull/536), [#537](https://github.com/mkhomutov/Persatrix/pull/537), closeout [#557](https://github.com/mkhomutov/Persatrix/pull/557)); the peer-voice prompt ([#538](https://github.com/mkhomutov/Persatrix/pull/538)); the F-7 Option D person-identity tier + migration ([#549](https://github.com/mkhomutov/Persatrix/pull/549)–[#556](https://github.com/mkhomutov/Persatrix/pull/556)).
  - **Documentation** — README/ROADMAP + the channels/persona guide verification + this checklist (PR 2); the MT execution report ([#561](https://github.com/mkhomutov/Persatrix/pull/561), re-run [#564](https://github.com/mkhomutov/Persatrix/pull/564)); the **RFC 0049 design ratification** ([#559](https://github.com/mkhomutov/Persatrix/pull/559)/[#560](https://github.com/mkhomutov/Persatrix/pull/560) — docs-only, code → v0.4.0); plans.
  - **Testing** — the 2 headline MTs (`MT-CHANNEL-RELEVANCE-001` / `MT-PERSONA-CONVERSATION-002`) + the combined realism walkthrough; the directedness + conversation-window + identity-migration structural gates; the carried-forward alias + session/epoch + cost-regression gates.
  - **Bug Fixes** — the F-1…F-7 conversation test-findings cluster ([#540](https://github.com/mkhomutov/Persatrix/pull/540)–[#548](https://github.com/mkhomutov/Persatrix/pull/548)) + the F-8 `@everyone` broadcast fix ([#562](https://github.com/mkhomutov/Persatrix/pull/562)/[#563](https://github.com/mkhomutov/Persatrix/pull/563), [ISSUE-0094](issues/ISSUE-0094-everyone-broadcast-rejected-by-agent-inbound-validation.md) resolved).
  - **Upgrade Notes** — see §3.1 below.

Suggested generation command:

```bash
git-cliff --tag v0.3.7 --unreleased --prepend CHANGELOG.md
```

The curated Highlights + Upgrade Notes are **already written** in `[0.3.7] - Unreleased`, so the likely path is a manual rename + date-stamp (temp-file-and-merge to avoid clobbering curated prose) rather than a clean `git-cliff` regenerate.

### 3.1 Required Upgrade Notes in Changelog

This is the canonical source for Upgrade Notes text (PR 3 reconciles it with the table already in `[0.3.7] - Unreleased`).

| Notable change | Detail |
|----------------|--------|
| **[Config — additive, back-compat]** `respond_policy → disposition` vocabulary | A channel member's `respond` field gains the **disposition vocabulary** — `participant` (joins open-floor rounds and directed turns), `addressed` (replies only when `@`-mentioned), `observer` (never auto-replies) — normalized from the legacy `always` / `when_mentioned` / `never` at the Go config-load boundary (`always→participant`, `when_mentioned→addressed`, `never→observer`). **Existing `config/channels.yaml` files using the legacy values load unchanged.** A reserved per-disposition `threshold` field ships **no-op** (an additive seam for the v0.3.8 Tier-B salience bid; nothing reads it in v0.3.7). See the [channels guide](guides/channels.md). |
| **[Behaviour — directedness]** Addressing-aware response gate (no pile-on) | A directed `@`-mention is answered by **exactly the addressee** — other `participant`s are suppressed with reason `directed_elsewhere` and **cost zero tokens** (no new LLM-call origin). An un-addressed open-floor message still admits **all** `participant`s, and an explicit **`@everyone` broadcast disables the directed filter** (all responders reply). Directedness keys on **structured `@`-mentions only** (the existing RFC 0011 `mentions` payload); natural-language addressing is a v0.3.8 Tier-B signal. Directly fixes the v0.3.6 "everyone answers everything" behaviour. See the [channels guide](guides/channels.md). |
| **[Migration — forward-only, upgrades on open]** Persona-memory v12→v14 (person identity on the cross-room tier) | The persona-memory schema (`agents/memory/migrations.py`) advances from **v12** to **v14**: migration 13 adds a nullable `identity TEXT` (JSON) column to the `relationships` table so person identity (name / role / stable preferences) lives on the genuinely cross-room relationship tier (F-7 Option D); migration 14 backfills pre-cutover `contact:<id>` notes by parsing their identity onto that column, so identity learned before the cutover is not stranded. **Upgrades automatically on first open**, no operator action, no data loss; an already-v14 DB is a no-op open. **There is no rollback path — forward-only**, per the established migration contract. The channel store (`channels.db`) stays at schema **v6** (no channel migration this release). See the [RFC 0031 person-identity-cross-room-tier amendment](rfcs/0031-amendment-person-identity-cross-room-tier.md). |

The group-channel working memory (per-peer `[<peer_id>]: ` attribution) and the peer-voice prompt are behaviour-improving and noted in Highlights, not Upgrade-gating.

---

## 4. Manual Test Sign-off

Release sign-off requires the v0.3.7 surface executed and recorded in [`docs/manual-tests/v0.3.7-execution-report.md`](manual-tests/v0.3.7-execution-report.md) — the **2 headline MTs** (authored during implementation) + the **combined realism walkthrough** (authored-and-executed in PR 1) + the carried-forward v0.3.6 surface, regression-checked against the v0.3.7 RC tip. The authoritative test index is [`docs/manual-tests/README.md`](manual-tests/README.md).

### Primary v0.3.7 Gates

| Area | Test ID |
|------|---------|
| Relevance gate Tier A — addressing-aware directedness: a directed `@`-mention → exactly one reply; open-floor → all `participant`s; `@everyone` → the directed filter disabled (all reply); suppressed members cost zero. Directly re-runs the v0.3.6 "everyone answers everything" repro and asserts it fixed — **primary v0.3.7 gate** | `MT-CHANNEL-RELEVANCE-001` |
| Persona conversational continuity (group channel) — per-peer attributed transcript; named-peer attribution + cross-peer referential (pronoun) follow-up resolved off the running transcript — **headline continuity test** (RFC 0034 Phase 2) | `MT-PERSONA-CONVERSATION-002` |
| Combined realism walkthrough — directedness + a mutually-aware ordered open-floor round naming peers + the qualitative "colleagues, not assistants" read in one session — **the peer-voice acceptance home** | (authored-and-executed in PR 1) |

### Results Summary (from execution report, `92a5a00` RC tip)

**Clean pass** — re-executed live on the post-fix tip after the F-8 (`@everyone` broadcast) fix landed. Every v0.3.7 headline gate step passes live on Anthropic; every structural release-blocker gate is green; the carried-forward v0.3.6 surface is regression-clean (one carried `⚠️ Accepted-with-known-gap` — `MT-MEMORY-005`, unchanged from v0.3.6). **0 Fail, 0 Not run** on the v0.3.7 surface.

Checklist:

- [x] [`docs/manual-tests/v0.3.7-execution-report.md`](manual-tests/v0.3.7-execution-report.md) exists (release-prep PR 1 — [#561](https://github.com/mkhomutov/Persatrix/pull/561), re-run on the post-fix tip [#564](https://github.com/mkhomutov/Persatrix/pull/564))
- [x] Each test records date, tester, environment, and evidence
- [x] `MT-CHANNEL-RELEVANCE-001` recorded `Pass` (**live, Anthropic**) — all five steps + the idle-cost invariant: directed → 1 reply, open-floor → all `participant`s, `@everyone` → all (D3, fixed), suppressed members made **0** provider calls; verified via both the REST `@everyone` wire shape and the `channel send --mention-all` CLI path
- [x] `MT-PERSONA-CONVERSATION-002` recorded `Pass` (**live, Anthropic**) — both legs: named-peer attribution + cross-peer pronoun binding off the per-peer attributed transcript
- [x] The combined realism walkthrough captured (**live**) — directed → one reply; ordered open round naming peers + converging; colleague voice
- [x] The directedness + conversation-window + identity-migration structural gates green on the RC tip
- [x] The carried-forward alias cost-attribution + session/epoch structural-isolation + cost-regression ("uninvolved persona costs zero") gates green on the RC tip
- [x] Every `Accepted-with-known-gap` row links a tracked issue or follow-up bullet

> The release-blocking finding from the **first** execution (`65303d7`) — `MT-CHANNEL-RELEVANCE-001` Step 4, the `@everyone` broadcast dropped by the agent inbound validator (**F-8**) — was fixed before this checklist: [#562](https://github.com/mkhomutov/Persatrix/pull/562) (inbound carve-out + drift-guard + delivery-chain test) + [#563](https://github.com/mkhomutov/Persatrix/pull/563) (CLI `--mention-all` emits the sentinel); [ISSUE-0094](issues/ISSUE-0094-everyone-broadcast-rejected-by-agent-inbound-validation.md) closed `2026-06-06`. The report's remaining findings are non-gating doc/observation follow-ups: **F-9** (the MT recipe Step 1 describes the config-file disposition view, not the normalized REST view — recipe-staleness, low) and **F-10** (the publish path is synchronous through the floor round — observation; tracked against RFC 0034 Phase 3 / the floor-round budget, a v0.3.8 item).

---

## 5. Tag and GitHub Release Procedure

Use a clean `main` checkout after the final release-prep PR (PR 4 of the prep plan) merges.

```bash
git checkout main
git pull --ff-only origin main
git tag -a v0.3.7 -m "v0.3.7 — Conversations worth watching"
git push origin v0.3.7
```

GitHub Release checklist (post-merge manual steps after PR 4 merges, by [v0.3.7-plan Phase 4](v0.3.7-plan.md#phase-4--tag-and-post-release-follow-up)):

- [ ] Create the GitHub Release from tag `v0.3.7`
- [ ] Use the curated `[0.3.7]` changelog section as the release notes baseline
- [ ] Include upgrade notes (§3.1 — incl. the identity migration) in the release body
- [ ] Include known gaps (§6) in the release body
- [ ] Attach binaries or container references if produced for this release — none expected (matches the v0.3.6 / v0.3.5 / v0.3.4 … pattern)

---

## 6. Known Gaps to Document in Release Notes

These items remain intentionally incomplete and should be described as deferred scope, not regressions. Seeded from [v0.3.7-release-prep-plan §Out of scope](v0.3.7-release-prep-plan.md) and [§Known follow-up issues](v0.3.7-release-prep-plan.md#known-follow-up-issues).

### v0.3.7-scope gaps

| Area | Description | Deferred to |
|------|-------------|-------------|
| RFC 0030 relevance **Tier B** deferred | The cheap `fast`-model salience bid, the `chair` disposition, natural-language recipient parsing, and per-disposition salience-threshold calibration are not built. The per-disposition `threshold` schema field ships **reserved/no-op**; nothing reads it in v0.3.7. Tier B hard-depends on the in-round transcript RFC 0034 Phase 2 lands here but consumes only in v0.3.8. | v0.3.8 |
| RFC 0030 **Layers 1/2/4** (reply budget, cost ceiling, end-of-interaction) | Tier A is eligibility-only; convergence/termination is the brainstorm rung. The moderator / bid-and-select **Layer 5** is v0.4.0. | v0.3.8 (Layers 1/2/4); v0.4.0 (Layer 5) |
| Directedness keys on **structured `@`-mentions only** | Natural-language addressing ("only to Iron Fox") is a v0.3.8 Tier-B salience signal, not a v0.3.7 hard filter. | v0.3.8 |
| RFC 0034 **Phase 3** deferred | Participant-count-aware conversation-window sizing, the in-process fetch-cache **LRU bound** (currently unbounded-per-process), and telemetry-driven calibration. RFC 0034 stays `⚠️ Partially Implemented` (Phases 1–2). The synchronous-publish-through-the-floor-round observation (F-10) is tracked here. | v0.3.x |
| RFC 0049 **consolidation-gradient implementation** deferred | v0.3.7 ratifies the **design** (the gradient, the one law *scope = f(consolidation level)*, the §D re-rooting of topic-knowledge scope) + four amendment stubs, **docs-only**. The code — cross-room L2 fact recall, the cross-scope consolidation pump, decisions-as-readable-memory — is gated on the RFC 0037 confidentiality keystone (Phase 0). RFC 0049 stays `📋 Proposed`. | v0.4.0 |
| MT recipe doc follow-up (F-9) | `MT-CHANNEL-RELEVANCE-001` Step 1's expected output describes the config-file disposition vocab; the REST surface reports the normalized legacy constants. Behaviour is correct — a one-line recipe note is the only fix. | Doc fast-follow |

### Carried forward from earlier releases

The v0.3.6-scope and earlier gaps remain open in v0.3.7 unchanged — RFC 0048 Slices 2–5 (memory inspector / isolation verifier / cost / control plane, Slice 5 hard-gated on [RFC 0039](rfcs/0039-user-accounts-authentication.md) auth), channel real-time push + chat token streaming, floor-control responder-per-round cap (D4) + single-replica/config-only opt-out (D5), and the further-back v0.3.5/v0.3.4 gaps (subject-scoped facts [ISSUE-0084](issues/ISSUE-0084-fact-scope-by-subject-not-uniform-session.md), the `--all-sessions` recall verb [ISSUE-0086](issues/ISSUE-0086-operator-all-sessions-recall-verb.md), the unauthenticated channels/REST surface closed structurally by RFC 0039, RFC 0029 Phases 2–6 Postgres backend, RFC 0009 Phases 3–4). The v0.3.7 release notes link the [v0.3.6](v0.3.6-release-checklist.md#6-known-gaps-to-document-in-release-notes) / [v0.3.5](v0.3.5-release-checklist.md#6-known-gaps-to-document-in-release-notes) checklists for full detail rather than re-listing them.

---

## 7. Summary Checklist

Single-page go/no-go gate. All items must be checked before tagging.

```text
[ ] make test green on the post-bump RC tip — Go ./internal/... -race -cover all pkgs ok no races, incl. directedness candidate-set parity + disposition normalization; session/epoch isolation 20; Rust + Vitest; Python unit incl. identity-migration suite (v12→v14 + backfill) — 0 feature fail; full Python integration carried by CI
[ ] make lint clean — ruff + clippy live by PR 4; golangci-lint / mypy carried by CI
[ ] make validate passes — configs valid + instructions_file refs; channels.yaml disposition vocab vs schemas/channel.schema.json; doc-audit 0 violations
[ ] make proto then git diff --exit-code shows no generated drift (carried by CI; no .proto change this release)
[ ] make check-licenses — Python + Rust cargo deny ok live by PR 4; Go go-licenses carried by CI
[ ] THIRD_PARTY_NOTICES.md up to date, no genuine delta (no new prod runtime dep)
[ ] make generate-sanitizer-patterns-check passes (Go↔Python parity in sync)
[ ] agents/pyproject.toml updated to 0.3.7
[ ] agents/observability/{metrics,tracing}.py service.version defaults updated to 0.3.7
[ ] cli/Cargo.toml updated to 0.3.7
[ ] cli/Cargo.lock regenerated and committed
[ ] internal/server/ui_handlers.go defaultServiceVersion updated to 0.3.7
[ ] web/package.json intentionally NOT bumped (stays 0.0.0 — bundle ships inside the Go binary)
[ ] make all + make ui build successfully at 0.3.7
[ ] CHANGELOG.md contains a curated, dated [0.3.7] section with the three upgrade notes (§3.1)
[ ] Existing [0.3.6] … [0.1.0] sections preserved verbatim
[x] Both v0.3.7 headline MTs + the combined realism walkthrough + the carried-forward v0.3.6 surface recorded in docs/manual-tests/v0.3.7-execution-report.md (clean pass; 0 Fail / 0 Not run on the v0.3.7 surface)
[x] MT-CHANNEL-RELEVANCE-001 recorded Pass LIVE on Anthropic (directed→1, open→all, @everyone→all; suppressed members zero-cost) — primary v0.3.7 gate
[x] MT-PERSONA-CONVERSATION-002 recorded Pass LIVE on Anthropic (named-peer attribution + cross-peer pronoun binding) — headline continuity test
[x] Combined realism walkthrough captured LIVE (one reply to directed + mutually-aware open round + colleague voice)
[x] Directedness + conversation-window + identity-migration structural gates green on the RC tip
[x] Alias cost-attribution + session/epoch isolation + cost-regression (uninvolved persona costs zero) gates green on the RC tip
[ ] Persona-memory upgrade-on-open verified — pre-v0.3.7 DB → v14 clean (identity column + backfill, no data loss); already-v14 DB no-op; legacy respond_policy config loads unchanged (PR 4, release gate)
[ ] Docker smoke — directed→one reply + ordered mutually-aware open round + @everyone→all (REST + --mention-all) + DM turn + in-image v14 migration (PR 1 ran live on 92a5a00; PR 4 re-runs on the exact tag tip)
[ ] Known gaps documented for release notes (§6 above)
[ ] git tag v0.3.7 created and pushed (Phase 4)
[ ] GitHub Release published with changelog, upgrade notes, and known gaps (Phase 4)
```
