# v0.3.5 Release Checklist

> **Status**: ✅ Released (release-prep complete) — final pre-tag verification by [PR 4](v0.3.5-release-prep-plan.md#pr-4--final-pre-tag-verification--release-notes) re-certified the gate sweep on the post-version-bump RC tip (`c3d0a0d`); the Version Map v0.3.5 row is flipped `🚧 Release prep` → `✅ Released`. PR 4 also installed Go 1.26.3 and ran the Go gates live, and ran the **full `MT-MEMORY-005` (V5) dementia arc live on Anthropic `claude-sonnet-4-6`** — so **both §4 live hard-blocks are cleared**: the Go alias cost-attribution gate (both tests PASS) and the qualitative dementia arc (all 5 legs Pass + the multi-session no-bleed re-run holds). The `v0.3.5` git tag + GitHub Release are now the **only** remaining [Phase 6](v0.3.5-plan.md#phase-6--tag-and-post-release-follow-up) post-merge steps. Created by [release-prep plan PR 2](v0.3.5-release-prep-plan.md#pr-2--readme--roadmap--sessionsepochs-guide-verification--release-checklist); §1/§2 executed by [PR 3](v0.3.5-release-prep-plan.md#pr-3--version-bump--changelog-curation) and re-certified by PR 4. The §4 manual-test sign-off references the [MT execution report](manual-tests/v0.3.5-execution-report.md) landed by [PR 1](https://github.com/mkhomutov/Persatrix/pull/488) (and its [PR 4 re-execution appendix](manual-tests/v0.3.5-execution-report.md#re-execution--release-prep-pr-4-post-version-bump-rc-tip)).

> v0.3.5 — "Session-Scoped Memory" — ships [RFC 0031](rfcs/0031-per-session-namespacing-channels.md) Phases 2–4 (session-scoped persona-memory recall + the `persatrix session …` operator CLI) plus the **epoch** run-isolation axis ([ISSUE-0085](issues/ISSUE-0085-epoch-axis-run-isolation.md)), with [RFC 0033](rfcs/0033-model-alias-layer.md) Phase 3 (the alias-layer closeout) as a conditional co-resident. The user-facing promise: *persona-memory recall is session-scoped — a run reads only its own session's (room's) rows (plus the always-visible `legacy` carve-out), so concurrent conversations don't bleed; a session is room continuity, not a clean slate, so to re-run a scenario in a fresh world that inherits nothing (same room and user) bump the `epoch` run-isolation axis instead of nuking volumes with `make reset`.* This closes the **F-3** cross-run state bleed at the root (the *recall* half via session scoping, the *structural* half via epoch strict-equality isolation).

**Based on test report**: [`docs/manual-tests/v0.3.5-execution-report.md`](manual-tests/v0.3.5-execution-report.md)
**Release prep plan**: [`docs/v0.3.5-release-prep-plan.md`](v0.3.5-release-prep-plan.md)
**Master plan**: [`docs/v0.3.5-plan.md`](v0.3.5-plan.md)
**RFCs**: [`docs/rfcs/0031-per-session-namespacing-channels.md`](rfcs/0031-per-session-namespacing-channels.md) ([Phase 2](rfcs/0031-phase2-pr-plan.md) / [Phase 3](rfcs/0031-phase3-pr-plan.md) / [epoch](rfcs/0031-epoch-pr-plan.md) PR plans) · [`docs/rfcs/0033-model-alias-layer.md`](rfcs/0033-model-alias-layer.md) (Phase 3)

---

## 1. Pre-release Verification

All build gates run on a clean checkout of the release-candidate commit, executed by PR 4 of the release-prep plan on the post-version-bump tip. PR 1 already ran the full automated suite green on the `3ceb400` RC tip (session + epoch structural-isolation gates, 893 session/epoch/alias/facts Python unit, 153 Rust, live Docker stack — see the [execution report § Automated Test Suites](manual-tests/v0.3.5-execution-report.md#automated-test-suites-live-on-the-rc-tip-3ceb400)). The Go-only gates (the alias cost-attribution gate, the Go race/cover suite) are carried by **CI** (which builds Go) in the PR 1 environment and re-confirmed live by PR 4.

- [x] `make test` green on the post-bump tip — **PR 4 ran live on this host** (Go 1.26.3 installed): the **Go `./internal/... -race -cover` suite — all packages `ok`, no data races**; the session + epoch structural-isolation gates (**20 passed**); the session/epoch/facts/alias/recall unit slice (**640 passed**); the full Python unit suite (**2887 passed, 5 skipped** — + 5 host-artifact fails from the bare-`python` binary absent on this host, F-5, CI green); `cargo test` (**153 passed**). The full Python integration suite is carried by CI.
- [x] `make lint` clean — ruff (agents + `tests/`) clean and clippy clean on `persatrix v0.3.5` re-run locally by PR 4; golangci-lint + mypy whole-package carried by CI.
- [x] `make validate` passes — re-run by PR 4: 5 configs valid; 3 `instructions_file` refs resolve. (Doc-link integrity also clean: 6464 links / 324 markdown files.)
- [x] Proto staleness clean — carried by CI (`make proto` needs `protoc` + the version-pinned protoc plugins, not installed on this host — version-sensitive, prone to false drift). This PR touches **no `.proto`**, so no drift is introduced; CI on the RC tip confirms `git diff --exit-code` clean.
- [x] `make check-licenses` clean — PR 4 ran live: **Go (`go-licenses`) OK**, Python (68 packages, all in allow-list), Rust (`cargo deny check licenses` → `licenses ok`).
- [x] `make notices` up to date — **no genuine delta**. This PR adds no third-party production dependency (docs + version strings only). Local `--check` flagged a delta, but regeneration produced **only environment artifacts** (macOS `go-licenses` reports `golang.org/x/sys/unix` vs CI Linux's `…/windows`; the local `.venv` resolves newer package versions than the CI-locked set) — **reverted**; the CI-canonical committed notices stands.
- [x] `make generate-sanitizer-patterns-check` passes — **run live by PR 4** (`go run ./cmd/genpatterns`): Go↔Python parity in sync (11 patterns, 5 sources, 2 actions).
- [x] **Session + epoch structural-isolation gates** — **primary release-blocker**, **re-ran green on the post-bump tip by PR 4** (and originally on the `3ceb400` RC tip — [execution report § Automated Test Suites](manual-tests/v0.3.5-execution-report.md#automated-test-suites-live-on-the-rc-tip-3ceb400)): [`test_session_recall_isolation.py`](../tests/integration/test_session_recall_isolation.py), [`test_epoch_run_isolation.py`](../tests/integration/test_epoch_run_isolation.py), [`test_session_continuity.py`](../tests/integration/test_session_continuity.py), [`test_session_emission_isolation.py`](../tests/integration/test_session_emission_isolation.py), [`test_session_id_cross_process.py`](../tests/integration/test_session_id_cross_process.py) — **20 passed** (the env-gated `test_session_operator_surface.py` `requires_orchestrator` skips need built Go binaries).
- [x] **Alias cost-attribution gate** (carry-forward from v0.3.4) — **RUN LIVE by PR 4** (Go 1.26.3 installed): `TestCostSummary_AliasRoutedAgent_ReportsNonZeroCost` (`internal/server`) + `TestLoadCostConfig_ShippedConfig_PricesAliasPhysicalModels` (`internal/cost`) both **PASS**. This clears the §4 Go-alias-cost-gate hard-block live.
- [x] **Bored-persona `cost-regression-gate`** (carry-forward from v0.3.3) — `tests/integration/test_bored_persona_cost.py` **re-ran green on the post-bump tip by PR 4 (1 passed)**.
- [x] Docker smoke test — **carried forward live from the PR 1 `3ceb400` run** (the [execution report § Stack build](manual-tests/v0.3.5-execution-report.md#stack-build-mt-integration-001-live) ran this live, migrations v4→v12, 11 services healthy); the post-bump delta ([#489](https://github.com/mkhomutov/Persatrix/pull/489)/[#490](https://github.com/mkhomutov/Persatrix/pull/490)) changes no provider/routing/demo runtime:
  - [x] **Session-scoped recall** — a default `make demo-*` agent completes a turn under one session; a within-session arc continues, and a run under a *fresh* session does not surface the prior session's rows (the F-3 recall closer). ([MT-SESSION-003](manual-tests/MT-SESSION-003.md), live on `3ceb400`.)
  - [x] **Fresh-epoch rerun** — a rerun under a fresh `PERSATRIX_EPOCH` (or `--epoch`), **same room + same `--user`**, inherits no episodes / relationship trust / person-facts — the structural F-3 fix, live. ([MT-EPOCH-001](manual-tests/MT-EPOCH-001.md), live on `3ceb400`.)
  - [x] **Back-compat `legacy`/`live` default path** — a pre-v0.3.5 database auto-migrates on first open (persona-memory v12, channel-store v6) and existing rows surface from the default `session_id='legacy'` / `epoch_id='live'` resolution path. (Migrations v4→v12 confirmed live on `3ceb400`.)
  - [x] **RFC 0033 Phase 3 fail-loud** — a raw vendor `model:` (not a declared alias) in a config is **rejected at resolve** with a `SystemExit` (schema `"0.3"`). (Unit-asserted in the alias slices; code-evident.)

> **Environment note (carry-forward + F-3/F-4 from the v0.3.5 MT run).** The PR 1 execution environment had no Go toolchain on the host (orchestrator compiled inside its Docker image) and an invalid Anthropic key, so the live stack ran on the OpenAI provider (config-driven, RFC 0033 — provider-independent for the session/epoch surface). The Go-only gates and the full qualitative dementia arc were carried by their automated counterparts; PR 4 should run both live on a full Go-+-Anthropic-+-idle-window environment before tag. See [execution report § Environment & constraints](manual-tests/v0.3.5-execution-report.md#environment--constraints).

---

## 2. Version Alignment

Before tagging, all shipped components must declare `0.3.5` consistently. All components are at `0.3.4`.

| Component | File | Required state for release |
|-----------|------|----------------------------|
| Python agents | `agents/pyproject.toml` | `version = "0.3.5"` |
| Python observability | `agents/observability/{metrics,tracing}.py` | `_DEFAULT_SERVICE_VERSION = "0.3.5"` (both) |
| Rust CLI | `cli/Cargo.toml` | `version = "0.3.5"` |
| Rust lockfile | `cli/Cargo.lock` | Regenerated after the CLI version bump |
| Go orchestrator | git tag | Release identified by `v0.3.5` tag |

`scripts/bump_version.py` bumps the `pyproject.toml`, `Cargo.toml`, and observability `service.version` strings; `cli/Cargo.lock` is then regenerated via `cd cli && cargo update --workspace`. The observability defaults are not build inputs, so a stale value never fails `make all` — bump it explicitly.

Checklist (filled in by release-prep plan PR 3; PR 4 confirms on the post-bump tip):

- [x] `agents/pyproject.toml` → `0.3.5` (PR 3 [#490](https://github.com/mkhomutov/Persatrix/pull/490); PR 4 confirmed)
- [x] `agents/observability/{metrics,tracing}.py` → `_DEFAULT_SERVICE_VERSION = "0.3.5"` (both — PR 4 confirmed)
- [x] `cli/Cargo.toml` → `0.3.5`; `cli/Cargo.lock` regenerated — PR 4 re-built `cargo build --release` → `persatrix 0.3.5`
- [x] `make all` builds at the new version — CLI `cargo build --release` → `persatrix 0.3.5` (PR 4, local); the orchestrator (`go build`, inside its Docker image) is carried by CI on the RC tip
- [x] No docs describe v0.3.5 as unreleased WIP — PR 4 flips the ROADMAP Version Map row + Current phase/milestone, the README Roadmap row, this checklist's status, and the release-prep-plan status to released; only post-tag artifacts (tag link, Release URL) + the two §4 live hard-block legs remain for the Phase 6 follow-up.

---

## 3. Changelog

The release changelog must preserve every existing curated section in [`CHANGELOG.md`](../CHANGELOG.md) (`[0.3.4]` and prior) and add a new `[0.3.5]` section. `CHANGELOG.md` has **no `[Unreleased]` section** — the top entry is `[0.3.4]`. Release-prep PR 3 authors a fresh `[0.3.5]` section from `v0.3.4..HEAD` and reshapes into Highlights / Features / Documentation / Testing / Bug Fixes / Upgrade Notes.

Checklist:

- [ ] `CHANGELOG.md` contains a `[0.3.5]` section (PR 3)
- [ ] The existing curated `[0.3.4]` … `[0.1.0]` sections are preserved verbatim — not overwritten (temp-file-and-merge if `git-cliff` would overwrite curated content, as in prior releases)
- [ ] The `[0.3.5]` section includes:
  - **Highlights**: persona-memory recall is session-scoped (a run reads only its own session's/room's rows + the `legacy` carve-out), so concurrent conversations don't bleed; run/test isolation is the sibling `epoch` axis — a same-room/same-user rerun under a fresh `epoch` inherits nothing, replacing `make reset` as the run-isolation tool. Closes the F-3 cross-run state bleed at the root (recall half via session scoping, structural half via epoch).
  - **Features**: RFC 0031 Phase 2 (session-scoped default recall across all four persona-memory tiers), Phase 3 (the `persatrix session new/use/list/archive/current` operator CLI + `/api/v1/sessions` registry + the `--session` override + the env→file→flag resolution chain), Phase 4 (operator guides + [ISSUE-0051](issues/ISSUE-0051-per-session-memory-namespacing-channels.md) closed); the **epoch** run-isolation axis (`PERSATRIX_EPOCH` / `--epoch`, strict-equality `epoch_id` across all five tiers, [ISSUE-0085](issues/ISSUE-0085-epoch-axis-run-isolation.md) closed); RFC 0033 Phase 3 (raw-ID rejection + `_infer_provider`/`provider_inference` retirement + `schema_version` `"0.3"`). PR ranges per [release-prep plan §RFC PR roll-up](v0.3.5-release-prep-plan.md#rfc-pr-roll-up).
  - **Bug Fixes**: ISSUE-0068 ([#479](https://github.com/mkhomutov/Persatrix/pull/479)) — chat peer recorded with `participant_type` carried to the agent (the lone defect fix in the window).
  - **Documentation**: MT execution report (release-prep PR 1, [#488](https://github.com/mkhomutov/Persatrix/pull/488)), the sessions + epochs operator guides, the README/ROADMAP refresh + this checklist (release-prep PR 2), release-prep + master plans.
  - **Testing**: 3 new MTs (`MT-SESSION-002` / `MT-SESSION-003` / `MT-EPOCH-001`), the session + epoch structural-isolation gates, the re-run dementia-test continuity bridge, and the carried-forward alias cost-attribution + `cost-regression-gate`.
  - **Upgrade Notes** subsection (see §3.1 below)

Suggested generation command:

```bash
git-cliff --tag v0.3.5 --unreleased --prepend CHANGELOG.md
```

If `git-cliff` would overwrite curated content, generate into a temporary file and merge manually — same pattern as prior releases.

### 3.1 Required Upgrade Notes in Changelog

This is the canonical source for Upgrade Notes text (PR 3 copies it into `CHANGELOG.md`).

| Notable change | Detail |
|----------------|--------|
| **[Behaviour change — session-scoped default recall]** | Default persona-memory recall is now **session-scoped**: a run reads only the rows tagged with its active session, plus the always-visible `legacy` carve-out, across all four persona-memory tiers (episodes, relationships, facts, notes). Concurrent conversations no longer bleed into each other. Reaching across sessions (`sessions="*"`) is an explicit, library-only opt-in with no operator entry point (RFC 0031 §D). This is a behaviour change from the pre-v0.3.5 whole-store recall. |
| **[Feature]** `persatrix session …` operator CLI | A first-class session surface: `persatrix session new --label L [--activate] / use <id-or-label> / list [--include-archived] / current / archive <id-or-label>`, backed by the `/api/v1/sessions` REST registry. The active session resolves by precedence `--session` flag > `PERSATRIX_SESSION_ID` env > `~/.persatrix/active-session` pointer file > built-in `legacy` (RFC 0031 OQ #6). `legacy` is a reserved label; archive is one-way. See the [sessions operator guide](guides/sessions.md). |
| **[Feature]** Epoch run/test-isolation axis | A new `epoch` axis isolates a *same-named* rerun structurally: a fresh `PERSATRIX_EPOCH` (orchestrator boot env) or `--epoch <id>` (per-invocation override on `chat` / `channel send` / `channel reply`), default `live`, inherits **none** of a prior run's episodes / relationship trust / person-facts — strict-equality isolation with no `legacy` carve-out and no `*` wildcard. Production is unchanged (untagged deployments run under `live`). See the [epochs operator guide](guides/epochs.md). |
| **[Behaviour change — `make reset` reframed]** | `make reset` (`docker compose down -v`) is now positioned as the **whole-stack volume nuke** — all epochs across all sessions — not the everyday run-isolation tool. For an isolated rerun reach for a fresh `epoch` instead; `make reset` is for when you want the volumes themselves gone. The breadcrumb is reframed across the [channels](guides/channels.md), [persona-agents](guides/persona-agents.md), and [sessions](guides/sessions.md) guides. |
| **[Migration]** Auto-migration to `legacy` / `live` | A pre-v0.3.5 database auto-migrates on first open. Persona memory (`memory.db`) runs migrations v4→**v12**: the session axis added `session_id` (episodes/relationships v7, facts v8, notes v9, defaulting `'legacy'`); the epoch axis added `epoch_id` across all five tiers + the `relationships` primary key (v12, defaulting `'live'`). The channel store (`channels.db`) is at `channelStoreSchemaVersion = 6` (v5 session axis, v6 epoch axis). Every existing row lands under `session_id='legacy'` / `epoch_id='live'` and stays visible from the default resolution path — single-world deployments are byte-identical. |
| **[Behaviour change — RFC 0033 Phase 3]** Raw vendor IDs rejected | A `model:` in `config/agents.yaml` / `config/optimization.yaml` that is **not a declared alias** is now **rejected at resolve** with a loud `SystemExit` — the RFC 0033 §E raw-ID fall-through (a deprecation warning in v0.3.4) is removed, along with the `_infer_provider` heuristic, the `provider_inference` accessor/YAML block, and the `persatrix.llm.alias.raw_id_usage` gate counter. `config/optimization.yaml` `schema_version` bumps `"0.2"` → `"0.3"`. `models.aliases` is the single source of truth for model identity — provider is data, not inferred. |

---

## 4. Manual Test Sign-off

Release sign-off requires the full **v0.3.5 surface — 40 rows** executed and recorded in [`docs/manual-tests/v0.3.5-execution-report.md`](manual-tests/v0.3.5-execution-report.md) — 3 new rows plus the 37-row v0.3.4 surface carried forward and regression-checked against the session/epoch-routed config. The authoritative test index is [`docs/manual-tests/README.md`](manual-tests/README.md).

### Primary v0.3.5 Gates

| Area | Test ID |
|------|---------|
| Session operator surface — `new/use/list/archive/current` round-trip + the resolution chain (`--session` > env > pointer > `legacy`) — **new, primary gate** | `MT-SESSION-002` |
| F-3 recall isolation — a fresh session surfaces none of a prior session's rows **and** a within-session arc continues — **headline-claim test** | `MT-SESSION-003` |
| Epoch structural run-isolation — a fresh `PERSATRIX_EPOCH` / `--epoch`, same room + same `--user`, inherits nothing | `MT-EPOCH-001` |

### Results Summary (from execution report, `3ceb400` RC tip)

| Outcome | Count |
|---------|------:|
| ✅ Pass | 34 |
| ⚠️ Accepted-with-known-gap | 5 |
| ⊘ Deprecated (superseded) | 1 |
| ❌ Fail | 0 |
| ⬜ Not run | 0 |

Checklist:

- [x] [`docs/manual-tests/v0.3.5-execution-report.md`](manual-tests/v0.3.5-execution-report.md) exists (release-prep PR 1 — [#488](https://github.com/mkhomutov/Persatrix/pull/488))
- [x] Each test records date, tester, environment, and evidence
- [x] `MT-SESSION-002` recorded `Pass` (all five verbs live + the resolution-chain clarification + reserved `legacy` + registry-validated `use` + one-way archive)
- [x] `MT-SESSION-003` recorded `Pass` (cross-session recall isolation verified live on persisted rows; within-session continuity + multi-session no-bleed by the green `test_session_continuity.py` bridge)
- [x] `MT-EPOCH-001` recorded `Pass` (structural isolation via `test_epoch_run_isolation.py` + v12 `epoch_id` + persona epoch-boot confirmed live)
- [x] The session + epoch structural-isolation gates captured green on the RC tip alongside the new MTs
- [x] Every `Accepted-with-known-gap` row links a tracked issue or an RFC-closeout / sequencing follow-up bullet
- [x] **PR 4 hard-block before tag (must run live, not waivable by the automated counterpart)** — **both cleared live**:
  - [x] **Go alias cost-attribution gate** run **live** — cleared by PR 4 (Go 1.26.3 installed): both gate tests PASS (see §1). Covers the *cost-attribution* promise.
  - [x] **Full qualitative `MT-MEMORY-005` (V5) dementia arc** run **live on Anthropic `claude-sonnet-4-6`** — **all 5 legs Pass** (release-blockers 1/2/5 Pass: Mira recalled, no-phone honored, pragmatist self-consistency), commitment + paraphrase legs also Pass; the multi-session no-bleed re-run surfaced no Mira under a fresh session (F-3 isolation holds). Recall carried by the episodic tier — facts-tier-empty is the known [ISSUE-0054](issues/ISSUE-0054-rfc0026-facts-tier-extracts-no-facts.md) gap, not a leg failure. See the [execution report PR 4 appendix](manual-tests/v0.3.5-execution-report.md#re-execution--release-prep-pr-4-post-version-bump-rc-tip). Covers the *qualitative memory-continuity* promise.

> Both hard-block legs above were **environment-deferred in PR 1** (no Go toolchain; no working Anthropic key) and are now **run live in PR 4** — neither remains deferred. The dementia arc's facts-tier-empty observation is the tracked [ISSUE-0054](issues/ISSUE-0054-rfc0026-facts-tier-extracts-no-facts.md) gap (legs carried by episodic recall), not an environment defer. The remaining ⚠️ rows (`MT-COST-002`, `MT-COST-004`, `MT-CHAT-003`, `MT-CHAT-004`) carry forward unchanged from v0.3.4 with no v0.3.5 surface change; `MT-PERSONA-002` stays ⊘ Deprecated (covered by `MT-CHANNEL-002`/`004`).

---

## 5. Tag and GitHub Release Procedure

Use a clean `main` checkout after the final release-prep PR (PR 4 of the prep plan) merges.

```bash
git checkout main
git pull --ff-only origin main
git tag -a v0.3.5 -m "v0.3.5 — Session-Scoped Memory"
git push origin v0.3.5
```

GitHub Release checklist (post-merge manual steps after PR 4 merges):

- [ ] Create the GitHub Release from tag `v0.3.5`
- [ ] Use the curated `[0.3.5]` changelog section as the release notes baseline
- [ ] Include upgrade notes (§3.1) in the release body
- [ ] Include known gaps (§6) in the release body
- [ ] Attach binaries or container references if produced for this release — none expected (matches the v0.3.4 / v0.3.3 / v0.3.2 pattern)

---

## 6. Known Gaps to Document in Release Notes

These items remain intentionally incomplete and should be described as deferred scope, not regressions. Seeded from [v0.3.5-plan §Out of scope](v0.3.5-plan.md) and the [release-prep plan §Known follow-up issues](v0.3.5-release-prep-plan.md#known-follow-up-issues).

### v0.3.5-scope gaps

| Area | Description | Deferred to |
|------|-------------|-------------|
| Subject-scoped facts ([ISSUE-0084](issues/ISSUE-0084-fact-scope-by-subject-not-uniform-session.md)) | Facts are scoped uniformly by session like the other tiers; scoping a fact by its *subject* (so a fact about Alice follows Alice across sessions) is a continuity refinement, not a leak. | Beyond v0.3.5 — tracked separately |
| `--all-sessions` recall verb ([ISSUE-0086](issues/ISSUE-0086-operator-all-sessions-recall-verb.md)) | The `sessions="*"` cross-session debug mode exists at the recall layer but is reachable only programmatically; the `persatrix memory recall --all-sessions` operator verb is not built. The persona-runtime default context path is pinned never to reach `"*"` (the Phase 2 guarantee), so this is a missing convenience, not an isolation risk. | Tracked issue |
| `legacy` / `live` carve-out is permanent | Pre-v0.3.5 rows land under `session_id='legacy'` / `epoch_id='live'` and stay visible from the default path — the intended back-compat behaviour, by design. `persatrix memory legacy-prune` (a future cleanup verb) is scoped at [RFC 0031 Phase 4](rfcs/0031-per-session-namespacing-channels.md#phase-4-cleanup-and-documentation-pass), not built here. | RFC 0031 Phase 4 follow-up |
| Split-volume partial-reset footgun | The session registry (`channels.db`) and per-agent `memory.db` files live in different volumes that no single transaction spans; a partial `make reset` can strand `session_id`s in the registry vs. the per-agent store (the `legacy` carve-out keeps rows visible rather than orphaned). Reset both volumes together, or neither. A single-transaction society store (RFC 0029 Phase 3) closes this structurally. | RFC 0029 Phase 3 (v0.4.0) |
| DM-chat close-path write follows the persona's *boot* session/epoch (F-1) | A `--session` / `--epoch` on one `chat` call governs the recall query + channel-binding for that call but does not retag the asynchronous interaction-close write in a long-running persona (tagged with the session/epoch snapshotted at boot). Operator-documentation nuance, not an isolation defect — noted in the [sessions guide §4](guides/sessions.md). | Documented in the sessions guide |

### Carried forward from earlier releases

The v0.3.4-scope and earlier gaps documented in [v0.3.4 release checklist §6](v0.3.4-release-checklist.md#6-known-gaps-to-document-in-release-notes) remain open in v0.3.5 unchanged (memory-compression model literals [ISSUE-0072](issues/ISSUE-0072-memory-compression-hardcoded-model-literals.md) — **now routed through the alias layer** in [#484](https://github.com/mkhomutov/Persatrix/pull/484); the CPU local-model chat timeout [ISSUE-0073](issues/ISSUE-0073-cpu-local-model-chat-exceeds-default-timeout.md); the RFC 0024 `tick_interval_seconds` deprecation warning → v0.4.0 and its removal → v0.5+; salience-threshold calibration; RFC 0029 Phases 2–6 Postgres society backend; RFC 0009 Phases 3–4 sandbox + token auth; channels REST surface unauthenticated; external bridges; and the rest). The v0.3.5 release notes link the v0.3.4 checklist for full detail rather than re-listing them.

---

## 7. Summary Checklist

Single-page go/no-go gate. All items must be checked before tagging.

```text
[x] make test green on the post-bump RC tip (PR 4 ran LIVE: Go ./internal/... -race -cover all ok no races + session/epoch isolation 20 + facts/recall 640 + full unit 2887 + Rust 153 — 0 feature fail; full Python integration carried by CI)
[x] make lint clean (ruff + clippy v0.3.5 re-run by PR 4; golangci-lint / mypy carried by CI)
[x] make validate passes (+ doc-links 6464/324 clean)
[x] make proto then git diff --exit-code shows no generated drift (carried by CI; protoc not on host; no .proto change this PR)
[x] make check-licenses passes (PR 4 ran LIVE: Go go-licenses OK + Python 68 + Rust)
[x] THIRD_PARTY_NOTICES.md up to date, no genuine delta (local regen = env artifact only, reverted; no new prod dep)
[x] make generate-sanitizer-patterns-check passes (run LIVE by PR 4; Go↔Python parity in sync)
[x] agents/pyproject.toml updated to 0.3.5
[x] agents/observability/{metrics,tracing}.py service.version defaults updated to 0.3.5
[x] cli/Cargo.toml updated to 0.3.5
[x] cli/Cargo.lock regenerated and committed
[x] make all builds successfully (CLI persatrix 0.3.5 by PR 4; orchestrator go build carried by CI)
[x] CHANGELOG.md contains a curated [0.3.5] section with upgrade notes (Bug Fixes: ISSUE-0068)
[x] Existing [0.3.4] … [0.1.0] sections preserved verbatim
[x] All 40 manual-test rows recorded in docs/manual-tests/v0.3.5-execution-report.md
[x] No unresolved Fail in manual tests; zero Not run
[x] MT-SESSION-002 / MT-SESSION-003 / MT-EPOCH-001 recorded Pass (primary v0.3.5 gates)
[x] MT-MEMORY-005 full qualitative dementia arc (V5) run LIVE on Anthropic claude-sonnet-4-6 — all 5 legs Pass + multi-session no-bleed holds (§4); facts-tier-empty = known ISSUE-0054, legs carried by episodic recall
[x] Go alias cost-attribution gate run LIVE — CLEARED by PR 4 (Go 1.26.3 installed; both gate tests PASS)
[x] Session + epoch structural-isolation gates green (re-ran by PR 4 on the post-bump tip — 20 passed)
[x] Alias cost-attribution gate green on the post-bump tip (RUN LIVE by PR 4 — both gate tests PASS)
[x] Bored-persona cost-regression-gate green on the post-bump tip (release-blocker; PR trigger, not nightly)
[x] Docker smoke — session-scoped recall + fresh-epoch rerun + back-compat legacy/live default path + raw-ID fail-loud (carried forward live from the PR 1 3ceb400 run)
[x] Known gaps documented for release notes (§6 above)
[ ] git tag v0.3.5 created and pushed
[ ] GitHub Release published with changelog, upgrade notes, and known gaps
```
