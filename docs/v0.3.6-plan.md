# v0.3.6 Plan — Web Console (Slice 1: Interactions)

**Status**: 📋 Planning
**Target version**: v0.3.6 (RFC 0048 Phase 1 / Slice 1 — the embedded operator/tester web console)
**Created**: 2026-06-02
**Branch prefix**: `feature/v036-`
**Target**: `main`
**Merge strategy**: Squash merge per [BRANCHING.md](BRANCHING.md)
**Codename**: *Web Console* (proposed — maintainer owns the final codename)

**Goal**: orchestrate v0.3.6 end-to-end. One user-facing story: **open a URL and talk** — a newcomer runs the orchestrator with `--enable-ui`, opens `http://localhost:8080/ui`, picks a persona, chats with it, and watches a channel, with **zero CLI knowledge**. This is the first vertical slice of the [RFC 0048](rfcs/0048-operator-tester-web-console.md) web console — mostly a *render-over-existing-API* problem (chat, channels, agents already exist per RFC 0002 / 0011 / 0016), so a thin first slice lands inside the v0.3.x line.

[RFC 0048](rfcs/0048-operator-tester-web-console.md) (Operator & Tester Web Console — Vertical-Slice, Feature-Toggled UI), 👍 Accepted 2026-06-02, is the umbrella. Its **Phase 1 / Slice 1** is the only implementation scope here, and it already has a standalone PR plan — [`0048-phase1-pr-plan.md`](rfcs/0048-phase1-pr-plan.md) (6 PRs). That plan owns the per-PR detail; **this master plan wraps it** with the release-prep + tag phases.

Slice 1 in three new-code surfaces (everything else renders over today's API):

- **The embedded-asset scaffold** — `WithUI` `ServerOption` + `--enable-ui` (default off) + `embed.FS` static serving under `/ui/`, following the orchestrator's optional-subsystem pattern.
- **Two read-only endpoints** — `/api/v1/ui/config` (which panels to render) and `/api/v1/ui/context` (who the principal is), plus `config/ui.yaml` + its schema — the feature-toggle / forward-compat mechanism.
- **A Svelte SPA + JS build step** — the repo's first JS toolchain, isolated to `web/`, emitting static assets into the embedded directory; a Chat panel and a Channel-timeline panel over the existing API.

**Out of scope** — not part of this story:

- **Memory strip** — deferred to Slice 2 by RFC decision (2026-06-02); it needs a new Go↔Python gRPC read method into the `agents/memory/` tiers, not a thin handler. Its `memory_strip` toggle ships **off** (Phase 1 PR 2), so the panel lands additively with no Slice-1 rework.
- **RFC 0048 Slices 2–5** (memory inspector, isolation verifier, cost/observability, control plane) — v0.4.0+, each its own design pass + PR plan; Slice 5 is hard-gated on [RFC 0039](rfcs/0039-user-accounts-authentication.md) auth.
- **Channel real-time push (OQ4)** and **chat token streaming (OQ5)** — named later enhancements; Slice 1 polls and uses synchronous request/response, matching today's API.
- **Authentication / multi-tenancy** — RFC 0039 territory; this slice only commits to *not blocking* it (today's no-auth localhost mode is the degenerate `principal=local` single-tenant case).

The load-bearing companion is [`0048-phase1-pr-plan.md`](rfcs/0048-phase1-pr-plan.md) (the implementation workstream); see [Related documentation](#related-documentation) for the full set.

---

## Table of Contents

- [Why this plan exists](#why-this-plan-exists)
- [Acceptance for v0.3.6](#acceptance-for-v036)
- [Open-question status (resolved in the RFC)](#open-question-status-resolved-in-the-rfc)
- [Master Progress Overview](#master-progress-overview)
- [Dependency Graph (RFC-level)](#dependency-graph-rfc-level)
- [Phase 0 — This PR](#phase-0--this-pr)
- [Phase 1 — Implement RFC 0048 Phase 1 / Slice 1 (web console)](#phase-1--implement-rfc-0048-phase-1--slice-1-web-console)
- [Phase 2 — v0.3.6 release-prep plan](#phase-2--v036-release-prep-plan)
- [Phase 3 — v0.3.6 release-prep execution](#phase-3--v036-release-prep-execution)
- [Phase 4 — Tag and post-release follow-up](#phase-4--tag-and-post-release-follow-up)
- [Candidate fold-ins (maintainer decision)](#candidate-fold-ins-maintainer-decision)
- [ROADMAP hygiene](#roadmap-hygiene)
- [Risk and mitigations](#risk-and-mitigations)
- [Decision / next steps](#decision--next-steps)
- [Related documentation](#related-documentation)

---

## Why this plan exists

The [`0048-phase1-pr-plan.md`](rfcs/0048-phase1-pr-plan.md) is self-contained and could ship without an umbrella. Three reasons it gets one anyway:

1. **A patch release needs a release contract, not just a PR sequence.** Per [ROADMAP §Version Map](../ROADMAP.md#version-map), a version is defined by what a user *can do*; v0.3.6's promise — "open a URL and talk" — is a release-level gate no single PR owns. This plan makes it the acceptance bar and sequences the release-prep + tag phases the PR plan leaves out.

2. **A repo-first toolchain crosses a release boundary.** Slice 1 adds the repo's **4th language** (JavaScript) and a build step. This plan owns the release-level guarantee that the Go-only lane stays green against a committed placeholder while only the release/asset lane runs `make ui` — a v0.3.6 binary embeds a real bundle; a Go-only contributor is never blocked.

3. **The security posture is a release note, not a code change.** The console makes the *unauthenticated* REST surface browser-discoverable. `--enable-ui` defaults off and the orchestrator binds `127.0.0.1`, but "do not expose beyond localhost without an auth reverse proxy until RFC 0039" is an upgrade note the release-prep phase must surface, not bury in a guide.

Same thin-orchestration shape as [v0.3.5-plan.md](v0.3.5-plan.md): the RFC's PR plan owns the internals; this doc owns which phases, in what order, by what release gate.

---

## Acceptance for v0.3.6

The promise — *open a URL and talk: chat with a persona and watch a channel from the browser, no CLI* — is met when:

- **The scaffold is shippable and inert by default.** `--enable-ui` defaults **off**; with it off, `/ui/` is a clean 404 and no default runtime behaviour changes. `go build ./...` is green with **no** JS toolchain (the committed placeholder embed compiles).
- **The console boots off its own endpoints.** With `--enable-ui`, the SPA fetches `/api/v1/ui/config` + `/api/v1/ui/context`, renders only `enabled && available` panels (ignoring unknown ones), and derives `user_id` from the `/ui/context` principal — **no hard-coded or free-text user** (RFC §F forward-compat rule).
- **The Chat panel delivers the hero moment.** Pick a persona (`GET /api/v1/agents`), send a message (`POST /api/v1/agents/{id}/chat`, `participant_type:"user"`), see the `reply`; the v0.3.5 isolation story is demonstrable via optional session/epoch selectors that pass through to the request.
- **The Channel-timeline panel shows personas interacting.** History renders newest-first (`GET /api/v1/channels/{id}/messages`) and stays live by polling with **visibility-pause + error-backoff + head-poll de-dupe**; optional human publish posts into a group channel and surfaces the agent fan-out on the next poll.
- **The feature-toggle mechanism is real and validated.** `config/ui.yaml` ships Slice-1 panels on (`chat`, `channel_timeline`), later panels off (`memory_strip`, `cost`); `available` is runtime-derived (never read from YAML); `make validate` checks it against `schemas/ui.schema.json` (a stray `available:` key fails).
- **The story is documented and the security posture is loud.** `docs/guides/web-console.md` ships with the localhost-only / reverse-proxy security note; README has a "try it in the browser" entry; CHANGELOG `[0.3.6]` carries the off-by-default upgrade note; `MT-CONSOLE-001` is authored (executed in release-prep).

Out of acceptance: RFC 0048 Slices 2–5 (memory inspector / isolation verifier / cost / control plane — v0.4.0+); the memory strip ([RFC §G](rfcs/0048-operator-tester-web-console.md#g-api-gaps--required-backend-work)); channel real-time push (OQ4); chat token streaming (OQ5); authentication (RFC 0039).

---

## Open-question status (resolved in the RFC)

All five RFC open questions were resolved in [RFC §Open Questions](rfcs/0048-operator-tester-web-console.md#open-questions) (2026-06-02) and consumed by the [phase-1 PR plan](rfcs/0048-phase1-pr-plan.md#open-question-resolutions-locked-in-the-rfc-2026-06-02) — none re-litigated here. Summary:

| OQ | Topic | Resolution | Where it lands |
|----|-------|------------|----------------|
| **OQ1** | Frontend framework | **Svelte + Vite**, static output; the `WithUI(fs.FS)` contract is framework-agnostic so the choice does not leak past PR 3. | Phase 1 PR 3 |
| **OQ2** | Toggle surface | **`config/ui.yaml`** (consistent with `channels.yaml` / `agents.yaml`); absent file → Slice-1 defaults. | Phase 1 PR 2 |
| **OQ3** | Read-only memory endpoint / memory strip | **Deferred to Slice 2**; `memory_strip` toggle ships off so it lands additively. | Out of scope (toggle off, PR 2) |
| **OQ4** | Channel real-time | **Deferred**; Slice 1 polls the existing messages endpoint. | Phase 1 PR 5 (polling) |
| **OQ5** | Chat token streaming | **Deferred**; Slice 1 is synchronous request/response. | Phase 1 PR 4 (sync) |

The phase-1 plan additionally makes four implementation decisions (D1 hash-mode routing, D2 `go:embed` placeholder, D3 middleware placement, D4 `validate.py` wiring) in [§Plan-authoring decisions](rfcs/0048-phase1-pr-plan.md#plan-authoring-decisions-resolve-rfc-implementation-ambiguities) — also consumed, not re-opened.

---

## Master Progress Overview

Update as each PR merges; for multi-PR rows, roll up every constituent PR and use the final merge date.

| # | Phase | Title | Branch | Status | GitHub PR | Merged |
|---|-------|-------|--------|--------|-----------|--------|
| 0 | 0 | v0.3.6 master plan (this doc) + ROADMAP hygiene (Version Map v0.3.6 → 🚧 Planning) | `feature/v036-plan` | 🔀 PR open | — | — |
| 1 | 1 | RFC 0048 Phase 1 / Slice 1 implementation (scaffold → endpoints → SPA shell → chat panel → channel timeline → docs/closeout) | `feature/v036-rfc0048p1-…` | ⬜ Not started | — | — |
| 2 | 2 | v0.3.6 release-prep plan | `feature/v036-release-prep-plan` | ⬜ Not started | — | — |
| 3 | 3 | v0.3.6 release-prep execution (MT-CONSOLE-001 exec → doc/onboarding sweep → version bump + changelog → final pre-tag verification) | `feature/v036-release-prep-…` | ⬜ Not started | — | — |
| 4 | 4 | v0.3.6 tag + post-release follow-up | `feature/v036-post-release` | ⬜ Not started | — | — |

**Status legend**: ⬜ Not started · 🔄 In progress · 🔀 PR open · ✅ Merged · ⏭ Deferred

> **Granularity for row 1**: stands for *all six PRs in [`0048-phase1-pr-plan.md`](rfcs/0048-phase1-pr-plan.md)* (scaffold · config/context endpoints + `ui.yaml`/schema · JS toolchain + Svelte shell · Chat · Channel timeline · docs/closeout). That plan's [Progress Overview](rfcs/0048-phase1-pr-plan.md#progress-overview-phase-1) is the per-PR tracker; this row rolls it up.

---

## Dependency Graph (RFC-level)

```
v0.3.5 complete (✅ released 2026-06-01)
    │
    └── RFC 0048 (Operator & Tester Web Console — Phase 1 / Slice 1 only)
            ├── prerequisites all shipped: RFC 0002 (REST API), RFC 0011 (Channels), RFC 0016 (Chat)
            ├── NO dependency on RFC 0039 (auth) — Slice 1 is interact-only; principal=local
            │
            └── 0048-phase1-pr-plan.md (6 PRs, sequential with a PR4∥PR5 fan-out):
                    PR1 scaffold ─→ PR2 endpoints ─→ PR3 SPA shell ─┬─ PR4 Chat panel ──┐
                                                                     └─ PR5 Channel TL ──┴─→ PR6 docs/closeout
                     │
                     └─→  v0.3.6 — Web Console (Slice 1)

   ── deferred (NOT in v0.3.6) ──
   RFC 0048 Slice 2 (Memory Inspector)   → v0.4.0+  (needs a new Go↔Python gRPC memory-read method)
   RFC 0048 Slice 3 (Isolation Verifier) → v0.4.0+  (depends on Slice 2)
   RFC 0048 Slice 4 (Cost & Observability)→ v0.4.0+  (cost toggle ships off in PR 2)
   RFC 0048 Slice 5 (Control Plane)      → post-RFC 0039 (hard-gated on auth)
```

The six PRs are sequential except PR 4 (Chat) and PR 5 (Channel timeline), which depend only on PR 3's shell and may run in parallel. PRs 1–2 are Go + config + schema (no JS); PR 3 introduces the JS toolchain; PR 6 closes out once both panels merge.

> **Maintenance**: this graph mirrors [`0048-phase1-pr-plan.md` §Dependency Graph](rfcs/0048-phase1-pr-plan.md#dependency-graph); edit both in the same commit.

---

## Phase 0 — This PR

**Branch**: `feature/v036-plan`
**Goal**: land this master plan and open the v0.3.6 planning phase on the ROADMAP.

**Scope**:

- Add `docs/v0.3.6-plan.md` (this file).
- Add a v0.3.6 row to the [ROADMAP §Version Map](../ROADMAP.md#version-map) (between v0.3.5 and v0.4.0): status `🚧 Planning`, the user-facing "open a URL and talk" line.
- Refresh the [ROADMAP.md](../ROADMAP.md) header (`Last updated` / `Current phase` / `Current milestone`) to the v0.3.6 planning phase; update the "Next phase" forward reference to name v0.3.6 as the next cut, v0.4.0 following.
- **No** RFC-Master-Index change — RFC 0048 has no row yet (added by [phase-1 plan PR 6](rfcs/0048-phase1-pr-plan.md#pr-6-featurev036-rfc0048p1-docs-closeout--docs--status-closeout) at closeout; adding it here would collide). RFC 0048 stays `👍 Accepted` until Phase 1 PR 1 opens. See [ROADMAP hygiene](#roadmap-hygiene).
- No production code.

**Acceptance**: file exists and links cleanly (`python scripts/checks/doc_audit.py` doc-links check green); ROADMAP header names v0.3.6 as the current planning phase; Version Map v0.3.6 row is `🚧 Planning`; no production code touched; no RFC Master Index / RFC 0048 status edit.

---

## Phase 1 — Implement RFC 0048 Phase 1 / Slice 1 (web console)

**Branch prefix**: `feature/v036-rfc0048p1-`
**Goal**: execute the six-PR [`0048-phase1-pr-plan.md`](rfcs/0048-phase1-pr-plan.md) workstream that ships the interact-only web console behind `--enable-ui`. That plan owns per-PR scope, tests, and checklists; row 1 of the [Master Progress Overview](#master-progress-overview) stands for the whole sequence.

**Acceptance**:

- All six Phase 1 PRs merged per [`0048-phase1-pr-plan.md`](rfcs/0048-phase1-pr-plan.md), meeting every [Acceptance for v0.3.6](#acceptance-for-v036) gate.
- `make ui` builds clean from a fresh `web/` (lockfile committed) and a flag-on binary built after it serves the real Svelte shell; the CI release lane runs Node setup + `make ui` before the Go build, while the Go-only lane builds against the committed placeholder embed (`go build ./...` green with no JS toolchain).
- PR 6 lands the docs (`docs/guides/web-console.md` with the security note, README "try it" entry, CHANGELOG `[0.3.6]`, `MT-CONSOLE-001` authored), adds the RFC 0048 Master Index row (`⚠️ Partially Implemented`, target `v0.3.6 (Slice 1) + v0.4.0+ (Slices 2–5)`), and flips RFC 0048 `🚧 Implementing → ⚠️ Partially Implemented (Phase 1 / Slice 1)` with `make rfcs-check` green.

---

## Phase 2 — v0.3.6 release-prep plan

**Branch**: `feature/v036-release-prep-plan`
**Goal**: author `docs/v0.3.6-release-prep-plan.md`, modeled on [v0.3.5-release-prep-plan.md](v0.3.5-release-prep-plan.md). Inventory the MT surface (the new `MT-CONSOLE-001` fresh-stack script — `make ui`, `--enable-ui`, chat, reload-and-confirm-recall, session/epoch scoping, channel polling); populate the release-prep sub-PRs; specify the CHANGELOG `[0.3.6]` Upgrade Notes.

**Acceptance**: file exists; follows the v0.3.5 release-prep structure; Upgrade Notes inventory the new `--enable-ui` flag (off by default), the `config/ui.yaml` toggle file, the new `web/` JS toolchain + `make ui` build step, and the **load-bearing security note** (console makes the unauthenticated REST surface browser-discoverable; binds `127.0.0.1`; do not expose beyond localhost without an auth reverse proxy until RFC 0039). No code changes.

---

## Phase 3 — v0.3.6 release-prep execution

**Branch prefix**: `feature/v036-release-prep-`
**Goal**: ship the release-prep PRs (mirroring v0.3.5): MT-CONSOLE-001 execution report (`make ui` build, `--enable-ui` run, live-exercise chat + channel timeline) → onboarding/doc sweep (README + `docs/guides/web-console.md`) → version bump `0.3.5` → `0.3.6` + curated `[0.3.6]` changelog → final pre-tag verification.

**Acceptance**: every release-prep sub-PR merged; `MT-CONSOLE-001` re-run live on HEAD and recorded; `CHANGELOG.md` carries a curated `[0.3.6]` section with the off-by-default + security Upgrade Notes; version bumped per [`make bump-version`](../Makefile); the Go-only and release/asset CI lanes both green on the post-bump tip.

---

## Phase 4 — Tag and post-release follow-up

**Branch**: `feature/v036-post-release`
**Goal**: tag `v0.3.6`, publish the GitHub Release, and do post-release ROADMAP hygiene.

**Acceptance**: `v0.3.6` tagged; GitHub Release published; Version Map v0.3.6 → ✅ Released; `Current phase` / `Current milestone` and the next-milestone forward reference flipped (v0.4.0 team/hierarchy unless re-sequenced); RFC 0048 stays `⚠️ Partially Implemented (Phase 1 / Slice 1)` (Slices 2–5 remain); Merged PR History backfilled for the full v0.3.6 cycle.

---

## Candidate fold-ins (maintainer decision)

Surfaced during pre-planning triage. None is required for Slice 1; the maintainer owns whether to commit them — default is Slice 1 alone, so the CHANGELOG story stays singular.

| Item | Recommendation |
|------|----------------|
| `/ui/` rate-limit exemption ([phase-1 plan D3 / §Risk](rfcs/0048-phase1-pr-plan.md#risk-and-mitigations)) | **Defer (fast-follow).** Static fetches ride the shared `apiH` rate limiter; not pre-optimised. If a page load trips it in practice, exempt the `/ui/` prefix as a localised follow-up — track as an issue, don't fold into Slice 1 unless observed. |
| Channel real-time push (OQ4) / chat token streaming (OQ5) | **Defer.** Named later enhancements in the RFC; Slice 1 polls + synchronous chat is the contract. Revisit after Slice 1 lands. |
| CSRF token/header plumbing for browser `POST`s | **Build the seam, not the mechanism.** Moot while unauthenticated; PR 5's client is built to send whatever token/header the future RFC 0039 auth layer chooses, documented in PR 6's guide — no live mitigation until auth exists. |

---

## ROADMAP hygiene

Per [.github/copilot-instructions.md §Status Hygiene](../.github/copilot-instructions.md):

- **This PR (Phase 0)** → Version Map v0.3.6 row `🚧 Planning`; refresh the header. **No** RFC Master Index edit (no row yet — added by Phase 1 PR 6; RFC 0048 stays `👍 Accepted` until Phase 1 PR 1 opens).
- **Phase 1 PR 1 opens** → RFC 0048 `👍 Accepted → 🚧 Implementing` (per the [phase-1 plan](rfcs/0048-phase1-pr-plan.md#roadmap--status-hygiene)); stays `Implementing` across PRs 1–5.
- **Phase 1 PR 6 merges** → RFC 0048 → `⚠️ Partially Implemented (Phase 1 / Slice 1)`; add the RFC Master Index 0048 row; CHANGELOG `[0.3.6]`; `make rfcs` regenerates INDEX; `Last updated` refresh.
- **Phase 4 (tag)** → Version Map v0.3.6 → `✅ Released`; `Last updated` refresh; RFC 0048 stays `⚠️ Partially Implemented` (Slices 2–5 remain).

---

## Risk and mitigations

The per-PR implementation risks live in the [phase-1 PR plan §Risk and Mitigations](rfcs/0048-phase1-pr-plan.md#risk-and-mitigations). The release-level risks:

| Risk | Mitigation |
|------|------------|
| The new JS toolchain (repo's 4th language) destabilises CI or blocks Go-only contributors at a release boundary. | Go-only lane builds against the committed placeholder; only the release/asset lane runs `make ui` (Phase 1 gates `go build ./...` with no JS toolchain). Frontend isolated to `web/`; Node setup cached on the lockfile. |
| Shipping a browser console makes the **unauthenticated** REST surface more discoverable → accidental beyond-localhost exposure. | `--enable-ui` defaults **off**; the orchestrator binds `127.0.0.1`; the release Upgrade Notes + `docs/guides/web-console.md` state that exposure beyond localhost requires an auth reverse proxy until RFC 0039 — a first-class release note (Phase 2/3), not buried in a guide. |
| Scope creep: the memory strip or a later slice quietly expands the release past "open a URL and talk." | RFC decision (2026-06-02) defers the memory strip to Slice 2; its toggle ships **off**, landing additively. [§Candidate fold-ins](#candidate-fold-ins-maintainer-decision) makes every addition an explicit opt-in; default is Slice 1 alone. |
| Slice 1 ships the interact surface but the release reads as "the whole console," over-promising on memory/cost/control-plane panels. | The Version Map line and CHANGELOG scope it to chat + channel timeline; the RFC slice roadmap (§E) and this plan's dependency graph name Slices 2–5 as v0.4.0+, so the partial status is explicit. |

---

## Decision / next steps

1. Land this Phase 0 PR — the operational handle for opening v0.3.6.
2. Open RFC 0048 Phase 1 PR 1 (`feature/v036-rfc0048p1-withui-scaffold`) from [`0048-phase1-pr-plan.md`](rfcs/0048-phase1-pr-plan.md); flip RFC 0048 `👍 Accepted → 🚧 Implementing` on that PR.
3. Decide the [candidate fold-ins](#candidate-fold-ins-maintainer-decision) before release-prep (Phase 2) opens, so the CHANGELOG story stays singular.
4. Author `docs/v0.3.6-release-prep-plan.md` (Phase 2) once Phase 1 is merged, modeled on the v0.3.5 prep.

Fall-back if a reviewer pushes back on a v0.3.6 patch ahead of v0.4.0: the RFC scopes Slice 1 "inside the v0.3.x line" ([target `v0.3.6 (Phase 1 / Slice 1)`](rfcs/0048-operator-tester-web-console.md)) because it is render-over-existing-API with no new architecture, and bundling it into v0.4.0 would delay the community-growth win behind the larger team/hierarchy work. If preferred, the slice can instead ship as a `[Unreleased]` entry folded into the v0.4.0 cut — the implementation workstream is unaffected either way.

---

## Related documentation

- [RFC 0048 — Operator & Tester Web Console](rfcs/0048-operator-tester-web-console.md) — canonical spec; §D Slice 1 is this release's contract, §E is the slice roadmap, §Security is the load-bearing release note.
- [RFC 0048 Phase 1 PR plan](rfcs/0048-phase1-pr-plan.md) — the six-PR implementation workstream (Phase 1 of this plan); owns all per-PR detail.
- [v0.3.5 plan](v0.3.5-plan.md) — structural template.
- [v0.3.5 release-prep plan](v0.3.5-release-prep-plan.md) / [v0.3.4 release-prep plan](v0.3.4-release-prep-plan.md) — release-prep templates for Phase 2.
- [ROADMAP §Version Map](../ROADMAP.md#version-map) — release definition.
- [BRANCHING.md](BRANCHING.md) — patch-release branching convention.
- Prerequisite RFCs (all shipped): [RFC 0002](rfcs/0002-rest-api-server.md) (REST API), [RFC 0011](rfcs/0011-channels-bridges.md) (Channels), [RFC 0016](rfcs/0016-human-participant-chat-interface.md) (Chat); forward-compat: [RFC 0039](rfcs/0039-user-accounts-authentication.md) (auth, gates Slice 5).
