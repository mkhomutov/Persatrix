# Orchestr8 Agent Runtime (Python)
FROM python:3.12-slim

WORKDIR /app
COPY agents/pyproject.toml agents/
RUN pip install --no-cache-dir -e agents/
COPY agents/ agents/
COPY config/ config/

ENTRYPOINT ["python", "-m", "orchestr8_agents.server"]
