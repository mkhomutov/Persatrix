# Enforce package.json "engines" (node >=20) on install. Without this, the
# engines field is advisory — npm warns but proceeds, so a contributor on an
# older Node could build a subtly broken bundle. With it, `npm ci`/`npm install`
# (and the `make ui` / CI lanes that call them) fail fast on an unsupported Node.
engine-strict=true
